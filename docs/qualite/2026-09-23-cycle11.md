# Cycle 11 : reprise Linux sur DreamQuest SYN, paquet .deb et outils des agents

## Périmètre et décision

Premier cycle mené hors du Mac, sur la copie de passation du 23 septembre 2026
déposée sur DreamQuest SYN (Ubuntu, Linux 7.0, 4 cœurs, 11 Go), à la demande de
Syn pour Ludo. Cible web et tests seulement : Rust et cargo sont absents, aucun
binaire Tauri n'est construit. Aucune release, aucun tag, aucun push. Les
correctifs sont des commits locaux de la branche `boucle/c11-linux` (arbre de
travail séparé) ; `main` reste à `443ef4f3`.

Le cycle 10 est sorti de HUMAN_GATE par la transition permise HUMAN_GATE vers
RESTART, sans forçage ; P-092 reste pendante, non acceptée.

## Recalibrage Linux

Dépendances recréées dans le dossier (`uv sync --locked`, Python 3.12.3 du
système ; `npm ci` frontend et racine) ; Chromium 145 de Playwright 1.58.2 déjà
présent. Profil jetable sous le TMPDIR du dossier de passation : ailleurs,
`encryption.py` ne le reconnaît pas comme jetable et écrit une clé dans le
trousseau SecretService de la session. Pile jetable Vite 5173 vers moteur
17393 (5173 et 1420 sont les seules origines CORS de développement) ; E2E sur
1420 et 17394.

| Suite (commandes de la CI) | Mac, 22/09 | DQ SYN, main 443ef4f3 | DQ SYN, branche 75f4aea1 |
|---|---|---|---|
| pytest principal | 3 248 + 3 ignorés | 3 248 + 3 ignorés | 3 308 + 4 ignorés |
| pytest autonome | 182 + 4 xfail | 182 + 4 xfail | 182 + 4 xfail |
| Vitest | 2 625 | 2 624/2 625 sous charge | 2 625/2 625 |
| E2E desktop | 100 | 100, sans relance | 100, sans relance |
| mypy | 951 (cliquet) | 951 | 951 |

Le seul rouge de main est B-953 : `MemoryPanel.perimetre.c9` dépasse sa borne de
5 s quand Vitest tourne avec pytest en parallèle (2,95 s isolé, déjà rouge sous
charge sur le Mac au cycle 10). Cinq instruments recalibrés (preuves dans
`.app-loop/cycles/11/calibration/`). Sur cette machine, `grep` en shell
interactif est un alias vers ugrep ; le backend lance `/usr/bin/grep`, GNU grep
3.11.

## Défauts du cycle

30 défauts enregistrés, B-948 à B-977 : 19 corrigés en TDD (test rouge, sabotage
rouge puis restauration), 2 rejetés, 4 différés, 5 confirmés restant ouverts.

- Paquet Linux : B-948 (haut) : sur le .deb publié de la 0.74.0, `docx/parts`,
  `pptx/oxml` et `pptx/shapes` manquent sous `/usr/lib` en lecture seule, et
  toute génération DOCX échoue (pied de page) ; témoins embarqués par le spec,
  contre-épreuve PyInstaller réelle, et job CI dédié (B-956). B-949 et B-954 :
  les outils du poste recevaient le LD_LIBRARY_PATH du bundle (OpenSSL 3.0.2 et
  libstdc++ GLIBCXX_3.4.30 du bundle chargés à la place de ceux du système).
  B-950 : `build-sidecar.sh` ne gérait pas le onedir Linux.
- Hermétisme : B-955 : la suite de tests supprimait le verrou Qdrant du vrai
  profil `~/.therese` (chemin en dur) ; constaté sur DQ SYN pendant le
  recalibrage (verrou périmé d'une instance arrêtée, aucune donnée métier).
- Outils des agents : B-957, B-958, B-960, B-961 (liste impossible par un lien,
  cas de Zézette sous macOS), B-962, B-963, B-965 (aucun chemin absolu au
  modèle), B-964 (un outil hors schéma s'exécutait : Katia, en lecture seule,
  pouvait écrire dans le vrai dépôt), B-969 (la recherche rendait le contenu des
  `.env*`, clés comprises, que `read_file` refuse).
- Routage des modèles des agents : B-967 (codestral et deepseek-v4 retombaient
  sur le modèle principal), B-968 (haut : un agent local basculait en silence
  vers un fournisseur cloud après deux échecs d'Ollama).
- Rejetés : B-951 (latest.json publie bien une cible .deb signée), B-952 (repli
  local conçu, reformulé en proposition).
- Différés : B-953 (marge du test), B-959 (arguments libres de pytest, vitest,
  ruff dans `run_command`), B-966 (fournisseur deviné d'après le nom du modèle),
  B-970 (BUG-181 Discord, non reproduit en web à 1280 x 800).
- Discord, Dr_logic-3D (v0.74.0) : BUG-182 reproduit (horloge Playwright) et
  corrigé (B-971, l'heure suit le contenu affiché) ; BUG-183 corrigé (B-972 :
  cinq panneaux de travail enfreignaient la règle « Fermer » nommé à l'écran
  de BoutonFermerLePanneau) ; BUG-181 non reproduit (B-970).
- Ouverts : B-973 à B-977, trouvés par la ronde B (Échap qui jette une saisie,
  question d'abandon sur formulaire vierge, focus perdu après Échap,
  calendrier par défaut créé en double sous concurrence, réponse de recherche
  périmée). Confirmés, non corrigés faute de temps.

## Contradicteurs et lecteurs

Grok non utilisé (facturation à l'usage non écartée, puis consigne de Ludo).
Codex (abonnement ChatGPT) : le bac à sable bubblewrap est refusé sur DQ SYN et
la première revue a été comptée « réussie » sans avoir rien lu ; relancé sous
Landlock. Onze passes sur le diff, du NO-GO au GO, dont quatre ont trouvé des
défauts dans les correctifs eux-mêmes (R-3, R-5, R-6 Windows, masquage et
routage). Quatorze lecteurs Claude indépendants ont relu les fichiers touchés ;
leurs relevés ont donné B-957 à B-969. Carte validée à 100 %, 1 602/1 602,
1 023 rapports.

## Plateau

Ronde A (orchestrateur, tête 75f4aea1) : toutes les portes vertes, pytest
3 308 + 4 ignorés, autonome 182, contre-épreuve PyInstaller 5/5, Vitest
2 625/2 625, E2E 100/100, tsc, ESLint, mypy 951, recette UI 14 contrôles. Le
gestionnaire la refuse pour une seule raison : un invariant divergent non
arbitré (`tests/test_b963_b964_outils_agents.py`, lecteurs n°9 et n°10 : deux
angles de deux tests distincts du même fichier).

Ronde B (auditeur Claude indépendant, 75f4aea1) : suites vertes (Vitest
2 625/2 625, pytest ciblés 148 et 33), 62 transitions dont 51 conformes, cinq
défauts produit (B-973 à B-977). Plateau 0/2. Après les correctifs Discord
(4e765966, 378a7a18) : dossier prototype 514/514, E2E 100/100 ; ces deux
commits ne sont couverts par aucune ronde.

## Portail humain

Le portail HUMAN_GATE du cycle 11 n'est pas atteint : la boucle est en REPAIR
et suspendue en fin de session. Attendent une décision de Ludo : l'arbitrage de
la divergence ci-dessus (recommandation : concordant) ; B-959 et B-966
(P-100, P-103) ; P-092 et les propositions P-096 à P-103, dont les trois
suggestions de Dr_logic-3D (P-096 publipostage : faire autrement ; P-097 frise
des travaux : faire, version minimale ; P-098 vocabulaire des capacités :
faire) ; la fusion de `boucle/c11-linux` dans `main` et tout push.

## Limites

Aucun binaire Tauri ni .deb reconstruit ; correctifs Windows et macOS vérifiés
par simulation ; aucune génération de modèle ni bascule réelle vers un
fournisseur ; B-948 prouvé sur PyInstaller 6.18 onedir et sur le contenu du
.deb publié, pas sur un .deb corrigé installé.
