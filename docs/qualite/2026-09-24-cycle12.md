# Cycle 12 : retour sur le Mac, propositions de Dr_logic-3D et effacement RGPD

## Périmètre et décision

Cycle repris sur le Mac le 24 septembre 2026, après le cycle 11 mené par Syn sur
DreamQuest SYN. Ordre validé par Ludo (« oui go ») : recalibrer sur le Mac,
solder la dette de `search_codebase` (0.74.1), puis P-096, P-097, P-098, P-099,
P-100 et P-103, puis les bugs B-1029 à B-1039. Les réparations sont passées
avant l'implémentation, comme le veut la boucle. Grok et Codex étaient
indisponibles : relectures de design, lecteurs de carte et ronde B sont des
rôles Claude séparés (diversité de modèles dégradée).

Aucune release. Tout est sur `main`, poussé au fil de l'eau (commits en
français, un sujet par commit).

## Propositions livrées

| Proposition | Contenu |
|---|---|
| P-097 | Frise des travaux récents, heures de début et de fin lues en UTC serveur |
| P-098 | Centre des capacités : « Action » fondu dans « Vue », légende qui dit ce que fait le clic |
| P-099 | Recette automatique du paquet `.deb` dans le workflow de release |
| P-100 | Arguments des commandes des agents bornés (pytest, vitest, ruff, npm) |
| P-103 | Le fournisseur reste attaché au modèle d'un agent ; un agent local ne bascule jamais en ligne |

P-096 (brouillons personnalisés en lot) n'est pas codé : deux revues de design
NO-GO. La V2 a un défaut bloquant (la reprise d'un lot arrêté n'a rien pour
recréer les brouillons). Plan, revues et onze décisions pour Ludo :
`docs/plans/2026-09-24-p096-brouillons-en-lot.md`.

## Changement de comportement à annoncer

B-1071 : quand Ollama est arrêté, le chat et l'atelier documentaire ne
basculent plus vers un fournisseur en ligne. L'échec est dit à l'écran. C'est
la promesse de l'onglet Modèles (« En local, rien ne quitte ton ordinateur »),
mais un utilisateur qui comptait sur le repli le verra. À mettre au changelog.

## Défauts du cycle

56 bugs fermés en TDD, chacun avec un test rouge avant le correctif et un
sabotage qui le refait rougir : B-1029 à B-1141 (sélection), plus B-835, B-959
et B-966. Trois rejetés, 60 différés au cycle 13.

- **Recherche des agents et secrets** : motifs démesurés, fichiers sensibles
  déguisés, dossiers de dépendances, puis `prod.env`, `.git-credentials`,
  `.ssh`, `.aws`, `.gnupg` (B-1040 à B-1049, B-1084) ; P-100 contourné par
  « pytest @fichier » et « -t --dir=/ » (B-1079).
- **Effacement RGPD** : la purge totale laissait les clés API servies par
  quatre mémoires (cache des clés, service des modèles, clé Brave, extracteur
  d'entités), corrigé en trois temps (B-1124) ; elle oubliait aussi les travaux
  et la synchro de dossiers, noms de fichiers et chemins locaux compris
  (B-1129).
- **Données** : mode démo qui écrivait le persona fictif sur la vraie fiche
  (B-1080) ; adresse e-mail double acceptée par cinq portes d'écriture
  (B-1074, B-1081, B-1119) et effacée par la synchro (B-1107) ; réimports qui
  déliaient le client ou remettaient un statut par défaut (B-1083, B-1106) ;
  synchro tableur qui réécrivait « pending » sur tout livrable validé depuis le
  cycle 6 (B-1109).
- **Écrans** : focus, confirmations en ligne, messages amenés dans la vue,
  devis et factures (B-1029 à B-1039, B-1059 à B-1070) ; régression P-097 de
  la frise (B-1077) ; commande de secours macOS qui visait `THÉRÈSE.app` au lieu
  de `THERESE.app` (B-1085).
- **Tests instables** : quatre tests sensibles à la charge ou au runner
  corrigés par leur cause (B-835, B-1118, B-1141, B-1142) ; le correctif
  systémique (délais globaux de Testing Library et de Vitest) est B-1143.

## Dernière passe de la carte

Carte différentielle à 100 % (1645 fichiers), relue par dix passes de lecteurs
(G à P). Règle de convergence tenue : seuls les défauts graves (perte de
données, sécurité, régression du cycle) ont été corrigés avant le plateau, le
reste a été numéroté et différé. Deux leçons, déjà écrites dans la mémoire, ont
coûté des tours :

- un test du cache des clés vérifiait l'instrument, pas la clé réellement
  servie au chat ; le lecteur suivant l'a vu (« tester le parcours, pas la
  classe ») ;
- un correctif poussé après les seules suites voisines a cassé une garde
  textuelle ailleurs (B-1135) : la suite complète passe avant le commit.

## Plateau

Instruments recalibrés sur le code final (lanceurs de tests, navigateur,
capture, réseau, journaux), preuves dans `.app-loop/cycles/12/calibration/`.

Première ronde A (orchestrateur, tête 2c367ef4) : propre, plateau 1/2.

Première ronde B (auditeur Claude indépendant, `cycle-12-round-b-b4c439ae`) :
non propre. Vitest 1 échec sur 2 725 au second passage, sous une charge de 15
à 18 (`PromptLibrary.cycle6`, sophie-02, B-1142) ; transitions 24/25, la
recette `.deb` de P-099 n'ayant pas pu être rejouée depuis le Mac. Tout le
reste vert à ses propres mesures (pytest 3 476, autonome 186, tsc, ESLint,
ruff, 0 erreur console, réseau et moteur, carte 100 %). Compteur remis à 0.

Seconde ronde A (orchestrateur, tête d1f7d9e9) : propre. En chemin, le
constructeur de la ronde lisait le mauvais JSON d'audit (motif qui ne voyait
pas le fichier de l'audit complet) ; corrigé, il exige maintenant un audit
postérieur au commit de tête. La CI de 2c367ef4 a ensuite montré que B-1142
n'était corrigé qu'à moitié (le repli de catégorie de sophie-02 dépassait
aussi une seconde) : second correctif, rondes à refaire.

Un passage Vitest sur 59622d61 a été invalidé par la machine elle-même : macOS
recalculait le stockage (disque à 92 %, charge jusqu'à 32), huit fichiers de
test n'ont pas pu démarrer. Rejoué à charge basse : vert.

**Plateau 2/2 sur 59622d61.** Ronde B (auditeur Claude indépendant,
`cycle-12-round-b-dfcba8ae`) : Vitest 2 725/2 725, pytest 3 472 réussis et 4
ignorés, autonome 182 et 4 xfail, tsc, ruff, ESLint 27 avertissements pour un
plafond de 27, 0 erreur console, réseau et moteur sur 90 cas, carte 100 %,
transitions 25/25 dont **P-099 rejouée sur DQ SYN** (0.74.0 rouge sur les
trois manques de B-948, 0.74.1 vert, paquets identiques à ceux des releases
GitHub). Ronde A (orchestrateur) sur la même tête : propre. Boucle en
HUMAN_GATE ; GAP_SCAN n'a pas relancé de campagne de personas, le portail
porte les décisions relevées pendant le cycle.

## Portail humain

Attendent une décision de Ludo :

- **Release** : aucune sans son GO. Contenu prêt, plateau 2/2 : les cinq
  propositions, 56 correctifs, le changement de comportement de B-1071.
- **P-096** : onze décisions (libellé « Écrire un e-mail », signature,
  RGPD, conservation du modèle, avertissement 24 h, trace sur la fiche, etc.).
- **Import et formules (B-1120)** : « +33… » est stocké avec une apostrophe ;
  la règle de l'export épargne « +33 » mais laisse passer « +1+cmd|… ». Les
  deux côtés sont à corriger ensemble.
- **Le tableur fait-il foi ?** (B-1108, B-1125) : la synchro délie un client,
  remet un statut par défaut ou échoue sur une ligne incomplète selon qu'on
  lise la feuille comme un miroir ou comme une source partielle.
- **Confidentialité (B-1139)** : l'extraction d'entités passe par le service
  global ; une conversation locale peut partir vers un fournisseur en ligne.

## Limites

Aucun binaire Tauri reconstruit, application installée non relancée : les
preuves visuelles viennent de la pile Vite + moteur jetable alignée sur la
tête. La recette `.deb` a été rejouée sur DQ SYN par la ronde B mais n'a jamais tourné sur un runner GitHub. Les suites E2E
Playwright ne sont pas rejouées localement ; la CI de 94294176 les passe.
Un échec Windows de la CI sur 3c20fafe venait d'un test qui comparait le
numéro de volume brut (B-1141) ; le produit n'était pas en cause.
