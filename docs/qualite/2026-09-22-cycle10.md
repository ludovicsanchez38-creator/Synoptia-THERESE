# Cycle 10 : projets, démonstration et navigation au clavier

## Périmètre et décision

Ce lot poursuit la boucle après la vérification de la release 0.74.0-alpha.
Il ne constitue pas une nouvelle publication : aucun bump, tag, binaire,
déploiement, remplacement de l'application ou message Discord n'est inclus.

Décision explicite de Ludo le 22 septembre : les formulaires de projets restent
masqués et en lecture seule en mode démo, avec un rappel de désactiver ce mode
pour modifier. Les valeurs fictives ne sont jamais enregistrées dans les données
métier. La confirmation de suppression depuis le panneau conserve, elle, le
véritable identifiant et un nom d'affichage masqué.

## Défauts reproduits

| Référence | Observation et correction attendue |
|---|---|
| B-934 | Distinguer lecture en cours, absence de dossier et panne ; proposer une reprise. |
| B-935 | Masquer les prompts de l'historique Images, les textes alternatifs et le récapitulatif de confirmation, sans modifier la requête envoyée. |
| B-936 | Composer les fonds translucides dans la recette de contraste ; signaler les rendus non modélisés comme indéterminés. |
| B-937 | Supprimer le clignotement infini résiduel avec réduction des animations applicative ou système. Les transformations étaient déjà supprimées. |
| B-938 | Confiner et restituer le focus dans la confirmation de suppression d'une facture, y compris après disparition de la dernière ligne. |
| B-939 | Fiches projets masquées en lecture seule, y compris au démarrage sans table globale de remplacement ; synchronisation et confirmations comprises. |
| B-940 | Invalider les réponses de fichiers d'un ancien projet ou d'une ancienne ouverture ; réinitialiser le sélecteur natif de fichiers. |
| B-941 | Nommer les quatre confirmations Contacts/RGPD, gérer leur focus et le retour après rechargement ou suppression. |
| B-942 | Conserver l'indication de délai du modèle local avec une bulle assistant déjà créée, sans second indicateur de réflexion. |
| B-943 | Empêcher un plan de synchronisation préparé tardivement pour A de devenir applicable depuis B ; isoler aussi les sondages et le journal. |

B-933 est différé : le backend actuel n'envoie plus les résultats d'extraction
d'entités au flux de discussion. Le scénario frais n'atteint donc pas la surface
signalée. La compatibilité avec d'anciennes données persistées reste non vérifiée.
Le sabotage générique de calibration enregistré auparavant sur cet identifiant
ne prouve aucune correction de B-933.

## Méthode et preuves

Les reproductions utilisent des données synthétiques, des réponses HTTP
interceptées ou des API doublées. Le moteur jetable utilise le port 17393 et
`/tmp/therese-demo-c10/data`. Le port 17293 et les données réelles sont exclus
des tests de mutation.

Chaque changement dispose de tests comportementaux rouges avant correction.
Les revues indépendantes ont ajouté trois cas manquants : même fichier choisi
après changement de projet, styles `::first-line`/`::first-letter` et masquage à
froid. Elles ont aussi reproduit B-943 et la perte du focus après suppression.

Instruments : Vitest, TypeScript, ESLint, Playwright Chromium. Les fixtures
permanentes sont conservées avec les composants et dans
`src/frontend/scripts-recette/`. Les journaux bruts, contrôles de calibration,
captures et revues sont conservés localement sous `.app-loop/cycles/10/`.

Recette UI rejouable : `node src/frontend/scripts-recette/recette-cycle10.mjs`,
sur Vite 1420 et le moteur jetable 17393. Elle couvre panne/reprise du dossier,
lecture seule démo, suppression de facture et les quatre confirmations Contacts,
en clair et sombre à 1280 × 800. Les captures attendent la fin des fondus.

La recette de contraste est volontairement partielle : un résultat indéterminé
n'est pas une conformité. Elle ne remplace pas l'inspection visuelle des
recouvrements, médias, SVG ou géométries complexes.

Diversité des modèles dégradée : Grok a répondu par une erreur d'authentification
expirée, pas une preuve de quota épuisé. Les rôles de reproduction, réparation et
contradiction ont été répartis entre agents distincts. Aucune exécution Claude
n'est revendiquée pour ces lots Astra.

## État de vérification

Snapshot fonctionnel intégré : `eced4f3b`. TypeScript passe. Le lint complet
(`npm run lint`) passe avec zéro erreur et les 27 avertissements du seuil
existant. La vérification des huit composants modifiés n'en relève que trois,
préexistants, sur les dépendances d'effets de MessageList, InvoicesPanel et
MemoryPanel.

- Recette UI : sept parcours en clair et sombre, soit 14 contrôles réussis.
  Les seules réponses 503 sont les pannes volontairement injectées pour tester
  le réessai. Les captures sont inspectées après stabilisation des fondus.
- Images : deux parcours supplémentaires dans l'application réellement servie,
  historique, sélection et confirmation masqués, sans génération. Quatre captures
  inspectées ; aucune erreur console, HTTP ou requête échouée.
- Synchronisation concurrente : même recette Chromium sur `f59f53d9` puis sur le
  correctif. Avant : racine A affichée dans B et bouton Appliquer présent. Après :
  racine B conservée, plan A ignoré, seul le plan B peut être appliqué.
- Contraste : 16 fixtures navigateur passent, dont les états indéterminés requis
  pour les effets non pris en charge. Ce chiffre n'est pas un audit de 16 écrans.
- Réduction des mouvements : les trois scénarios navigateur passent ; points
  immobiles pour les préférences applicative et système, témoin animé sans ces
  préférences.
- Contre-épreuve sur l'ancienne version : 69 échecs parmi les mêmes 82 tests
  comportementaux, dans un worktree isolé sans modification du code de main.
- Le moteur jetable répond sain et son journal contient 104 lignes, sans ERROR,
  au contrôle du 22 septembre à 19:00 UTC.

La première suite complète après intégration compte 2 623 succès sur 2 625,
avec deux échecs dans des tests anciens (accordéon des prompts et périmètre des
contacts). Les cinq tests de ces deux fichiers passent lors du contrôle ciblé.
Le rejeu complet avec deux workers passe : **2 625 tests réussis sur 2 625**,
sans changement d'assertion, sans exclusion et sans relance automatique des
tests en échec (`frontend-complet-borne.json`, sortie 0). Les premiers échecs
restent conservés ; leur diagnostic ne repose pas sur le seul rejeu vert.

La carte différentielle est validée : **1 589 fichiers sur 1 589** dans
`src`, `tests`, `scripts` et `.github`. Les 23 fichiers modifiés ou ajoutés ont
fait l'objet de deux lectures, soit 16 rapports supplémentaires. Le premier
passage du validateur a rejeté des identifiants de rapports dupliqués ; seuls
ces identifiants ont été rectifiés avant le second passage réussi.

Les dix défauts B-934 à B-943 sont clôturés avec leurs preuves. Un passage en
ZERO_CHECK a eu lieu, sans plateau annoncé. Un arbitrage humain a été demandé sur
deux invariants complémentaires du test de suppression des factures : absence
de suppression sans confirmation et retour du focus après la dernière facture.
Le compteur ne sera pas forcé avant cet arbitrage et les deux rondes propres.

Une recette A supplémentaire est terminée sur le code fonctionnel `eced4f3b` :
800 et 1 280 pixels, clair et sombre, 21 transitions principales par contexte
(84 occurrences), plus deux états Images par contexte. Les huit parcours,
les 21 tests ciblés et l'inspection des 44 captures passent. Les succès de
suppression sont intégralement simulés avant le serveur. Aucune nouvelle anomalie
n'a été détectée sur ce périmètre ; ce n'est pas un plateau global. Le rapport
`zero-check/ronde-a.md` explicite les limites et la participation antérieure de
son auteur à trois correctifs, qui exige une contradiction d'auteur distinct.

Le diagnostic des deux rouges est conservé dans `diagnostic-suite.md`. Contacts
a dépassé les cinq secondes de délai sous charge. Pour les prompts, un clic
synthétique lancé par MutationObserver avant les effets de montage reproduit
une course ; les trois clics ordinaires et les six recherches passent. Ce cas
est conservé en B-944, différé faute d'atteignabilité utilisateur démontrée,
et non présenté comme corrigé ou comme simple lenteur d'animation.

## Contrôle CI après push

Le lot et sa documentation ont été poussés sur `main` au commit `ed035d98`.
Le job [Audit sécurité du run 35771978192](https://github.com/ludovicsanchez38-creator/Synoptia-THERESE/actions/runs/35771978192/job/106895669491)
échoue sur l'audit Python, après réussite de l'audit NPM. Sa sortie réelle
signale quatre avis non exceptés :

- AnyIO 4.12.1 : CVE-2026-63374 et CVE-2026-64847, correction annoncée 4.14.2.
- Soup Sieve 2.8.4 : CVE-2026-85999 et CVE-2026-86000, correction annoncée 2.9.0.

Ces dépendances n'ont pas été modifiées par les correctifs d'interface. Le journal
est conservé dans `.app-loop/cycles/10/ci-audit-dependances.log`. B-945 enregistre
ce blocage confirmé ; cela ne démontre pas une exploitation dans THÉRÈSE.
La boucle est revenue en REPAIR pour une mise à jour ciblée et testée en worktree
isolé, sans ajouter d'exception de sécurité et sans changer la venv du moteur
jetable ni celle de main. Le run initial est désormais terminé : frontend,
backend principal et autonome, ruff, mypy et clippy passent ; seul l'audit échoue.
Le workflow E2E séparé du même commit passe également, ainsi que les deux
jobs du [workflow Windows 35771978056](https://github.com/ludovicsanchez38-creator/Synoptia-THERESE/actions/runs/35771978056).

### Correction du verrou Python

Le commit `c422067c`, intégré et poussé sur `origin/main`, ne modifie que
`uv.lock` : AnyIO passe à 4.14.2 et Soup Sieve à 2.9 (normalisation de 2.9.0).
Les contraintes applicatives, les workflows et les exceptions sont inchangés.
La comparaison structurée du verrou ne relève aucun autre changement de paquet.
Le verrou et son export ont été contrôlés avec les versions locale et CI de UV.

L'audit identique à la CI passe dans un environnement neuf : aucun avis non
excepté, **16 avis préexistants toujours ignorés**. Le projet éditable n'est pas
audité comme paquet publié. Ce résultat ne signifie pas zéro vulnérabilité.
La revue indépendante distingue les versions affectées de l'exploitabilité
applicative, qui n'a pas été démontrée.

Contrôles sur les nouvelles dépendances, avec Python 3.13.5 sur macOS arm64 :

- Backend complet : **3 248 passés, 3 ignorés**, aucun échec ni erreur.
- Racine backend autonome : **182 passés, 4 xfail**.
- Annulation, RGPD et Gmail ciblés : **52 passés** ; indexation : **9 passés**.
- Sept contrôles synthétiques supplémentaires passent : vrai repli Gmail HTML
  seul, trois sélecteurs CSS, transmission IDNA au contexte SSL doublé, vrai
  worker AnyIO écrivant sur stderr et annulation. Aucun handshake TLS réel.

Le premier passage complet comptait un timeout pendant un import de SciPy,
avant l'assertion concernée. Le rejeu isolé puis le second passage complet
passent avec le même seuil de 30 secondes, sans changement du code ou du verrou.
Les trois sorties sont conservées ; import à froid et contention restent des
hypothèses, pas une cause établie.

Preuves locales : `repair-dependances.md`, `revue-dependances-portee.md`,
`dependances-audit-rouge.log`, `dependances-audit-vert.log`, journaux et JUnit
`dependances-*.xml` sous `.app-loop/cycles/10/`. Les contre-épreuves n'ont pas
touché aux données réelles ni aux environnements Python de main et du moteur
jetable. Les recettes UI précédentes ne sont donc pas présentées comme une
validation intégrée avec ces nouvelles dépendances.

B-945 est clôturé sur ces preuves locales et la boucle revient en ZERO_CHECK.
La nouvelle CI GitHub doit encore confirmer le résultat sur Linux et Windows.
Le rafraîchissement de la carte après ce commit ne trouve aucun fichier source
à relire ; la couverture est toujours validée à 100 %.

## Point de reprise

L'arbitrage humain reste requis par `boucle-amelioration-app` avant le plateau :
confirmer ensemble l'absence de suppression de facture sans confirmation
explicite et le retour du focus sur « Nouveau devis ou facture » après suppression
de la dernière facture. Aucun arbitrage automatique n'a été enregistré à la
place de Ludo. Le `next-action` mécanique recommande encore une ronde, mais il
ne dispense pas de cet invariant du skill.

Après réponse, enregistrer l'arbitrage, vérifier la CI et les calibrations,
puis achever les deux rondes indépendantes. Aucun plateau global, nouvelle
release ou installation n'est revendiqué. Le compteur reste à zéro.

Usage enregistré à ce point : 22 appels GPT/Codex, 940 483 tokens estimés ;
un appel Grok en échec d'authentification, 45 000 tokens estimés. Ce sont des
estimations faute de compte natif, pas une mesure de facturation. Les
coupe-circuits restent non atteints ; aucune transition forcée supplémentaire.

Limites de ce lot : API synthétiques pour les scénarios fonctionnels ; aucune
génération fournisseur, session Tauri packagée ou installation Windows rejouée.
Le code source backend n'a pas été modifié, mais son verrou de dépendances l'a
été. La validation des binaires relève d'une future release et ne se déduit pas
de ces recettes navigateur.
