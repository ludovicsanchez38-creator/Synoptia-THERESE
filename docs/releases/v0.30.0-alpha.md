# Release v0.30.0-alpha - 10/07/2026 (3e release du jour)

## Contexte

Reprise du soir (Codex rechargé) : chantier 5 du programme suggestions +
demande Ludo d'ajouter les modèles de juillet 2026 au catalogue. Rituel
habituel : TDD, gates, identifiants vérifiés aux sources (post-cutoff).

## Contenu

### Chantier 5 - Profil d'export DOCX (`86d67c7`)

ExportProfile Pydantic versionné (langue, polices, tailles, couleurs dont H2
distincte, footer, marges) pilotant le rendu déterministe markdown_docx
(exports Atelier + conversations ; les DOCX du chat docx-pro restent sur leur
pipeline). JSON en écriture atomique dans le data dir, corrompu -> repli
défauts + warning surfacé (GET /api/config/export-profile), allowlist backup,
endpoints GET/PUT/DELETE, section UI « Exports Word » dans Réglages > Services.
14 tests backend + 3 composant.

Découvertes de session :
- **DataTab est un composant ORPHELIN** (monté nulle part dans SettingsModal) -
  ma section y était invisible, déplacée dans ServicesTab. La vérification
  navigateur a payé. Candidat au nettoyage (avec ses sous-sections stats/démo
  qui vivent peut-être ailleurs).
- **Env web de dev limité** : initApiBase() n'est pas appelée au boot web pur
  (le ?port=XXXX de core.ts n'est lu que par le flux Tauri/panels) et l'auth
  de session se lie au port par défaut -> vérification UI complète impossible
  quand la prod occupe 17293. Recette de la section en app packagée.
- **mypy + pydantic** : écrire les défauts `Field(default=...)` explicites
  (les positionnels comptent des faux « Missing named argument »).

### Lot modèles (`28a5fbc`)

Identifiants VÉRIFIÉS aux sources le 10/07/2026 :
- Anthropic : `claude-fable-5` (frontier, 1M contexte, thinking toujours
  actif, effort low->max) + `claude-sonnet-5` (référence API officielle).
- OpenAI : `gpt-5.6-sol` / `gpt-5.6-terra` / `gpt-5.6-luna` - GA 09/07/2026,
  reasoning effort 6 niveaux none->max.
- xAI : `grok-4.5` - **l'ID contient un POINT** (`grok-4-5` -> model-not-found,
  docs.x.ai). Sorti le 08/07, 500k contexte, $2/$6.
- Mistral : `mistral-medium-latest` (Medium 3.5, avril 2026) ajouté,
  Small renommé Small 4 (docs.mistral.ai : IDs datés 2604/2603/2512).

**Bug latent corrigé** : le provider Anthropic envoyait `temperature`
inconditionnellement, or Fable 5 / Sonnet 5 / Opus 4.7 / 4.8 REFUSENT les
paramètres de sampling (400 API) - Opus 4.8 étant déjà au catalogue,
sélectionner ce modèle avec une clé Anthropic directe cassait le chat.
`_build_request_body` + garde `_NO_SAMPLING_PREFIXES`, 7 tests paramétrés.

NB : le message du commit `28a5fbc` a perdu le mot « temperature » (backtick
avalé par zsh dans `git commit -m`) - gotcha : passer par -F ou guillemets
français pour les messages contenant des backticks.

## À suivre (validé Ludo)

- **Mini-chantier effort (11/07)** : réglage « Effort de raisonnement » dans
  l'onglet IA, traduit par provider (Anthropic `output_config.effort`
  low/medium/high/xhigh/max, défaut high ; OpenAI `reasoning_effort`
  none->max), envoyé UNIQUEMENT aux modèles qui le supportent (garde par
  préfixe comme le sampling). Grok : support effort NON vérifié, à sourcer.
- Chantiers 4 (Variables) et 3 (Ollama auto-hébergé) - designs V2 prêts.

## Gates (tous verts avant tag)

Backend 1475/1475 (hors e2e), vitest 420/420, tsc, ESLint 0 erreur,
ruff clean, mypy fresh 999 = baseline.
