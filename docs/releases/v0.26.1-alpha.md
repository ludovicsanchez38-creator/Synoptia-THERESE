# Release THÉRÈSE v0.26.1-alpha

**Date** : 2026-07-02
**Type** : PATCH (3 fixes)
**Version** : 0.26.0 → 0.26.1
**Mode** : fast-track (fixes uniquement, < 5 fichiers/PR, pas de dépendances, CI verte)

## Contenu — 2 PR

| PR | Type | Description |
|---|---|---|
| #90 | `fix(email)` + `fix(calendar)` | **BUG-112** : config email « autre fournisseur » — toggle mot de passe permanent (le révélateur natif WebView2 disparaissait après perte de focus) + validation unifiée `hasCompleteConfiguration` pour Tester/Enregistrer (valeurs trimées, ports > 0) + labels accessibles. **BUG-114** : événements calendrier simultanés côte à côte via nouveau helper `calendarEventLayout.ts` (groupes de recouvrement, colonnes gloutonnes, expansion de largeur), vues semaine + jour. Travail nocturne de Zézette, resté non commité dans son workspace — repris, committé en 2 lots et poussé à la main. Mergé en **rebase** (2 commits distincts conservés). |
| #91 | `fix(installer)` | **BUG-113** : doublon d'icônes bureau Windows après MAJ. Cause racine trouvée via l'historique git : productName « THÉRÈSE » (accentué) jusqu'en v0.1.15 → `THÉRÈSE.lnk` orphelin depuis le renommage en « THERESE » (v0.1.16), recréé en double à chaque install (Windows insensible à la casse mais pas aux accents). Hook `NSIS_HOOK_POSTINSTALL` : purge du legacy (bureau user + commun) ; purge aussi au POSTUNINSTALL. Fichier `.nsh` passé en **UTF-8 avec BOM** (littéraux accentués — sans BOM makensis lit en codepage ANSI) avec test garde-fou. Zézette avait escaladé faute de repro Windows. |

## Origine des bugs (Discord #bugs, 26/06 → 01/07)

- BUG-112 signalé par **Dr_logic-3D** (Win10, v0.26.0) le 26/06, complété le 27/06 (« Tester » bloqué aussi + toggle qui disparaît au changement de focus)
- BUG-113 signalé par **Dr_logic-3D** le 27/06 (apparu à la MAJ auto 0.25 → 0.26)
- BUG-114 signalé par **Capov** le 01/07 (capture fournie)

## Audit / qualité

- CI PR #90 et #91 : Frontend, Backend, Rust (clippy), Lint **PASS**. Dette pré-existante non bloquante (`Audit sécurité` CVE torch/transformers + `mypy baseline`). Merge `--admin`.
- pytest complet re-passé sur le VPS avant push de la branche Zézette : 100 % PASS.
- 4 nouveaux tests de régression (TestBUG112 ×2, TestBUG113 ×2, TestBUG114 ×2) + vitest `calendarEventLayout.test.ts` et `SmtpConfigStep.test.tsx`.
- Ruff local : 87 erreurs pré-existantes identiques sur main (config CI différente), 0 sur les lignes ajoutées.

## Chronologie (02/07/2026)

- ~16h30 : scan Discord (4 canaux + threads) depuis le 26/06 → 3 bugs identifiés
- ~16h40 : workspace Zézette inspecté : branche `fix/BUG-112-114-night-batch` non committée, base = main à jour, pytest vert → commit en 2 lots (identité Zézette) + push + PR #90
- ~16h50 : cause racine BUG-113 identifiée (productName accentué ≤ v0.1.15) → fix hook NSIS + PR #91
- ~17h00 : merge #90 (rebase), CI #91 verte, merge #91 (rebase)
- ~17h10 : bump 0.26.1 (7 fichiers + lockfiles npm/cargo/uv), tag, draft release pré-créée
- ~17h45 : build 3 OS **success** (run 28603234159), 10 assets signés (dmg ARM64, exe/msi Windows, deb Linux, latest.json)
- ~17h50 : release publiée, landing `synoptia.fr/therese/alpha/` → v0.26.1 (vérifiée en ligne), `latest.json` déployé (endpoint vérifié, 7 plateformes), changelog Discord posté

## Suivi (non bloquant)

- **BUG-113** : validation réelle par Dr_logic-3D à la prochaine MAJ (l'icône en double doit disparaître d'elle-même). Fiche déplacée `escalated/` → `done/` avec annotation.
- **BUG-111 (suite)** : Dr_logic note que la MAJ **auto** n'affiche plus aucune option (dont « supprimer les données ») — normal en install silencieuse ; sa suggestion « options avancées en mode contributeur » gardée comme feedback produit. Il attendait le correctif BUG-111 (livré en v0.26.0) pour reprendre la recette S1 : relance possible dès cette release.
- **BUG-041 (16/06, escalated)** : drag & drop projets entre rubriques inopérant — toujours en attente, hors périmètre de cette release.
- **Dette escalated février** : 4 fiches packaging Linux/macOS (NumPy/SciPy/OpenBLAS) anciennes, à trier.
- **Process Zézette** : push OK mais `gh pr create` toujours cassé dans son runtime → PR ouvertes à la main. Et son travail de nuit était resté **non committé** cette fois (ni commit ni push) : à creuser dans son prompt de batch.
