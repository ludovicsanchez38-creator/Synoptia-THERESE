# Release THÉRÈSE v0.13.1-alpha

**Date** : 2026-06-01
**Type** : PATCH (mise à jour du modèle de génération d'image)
**Bump** : 0.13.0 → 0.13.1

## Contenu

- **OpenAI image** : passage de `gpt-image-1.5` à **`gpt-image-2`** (sorti le 21/04/2026, succède à gpt-image-1). Même API images (generate / edit), paramètres `size` et `quality` identiques (drop-in). Appliqué sur tous les emplacements : enum `ImageProvider`, appels modèle dans `image_generator.py`, schémas (`schemas_images.py`), routers (`images.py`), `command_registry.py`, et côté frontend (LLMTab, ImageGenerationPanel, GuidedPrompts, CommandExecutor, actionData, api/images.ts, SettingsModal) + tests.
- **Gemini image** : `gemini-3.1-flash-image-preview` (= Nano Banana 2, modèle image par défaut de Google depuis le 26/02/2026) déjà à jour → inchangé ; libellé d'affichage clarifié (« Nano Banana 2 »).

## Méthode

- Inventaire des références image (~18 sites backend + frontend + tests).
- Recherche web sur sources officielles (developers.openai.com, ai.google.dev) : `gpt-image-2` confirmé (id API exact, params, tailles jusqu'à 4K), `gpt-image-1.5` non déprécié mais ancien.

## Vérification

- **CI verte** sur les 4 jobs (Frontend, Tests Backend, Lint Python, Audit sécurité) sur le commit du contenu (c35e995).
- `tests/test_routers_images.py` (suite racine, celle qu'exécute la CI) aligné sur `gpt-image-2`.
- tsc + eslint + ruff propres.

## Suivi (non engagé)

- **FAL** : encore en FLUX 1.1 pro (`fal-ai/flux-pro/v1.1`). FLUX.2 est la génération courante (`fal-ai/flux-2-pro`) mais l'API/format de requête diffère → à migrer séparément avec test.
- **xAI image** (`grok-imagine-image`) : non proposé dans l'app (l'enum image = OpenAI / Gemini / FAL), à ajouter si souhaité.

## Chronologie

- MAJ modèle image (sur main, CI verte) → bump 0.13.0 → 0.13.1 → tag `v0.13.1-alpha` → build.
