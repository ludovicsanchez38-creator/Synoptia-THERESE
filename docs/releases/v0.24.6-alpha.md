# Release THÉRÈSE v0.24.6-alpha

**Date** : 2026-06-18
**Type** : PATCH (fix updater)
**Version** : 0.24.5 → 0.24.6

## Contenu

Release mono-correctif : **BUG-110** (mise à jour automatique bloquée sous Windows).

- Pas de PR à reviewer : le correctif BUG-110 avait déjà été mergé sur `main` (PR #83, squash `9b111c2`) avant cette release.
- Contexte : le fix venait de la tournée de nuit Zézette (17/06) mais n'avait jamais pu être poussé/PR (remote Git en HTTPS sans credential, alors qu'une clé de déploiement SSH read-write existait depuis le 07/05 mais n'était pas câblée). Remote basculé en SSH → Zézette débloquée.
- Le commit d'origine était contaminé (4 fichiers WIP hors périmètre dont `ProjectsPanel.tsx`) et basé sur un `main` périmé → reconstruit propre (3 fichiers), rebasé sur `main` à jour.

## Correctif BUG-110

- **Symptôme** : sous Windows, l'app restait en v0.24.3 après tentative de mise à jour (2 popups « backend non joignable » puis retour ancienne version). Signalé par lcjp.
- **Fix** : `updater.windows.installMode` `quiet` → `passive` (échec visible), messages contextuels répertoire/permissions dans `UpdateBanner.tsx`, délai 1 s avant relaunch.
- **Tests** : classe `TestBUG110_UpdateBloquee` (3 tests). Bug CI corrigé au passage : les tests utilisaient un chemin absolu en dur (`/home/ubuntu/therese-v2/...`) → remplacé par `Path(__file__)` relatif (échouait en CI, FileNotFoundError).

## CI / Build

- CI sur `main` : Frontend, Backend, Rust (clippy), Lint **PASS**.
- Dette pré-existante non bloquante (échoue aussi sur `main`) : `Audit sécurité` (CVE torch épinglées) + `mypy baseline`. Merge via `--admin`.
- Build Release : **macOS ARM64 + Windows x64 + Linux x64 OK**, clé de signature updater OK.
- Assets : .dmg, .exe (NSIS) + .msi, .deb, tar.gz + signatures + `latest.json`.

## Diffusion

- Landing `synoptia.fr/therese/alpha/` → v0.24.6 (vérifié en ligne).
- `latest.json` déployé et vérifié (`darwin-aarch64`, `linux-x86_64`, `windows-x86_64` signés) → updater armé pour les testeurs bloqués en 0.24.3.

## Chronologie

- Merge BUG-110 (#83) : 08:38 UTC
- Bump + tag v0.24.6-alpha : ~08:5x UTC
- Build 3 OS : success
- Landing + latest.json : déployés et vérifiés
- Release publiée : https://github.com/ludovicsanchez38-creator/Synoptia-THERESE/releases/tag/v0.24.6-alpha

## Dette / suivi

- BUG-A (toggle « Ouvrir sur le chat directement » sans effet) et BUG-B (sync CRM crée une feuille vide au lieu de proposer une feuille existante) : signalés par Ludo le 18/06, à traiter par Zézette (fiches bug-queue).
- BUGS-ACTIVE.md à rafraîchir : BUG-102/103/109 sont en réalité déjà livrés en 0.24.5 (tracker périmé).
