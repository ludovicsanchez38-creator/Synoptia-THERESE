# Release v0.71.0-alpha - 10/09/2026

## Nature

Premier lot de la direction artistique « Application affinée » (direction 2),
validée par Ludo le 10/09 (« la DA application affinée est superbe. je
valide », puis « Go DA! » à 17:30). Le socle et la coque de l'application
prennent la forme de la maquette (`docs/da/2026-09-05-propositions/maquettes/da/base.css`)
en consommant les jetons existants ; onze primitives sont livrées pour les
écrans à venir. Aucun comportement ne change.

Cadrage : `docs/plans/2026-09-10-cadrage-da-application-affinee.md`. Design du
lot, challengé par Grok AVANT le code, trois passages (NO-GO 22 points, NO-GO
9 points, GO) : `docs/plans/2026-09-10-da-lot1-socle-coque-design.md`.
Recette : `docs/da/2026-09-10-lot1-recette.md`. Décisions prises par défaut
faute de réponse aux trois questions du cadrage : socle d'abord, une release
par lot.

Mode : pas de PR, 25 commits directement sur `main` entre `v0.70.0-alpha` et
le bump. Ludo a dit « j'ai plein de crédits grok tu peux même lui confier
des tâches » : les primitives ont été codées par Grok Build dans un arbre de
travail git séparé (`grok/da-lot1-primitives`, onze commits en TDD), relues
puis fusionnées. Revue adversariale Grok du diff avant le tag (NO-GO 7
points, tous corrigés, GO au troisième passage) à la place de l'audit à trois
agents ; GO de Ludo à 19:45 (« GO 0.71.0 »).

## Contenu

| Mesure | Valeur |
|---|---|
| Commits depuis `v0.70.0-alpha` | 25 (bump inclus) |
| Socle | cascade Tailwind 4 ordonnée (`@layer base` : titres 1,625/1,1875/1 rem, body 14 px, focus 3 px), jumeaux hors couche retirés, teintes sémantiques promues dans `@theme` (B-109), `--container-colonne`, `--font-editorial`, `btn-da` et `card-brutal` retirés |
| `Button` | 36 px sans ombre ni soulèvement (`lg` 44 px), ghost en accent, danger sur la teinte ; API inchangée (254 usages) |
| Coque | barre `min-h-13` sans « Interface unifiée », rail `w-14` à boutons 40 px avec `aria-current`, colonne et composeur 56 rem, établi en tête de la carte du composeur, envoyer 36 px, ombres en jetons, repli sous 840 px |
| Primitives (Grok) | Carte, Ligne, Etiquette, Segments, EtatVide, Alerte, Squelette ; Input, Select, Textarea, FormField alignés |
| Gardes nouvelles | `jetonsDA`, `Button.da`, `aucuneCouleurEnDur`, `ConversationCanvasPrototype.da`, contraste élevé des étiquettes ; e2e B-320 réécrit sur l'invariant du dégagement |
| Tests | vitest 2 050, pytest 3 392 (XML), e2e desktop 99 (CI), tsc 0, eslint 27 (plafond), ruff propre, mypy 951 (cliquet) |
| Bump | MINOR (feat DA) |

## Revue et recette

- Design v1 → Grok NO-GO (22 points, tous confirmés à la lecture du code) ;
  v2 → NO-GO (21 traités, 1 partiel, 9 nouveaux dont deux P1) ; v3 → GO.
- Revue du diff : NO-GO 7 points (deux P1 : `aria-current` d'Accueil perdu
  après un verbe de l'établi ; zone droite d'une `Ligne` qui mangeait le clic
  du bouton étiré ; cinq P2/P3 : gardes contournables, e2e mesurant un
  wrapper vide, `brightness`). Tous corrigés en TDD ; second passage un
  partiel, troisième GO.
- Recette au pixel (`.cartography-work/validation/da-lot1/`, 26 captures) :
  rail 64 → 56, boutons 44 → 40, barre 56 → 52, body 16 → 14, h1 24/700 →
  26/800, colonne 860 → 896, envoyer 44 → 36 sans bordure, badge retiré ;
  un défaut trouvé et corrigé (barre écrasée à 800 px).
- Sabotages rouges : rail, couleur en dur, teinte hors `@theme`, contraste
  élevé dans `@layer`, `aria-pressed` de Segments, dégagement du composeur
  (e2e).

## Décision

MERGER. Le lot ne change aucun comportement (suites de comportement
intactes, e2e 99), la forme est celle validée, chaque écart au design a été
soit corrigé soit déclaré. Réserves : corps de texte à 12,25 px en police
Petite (plancher 12 px tenu) ; primitives non montées par un écran (preuve
par tests) ; `UpdateBanner.tsx` en liste blanche de la garde des couleurs.

## Dette et suites

- [ ] Lot 2 : écran Accueil (brief, états relance / validée / vide / erreur)
  avec les primitives ; puis tiroir, contacts, devis, projets, décision,
  agenda, paramètres ; puis les écrans sans maquette (Email d'abord) après
  maquettes par Syn.
- [ ] `UpdateBanner.tsx` : seize `rgba` en styles inline, lot des couches.
- [ ] P-058 à P-065 au portail humain ; B-734 (Windows intermittent), B-747,
  B-740 à B-742, B-667, B-685, B-730, B-420 différés.
- [ ] Trois questions du cadrage DA sans réponse explicite (défauts pris).

## Chronologie (Europe/Paris)

- 17:30 « Go DA! » ; 17:35 → 18:08 orientation, design v1 écrit et commité.
- 18:09 → 18:35 Grok : NO-GO v1 (22), NO-GO v2 (9), GO v3 ; une relecture
  arrêtée sans verdict à 60 tours, relancée à 150.
- 18:41 Grok lancé sur les primitives (premier essai en `acceptEdits` sans
  aucune écriture ; relancé en `bypassPermissions` à 18:49).
- 18:45 → 18:58 socle et coque en TDD (17 rouges puis verts, trois
  sabotages), portes frontend, e2e B-320 réécrit (sabotage d'abord vert par
  construction, invariant mécanique ajouté, sabotage rouge 0 < 245).
- 19:00 → 19:05 recette avant/après (deux Vite, deux copies de React sur le
  premier : un seul Vite relancé `--force`), barre à 800 px corrigée.
- 19:06 fusion des onze commits de Grok (sabotage Segments rouge), portes
  vertes (vitest 2 048, tsc, eslint) ; 19:10 → 19:39 revue Grok du diff :
  NO-GO 7, correctifs, partiel, GO.
- 19:45 GO de Ludo ; 19:47 bump `1f8262aa`, tag poussé, draft créée, build
  lancé (run 34514708442).
