# Release THÉRÈSE v0.28.0-alpha

**Date** : 2026-07-08
**Type** : minor (feature + correctifs)
**Version** : 0.27.0 → 0.28.0
**Pilote** : Claude Code (Mac), workflow `/release-therese`

## Contexte

Release de fin de freeze. Les 7 bugs remontés par Dr_logic-3D (Discord testeurs)
ont été corrigés à la main pendant le freeze (Zézette avait échoué la nuit du
08/07, timeout). Le périmètre englobe aussi le travail 08/07 antérieur déjà sur
`main` mais non releasé (4 correctifs signalés par Ludo + 1 feature usage tokens),
plus le durcissement CI.

## PRs incluses

Aucune PR ouverte : tout le travail a été commité directement sur `main` pendant
le freeze. Pas d'audit multi-agents (rien à auditer sur des PR) ; le code est déjà
mergé, couvert par des tests de régression ciblés et **CI verte 6/6** (run
28959875475 sur cb0572b, puis 28960250799 sur b44e673, puis 28961118779 sur le
commit de bump c236da0).

## Contenu (15 commits fonctionnels depuis v0.27.0-alpha)

### Feature
- `feat(llm)` 295b99d : usage tokens réel des 10 providers (au lieu de l'estimation).

### Correctifs testeurs Discord (7)
- BUG-121 4bef2db : `send_email` jamais exécuté sans confirmation (invariant, préfixe MCP).
- BUG-120 4eca2d9 : repli calendrier local pour un compte IMAP.
- BUG-124 96bfeba : filet anti réponse vide quand le modèle enchaîne des outils.
- BUG-123 6526c93 : lecture message complet via le bon provider en IMAP.
- BUG-122 4e3010b : onglet Envoyé liste le dossier Sent en IMAP.
- BUG-125 19ef2de : tâches en retard visibles en tête du dashboard.
- BUG-127 eada39e : réponse agent Atelier lisible en thème clair.

### Correctifs signalés par Ludo (4)
- 35082b2 : profil émetteur facture lisible thème clair + rafraîchi.
- 68be675 : contraste badges de statut (factures, CRM, projets).
- 371954d : validation fuseau IANA avant Google Calendar.
- 6b35525 : sanitisation body_html des mails (Gmail + IMAP).

### Durcissement CI (3)
- c48584e : ruff propre sur `tests/` (105 -> 0), lint CI étendu.
- d9d1a3f : bump `aiosmtplib` 5.1.2 (CVE-2026-53533).
- cb0572b : mypy fresh 1000 -> 999 (annotation `get_profile`).

## Décision Claude Code

RELEASER. Tout est sur main, testé, CI verte. La feature usage-tokens justifie un
bump MINOR (0.28.0), confirmé par Ludo. Périmètre complet de main confirmé par Ludo.

## Résumé technique

- Version appliquée dans 7 fichiers + badge README + 3 lockfiles (uv.lock,
  Cargo.lock, package-lock.json) via `bump-version.sh 0.28.0` (commit c236da0).
- `aiosmtplib` 5.1.2 préservé dans uv.lock après re-lock du bump.
- Tests backend : 1421 collectés, suite locale complète verte (0 régression après bump aiosmtplib).
- CI (commit de bump c236da0) : **success** (run 28961118779).
- Build 3 OS : **success** (run 28961120703) — macOS aarch64 (.dmg + .app.tar.gz),
  Windows x64 (.exe NSIS + .msi), Linux amd64 (.deb). Pas de macOS Intel (matrice
  aarch64-only, conforme aux releases précédentes).
- Release publiée : https://github.com/ludovicsanchez38-creator/Synoptia-THERESE/releases/tag/v0.28.0-alpha (10 assets, dont `latest.json` signé).
- Landing : `synoptia.fr/therese/alpha/` → **Alpha v0.28.0** (edge + serveur vérifiés).
- Updater : `latest.json` publié et vérifié sur l'endpoint (version 0.28.0, 7 plateformes).
- Installation locale macOS : **THERESE.app 0.28.0** installée (quarantaine retirée).
- Changelog Discord : posté dans #changelog (bot Katia).
- Agents : Katia (CHANGELOG + therese-src) et Zézette (MEMORY) mis à jour.

## Warnings / dette technique

- Le vrai décompte mypy « fresh » reste à 1000 hors de l'annotation `get_profile`
  (le 1000e est né dans 295b99d, chemin multi-LLM) ; baseline honnête = compte
  fresh. Gotcha documenté : mesurer mypy cache vidé (`rm -rf .mypy_cache`).
- Dette pré-existante inchangée (starlette 1.3/httpx2 à froid, torch 2.10, e2e hors CI).
- BUG-126 (datetime naive/aware notification_service) + BUG-116/117/118/119 mineurs
  restent en pending bug-queue (hors périmètre release).

## Chronologie (2026-07-08, Europe/Paris)

1. Nettoyage CI pré-release : ruff tests, aiosmtplib CVE, mypy fresh 1000→999. CI 6/6 verte.
2. Décisions Ludo : version 0.28.0 (minor), périmètre = tout main.
3. Bump 0.28.0 (7 fichiers + lockfiles), commit c236da0, push + tag v0.28.0-alpha.
4. Draft release pré-créée, build 3 OS lancé, surveillance.
5. Build 3 OS success → release publiée (undraft).
6. Landing 0.27.0→0.28.0 (synoptia-site, rsync therese/ uniquement) + latest.json publié.
7. Rapport de release + changelog Discord + installation locale.
8. MAJ agents Katia + Zézette. Récap + libération du lock.
