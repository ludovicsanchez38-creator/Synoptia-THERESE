# Release v0.74.0-alpha (16/09/2026)

## Nature

Release de fin du cycle 9 de la boucle d'amélioration (skill Plateau, Claude seul, Grok et Codex à sec) et du lot 0.74 « petites UX » accepté par Ludo le 16/09 au matin (« oui pour les trois, go lot 0.74 »). GO de release explicite de Ludo le 16/09 (« Go release »).

Aucune PR : 131 commits directement sur `main` depuis `v0.73.0-alpha` (base `3ece1849`…, tête taguée `0c954d76`). Mode complet (audit multi-agents obligatoire : le lot contient des `feat`).

## Contenu

- **Cycle 9, environ 150 correctifs en TDD (B-760 à B-931)**, un commit par sujet : cinq surfaces mortes retirées (ancienne palette, en-tête de chat, centre de notifications, connexion e-mail obsolète) ; purge RGPD qui laissait l'adresse et le nom (B-879, B-880) ; fuite de connexion aiosqlite quand un flux de chat est annulé (B-806) ; identifiant d'outil non normalisé qui autorisait un `rmtree` hors dossier (B-794) ; trois `confirm()` natifs remplacés par des confirmations en ligne (B-872) ; Board qui sondait Ollama en dur (B-842) ; page suivante d'e-mail collée sous une autre rubrique et double chargement sur Entrée (B-855, B-897) ; rendez-vous à 00:30 ouvert à la veille (B-901) ; `git add` non vérifié dans l'Atelier (B-852) ; cache de vignettes par identifiant (B-862, B-895) ; formulaire guidé à champ fichier (B-762) ; bouton Rechercher sans nom sous 840 px (B-761).
- **Lot 0.74 « petites UX »** : P-095 (⌘⇧K nommé par l'action), P-063 (infobulle des titres de trame), P-062 (refus Images → bouton vers Paramètres > Services), P-064 et P-093 (états vides Projets et Contacts avec explication et geste), P-015 (« Devis et factures » nomme les deux objets), P-027 (délier dit que l'index garde les documents), P-070 (aperçu du dernier message), P-094 (repère de durée avec un modèle local), P-085 (fournisseurs secondaires derrière « Autres »), P-061 (jetons de variable mal formés signalés), P-002 (contraste de la vue Mémoire mesuré, recette `scripts-recette/recette-contraste-memoire.mjs`).
- **Retouches de l'audit de release** (huit commits `519ab4bf` à `5f907279`, voir ci-dessous).

## Audit multi-agents (étape 1b)

Diff audité : `scratchpad/diff-lot074-code.patch` (les 12 composants du lot 0.74, 456 lignes) plus `docs/known-patterns.md`. Trois rôles Claude séparés (Grok et Codex à sec).

| Agent | Verdict | Constats retenus |
|---|---|---|
| Sécurité et bonnes pratiques | WARNING | masque démo contourné (tiroir, trame), faux positifs et triple appel de `jetonsMalFormes`, `useEffect` de LLMTab, constante entre deux imports, `title` non masqué, message « Dossier délié » persistant, `type="button"` absent |
| UX/UI | WARNING | H1 cible de 20 px, H2 focus perdu entre deux bascules, H3 fournisseur courant masqué par « Réduire », M1 « tableur », M2 « purge du projet », M3 « Ignoré », M4 aperçu inégal, M5 trois libellés pour « Nouveau contact » |
| Avocat du diable | WARNING | 0 P1, 6 P2 (les mêmes défauts, avec preuves : regex sondée sur 19 cas, 86 tests du lot verts), 8 P3 (aperçu calculé deux fois, test « Aucune facture » devenu muet, condition morte, Katia dans l'Atelier, repère « plusieurs minutes » à t=0, infobulle native) |

### Décision : MERGER AVEC RÉSERVES, réserves levées avant le tag

Aucun BLOCK. Les trois rapports convergent sur une même famille de défauts (« rien ne se comporte comme annoncé »), tous corrigeables en moins d'une heure : corrigés en TDD avant le bump, un commit par sujet.

| Commit | Sujet |
|---|---|
| `519ab4bf` | Paramètres : une seule bascule « Autres » persistante (`aria-expanded` variable, `aria-controls`, `min-h-9`, anneau de focus décalé), le fournisseur courant ou muni d'une clé n'est jamais replié, `SECONDAIRES` sous les imports |
| `598c0c88` | Chat : P-061 ne signale que les presque-noms (`^[\p{L}\p{N}_\- ]{1,40}$`, clôtures de code exclues, sans doublon, `useMemo`), libellé « Pas une variable : … (envoyé tel quel) », « tiret bas » |
| `780ac58b` | Conversations : titres, `aria-label` et aperçu du tiroir passent par le masque démo ; aperçu coupé avant normalisation, sans gras ni code Markdown, muet sur une conversation à un seul message |
| `60828c7a` | Documents : texte et `title` des sections de trame masqués en démo |
| `630cb5bf` | Mémoire : « Dossier délié » remis à zéro à chaque action, consigne vers la fiche du projet (geste qui existe) |
| `326b84a3` | Contacts : l'état vide promet l'import vCard, libellé « Nouveau contact » aligné, `type="button"` sur les états vides Projets et Contacts |
| `73b1899d` | Factures : filtre Avoirs → « Nouvelle facture » (le formulaire s'ouvre sur une facture) ; test de panne réarmé sur le nouveau libellé |
| `5f907279` | Images : « Ouvrir les Paramètres (Services et connecteurs) » |

### Réserves restantes (différées, pas bloquantes)

- L'Atelier affiche encore « Katia » sur la surface atteinte par ⌘⇧K (P-095 appliquée au seul raccourci) : à trancher avec Ludo (nom de l'agent partout ou nulle part).
- Repère « plusieurs minutes » avec un modèle local affiché dès t=0 (cohérent avec B-627) : un repère progressif après 5 s serait plus juste.
- P-070 : l'aperçu n'existe que pour les conversations hydratées dans la session ; un `last_message_preview` côté serveur donnerait un tiroir homogène.
- `SECONDAIRES` recopié dans l'onglet plutôt qu'un drapeau `secondaire` dans `lib/catalogueModeles.ts`.
- Infobulle native `title` (P-063) invisible au clavier et au toucher, faute de primitive `Tooltip`.
- B-932 : `test_plafonds_de_jetons_appliques` dépend d'un Ollama réel sur 11434 dans la suite complète.
- Instrument : `audit-da-c9.mjs` attend un bouton de coque rendu `inert` par la modale « À propos » (1 à 2 KO sur 10, rejeu isolé 10/10) ; à corriger au cycle 10.

## Validation locale avant tag (tête `0c954d76`)

- vitest 2531/2531 (exit 0, `--reporter=json`, 0 erreur non gérée) ; pytest 3251/0 sur `6b6c51b1` (aucun fichier backend touché depuis) plus les tests de version et d'index des noms après le bump (57 verts) ; tsc 0 ; eslint 27 avertissements (plafond 27, 0 erreur) ; ruff propre ; mypy à la baseline.
- Audit DA des 16 écrans harmonisés sur la pile jetable mise sur `5f907279` : 159/160, le seul KO étant la course d'instrument « À propos » (rejeu isolé `repro-apropos.mjs` 10/10, moteur 200) ; sondes clavier identiques à la référence de la ronde A.
- Plateau 2/2 (ronde A orchestrateur, ronde B agent indépendant) sur `6246b549` avant le lot ; calibration des cinq instruments valide jusqu'au 17/09 06:33 UTC.
- Bump `bash scripts/bump-version.sh 0.74.0` : 7 fichiers + badge README + index des noms + `package-lock.json`, `Cargo.lock`, `uv.lock` ; `git status --porcelain` vide avant le tag.

## Publication

- Tag `v0.74.0-alpha` poussé le 16/09 à 11:48 (Europe/Paris) ; draft pré-créée avec `/tmp/therese-release-notes-0.74.0.md` (contournement du 403 tauri-action) ; run « Release THÉRÈSE » 35081537833.
- Build : vert à 12:19 (gate à 12:03 : lint, backend autonome, e2e, frontend, mypy, tests backend, audit sécurité, clippy ; puis macOS ARM64, Linux x64, Windows x64). Release publiée (pré-release) à 12:20 avec 10 assets (dmg, app.tar.gz + sig, deb + sig, exe + sig, msi + sig, latest.json).
- Landing `synoptia.fr/therese/alpha/` : source `synoptia-site/src/static/therese/alpha/index.html` 0.73.0 → 0.74.0 (5 occurrences, 0 restante), miroir `_site`, sauvegarde `therese-landing-pre-0.74.0.tar.gz` sur le VPS, rsync du seul dossier `therese/`, `curl` en ligne « Alpha v0.74.0 ».
- `latest.json` : téléchargé depuis la release (version 0.74.0, 7 plateformes signées), déposé sur `synoptia.fr/therese/alpha/latest.json`, endpoint vérifié.
- Discord #changelog (3 messages + « À venir ») et MAJ Katia/Zézette : **non faits au moment du rapport** : le VPS Agents est injoignable depuis le Mac (allowlist ufw, Mac hors LAN, jeton Cloudflare Access `ssh-dq-ludo` à renouveler par Ludo). Fichiers prêts dans `/tmp/rel074/` avec `publier-discord-et-agents.sh`.
- Installation locale : `/Applications/THERESE.app` 0.74.0 (ancienne version à la Corbeille, quarantaine retirée).

## Chronologie (Europe/Paris)

- 08:06 lot 0.74 terminé sur `6b6c51b1` ; STABILIZE et CALIBRATE rejoués (cinq instruments PASS).
- ~10:30 GO de Ludo (« Go release »), lock posé, pré-vol OK, diff du lot extrait, trois agents d'audit lancés (les trois ont dépassé le chien de garde de 600 s ; sécurité et UX avaient écrit leurs rapports, l'avocat du diable a été relancé).
- 11:1x rapports lus (trois WARNING), tests rouges écrits (10 rouges pour la bonne raison), corrections.
- 11:39 huit commits de l'audit poussés ; 11:40 bump 0.74.0 ; 11:44 pile jetable sur HEAD, audit DA 159/160 + rejeu isolé 10/10.
- 11:48 tag `v0.74.0-alpha`, draft créée, build lancé.
- 12:03 gate vert ; 12:13 macOS et Linux verts ; 12:14 app 0.74.0 installée ; 12:19 Windows vert ; 12:20 release publiée, landing déployée et vérifiée, `latest.json` en ligne.
