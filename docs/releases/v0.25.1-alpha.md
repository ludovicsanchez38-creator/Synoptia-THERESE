# Release THÉRÈSE v0.25.1-alpha

**Date** : 2026-06-19
**Type** : PATCH (2 fixes)
**Version** : 0.25.0 → 0.25.1

## Contenu — 2 bugs signalés par Ludo (18/06)

| Bug | PR | Type | Description |
|---|---|---|---|
| BUG-A — toggle "Ouvrir sur le chat directement" sans effet | #85 | `fix(ui)` | La préférence était en état local React sans persistance → préférence `skipDashboard` persistée (Zustand) + migration localStorage + init vue. Fix Zézette (tournée de nuit), 5 tests. |
| BUG-B — synchro CRM feuille vide sans explication | #86 | `fix(crm)` | Fonction pure `build_sync_message` : quand la synchro remonte 0 élément (feuille vide auto-créée), message qui guide vers l'option feuille existante. + message de connexion honnête. 3 tests TDD. |

## Audit

- **BUG-A** : audit 3 agents lancé. Verdict initial BLOCK = **faux positif** (les agents lisaient `main`, pas la branche → croyaient le diff non appliqué). Diff réel sain : migration `migrateFromLocalStorage` + `removeItem`, storage Zustand synchrone (pas de race de réhydratation), CI verte. Mergé.
- **BUG-B** : fast-track (fix backend, <5 fichiers, fonction pure + 3 tests TDD, pas de dépendance).

## CI / Build

- Les deux PR : Frontend, Backend, Rust (clippy), Lint **PASS**. Dette pré-existante non bloquante (`Audit sécurité` CVE torch/transformers + `mypy baseline`). Merge `--admin`.
- Build Release : **macOS ARM64 + Windows x64 + Linux x64 OK**, clé de signature updater OK.

## Diffusion

- Landing `synoptia.fr/therese/alpha/` → v0.25.1 (vérifié).
- `latest.json` déployé et vérifié.

## Suivi (non bloquant)

- **BUG-B — déblocage UX complet** : le fix actuel explique la feuille vide ; le chantier plus gros (proposer de choisir une feuille existante AU MOMENT de la connexion, au lieu d'auto-créer une feuille vide) reste à faire — touche le flux OAuth callback + CRMSyncPanel.
- **Process** : le `gh pr create` de Zézette échoue encore dans son runtime (alors que son `git push` passe via la clé SSH). À régler pour qu'elle ouvre ses PR seule (sinon ouverture manuelle depuis le Mac).
- Tests E2E rendu calendrier, suivi tokens réel LLM : déjà en dette.
