# Release v0.24.0-alpha

> Date : 11/06/2026 - Type : MINOR (0.23.0 → 0.24.0)

## Contenu

Pas de PRs externes : release 100 % UX/DA issue des retours de Ludo du matin même
(8 commits feat/fix sur main depuis v0.23.0-alpha, validés en live sur planche
comparative + navigateur).

| Lot | Contenu |
|---|---|
| DA « brutaliste éditorial » | Boutons : bordure encre 2px + ombre dure, enfoncement au clic (Button.tsx → 30+ écrans). Direction choisie par Ludo sur planche PNG 3 directions |
| Badges | Tous les badges d'état en tags carrés rounded-[6px] (19 badges / 16 fichiers). Restent ronds : compteur cloche, toggles, radios, avatars |
| Pastilles duotone | Fin des dégradés cyan-magenta : 37+ pastilles converties en fond uni teinté cerclé d'encre (lanceur guidé, panels, settings, modales, wizard email, About) |
| Bouton Accueil | Isolé dans sa propre barre, séparé du groupe de navigation |
| Sidebar | FERMÉE au lancement (avant : overlay par-dessus l'Accueil). ⌘B/Ctrl+B pour ouvrir |
| Fenêtre | tauri-plugin-window-state : taille/position mémorisées. Premier lancement : ~86 % du moniteur, centré, dashboard entier visible |
| Accueil compacté | 4 conversations récentes (vs 5), paddings réduits : tient sans scroll sur un portable |

## Audit

Revue adversariale au fil du chantier (pas en bloc) : **1 critical attrapé**
(test_regression verrouillait littéralement « sidebar ouverte par défaut » =
gate de release) + 2 importants (e2e, protocoles personas) + 5 minors corrigés
(contraste badge AA, high-contrast, transitions cascade layers, StateFlags).

Décision Claude Code : **MERGER** (commits déjà sur main, validés en live par Ludo).

## Résumé technique

- Version : 0.23.0 → 0.24.0 (7/7 fichiers + lockfiles npm/cargo/uv - gap uv.lock de la 0.23 corrigé)
- Tests : backend 1156+3 verts (3 régressions UI mises à jour : BUG-060/F-15 verrouillaient l'ancien style), frontend 221 verts
- Build : 3 OS verts du premier coup - la draft pré-créée a éliminé le 403 de la 0.23
- Updater : latest.json 0.24.0 publié (7 plateformes signées)

## TEST UPDATER N→N+1 : VALIDÉ DE BOUT EN BOUT

Premier cycle réel de mise à jour automatique (la 0.23 avait livré l'updater,
la 0.24 le prouve) :
1. App 0.23.0 lancée → bandeau « Nouvelle version 0.24.0 disponible » (manifest signé vérifié)
2. « Installer maintenant » → téléchargement + vérification minisign → « Mise à jour v0.24.0 prête »
3. « Redémarrer pour installer » → installation du bundle OK
4. Relance → 0.24.0 fonctionnelle sur la base chiffrée existante, Info.plist 0.24.0

**Réserve** : le redémarrage AUTOMATIQUE n'a pas relancé l'app (bandeau fallback
« Redémarre THÉRÈSE manuellement ») - l'installation était faite, seul le
relaunch a nécessité un quit/relance manuel. À investiguer (interaction
relaunch Tauri ↔ kill du sidecar au RunEvent::Exit probable). Tracé en dette.

## Warnings et dette

- [ ] Relaunch auto post-update KO (fallback manuel propre) : investiguer `tauri_plugin_process::restart` vs séquence d'arrêt sidecar (5 s de shutdown graceful > délai du relaunch ?).
- [ ] Fenêtre premier lancement : s'ouvre sur le moniteur de création (externe chez Ludo). Comportement Tauri par défaut + center() sur ce moniteur - acceptable, à observer.
- [ ] suite pytest : le hook `os._exit` de conftest coupe AVANT le résumé final quand la sortie est redirigée (pipe/fichier) → toujours vérifier l'exit code + compter les F de la barre de progression, pas seulement lire le tail. A failli masquer 3 FAILED cette release.
- Reste de la dette : CLAUDE.md section « Dette technique ».

## Chronologie

- 12:33 - Pre-flight OK (lock, gh, repo, VPS, 0 PR)
- 12:35-13:40 - Tests : 1er run suite backend figé (hang thread shutdown, tué),
  relance verbose → 3 FAILED identifiés = régressions UI verrouillant l'ANCIEN
  style (BUG-060 gradient CRM, F-15 pill modèle) → tests mis à jour (d39aa5c),
  test_regression 524/524 vert (exit 0)
- 13:52 - Bump 0.24.0 (7/7 + 3 lockfiles), commit 882a412
- 13:58 - Tag v0.24.0-alpha poussé + draft pré-créée AVANT le build (anti-403)
- 14:22 - Build SUCCESS premier coup : preflight + macOS ARM64 + Windows x64 + Linux x64
- 14:24 - Release publiée (10 assets), landing 0.24.0 (serveur + CDN), latest.json déployé (7 plateformes)
- 14:29-14:41 - TEST UPDATER : bandeau détecté sur la 0.23 → install → relance
  manuelle → 0.24.0 opérationnelle (DA + sidebar fermée + fenêtre adaptative
  1301x865 constatées sur l'app réelle)
