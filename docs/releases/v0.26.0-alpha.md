# Release THÉRÈSE v0.26.0-alpha

**Date** : 2026-06-26
**Type** : MINOR (2 features + 1 fix)
**Version** : 0.25.1 → 0.26.0

## Contenu — 3 PR

| PR | Type | Description |
|---|---|---|
| #87 | `feat(crm)` | **BUG-B déblocage complet** : à la connexion CRM, choisir une feuille Google existante (endpoint `GET /api/crm/google-sheets/list` + UI). Travail Zézette + corrections lint/type. |
| #88 | `fix(installer)` | **BUG-111** : désinstall Windows « supprimer les données » efface aussi `~/.therese` (hook NSIS `POSTUNINSTALL` conditionné à `$DeleteAppDataCheckboxState`). |
| #89 | `feat(voice)` | **Voix locale souveraine en option** : STT faster-whisper + TTS Piper, groupe pip optionnel `voice-local` (hors build par défaut), modèles à la demande, prérequis RAM documentés. |

## Audit / qualité

- Développé en TDD (chaque lot a ses tests de régression : BUG-B, BUG-111, voice).
- BUG-B avait été poussé par Zézette mais non mergeable (lint Python + type frontend `google_auth_configured` → `has_token`) : corrigé avant merge.
- Conflits `tests/test_regression.py` (les 3 PR ajoutaient une classe en fin de fichier) résolus par rebase : les 3 classes coexistent.

## CI / Build

- Les 3 PR : Frontend, Backend, Rust (clippy), Lint **PASS**. Dette pré-existante non bloquante (`Audit sécurité` CVE torch/transformers + `mypy baseline`). Merge `--admin`.
- Build Release : **macOS ARM64 + Windows x64 + Linux x64 OK**, clé de signature updater OK.

## Diffusion

- Landing `synoptia.fr/therese/alpha/` → v0.26.0 (vérifié).
- `latest.json` déployé et vérifié.

## Voix locale (en option) — prérequis RAM

| Composant | RAM |
|---|---|
| STT Whisper `tiny` / `base` | ~1 Go |
| STT Whisper `small` | ~2 Go |
| TTS Piper (1 voix) | ~0,5 Go |

Installation : `pip install 'therese-backend[voice-local]'`. Détails : `docs/VOICE-LOCAL.md`.

## Suivi (non bloquant)

- **BUG-111** : validation réelle sur Windows à faire par Dr_logic-3D (il attendait ce correctif pour reprendre la recette S1). UX « lister les données + confirmer » avant suppression = amélioration future.
- **Voix locale** : brancher l'UI (toggle réglages + bouton micro/lecture) sur les endpoints `/voice/local/*` et `/voice/tts` ; valider en réel STT/TTS après installation du groupe optionnel + téléchargement des modèles (notamment l'API Piper).
- **Process Zézette** : `gh pr create` toujours cassé dans son runtime (push OK) → PR ouvertes à la main. Tâche dédiée en cours.
