# Release v0.41.0-alpha - 17/07/2026

Chantier « harmonisation ancienne/nouvelle interface » demandé par Ludo après
la 0.40.1, mené sur main par mac-claude (14 commits), rituel fafa-et-soso :
une revue adversariale unique en fin de chantier (règle posée ce jour), NO-GO
6 findings, remédiation, contre-vérification (4 fermés + 2 résiduels fermés
sur prescriptions). Mode classic (ChatLayout) volontairement intact : secours
pendant l'alpha, retrait envisagé en 0.42.

## Livré

| Sujet | Détail |
|---|---|
| Scroll dans un mail (vue complète) | Wrapper des vues embarquées en flex flex-col + min-h-0 : la zone de scroll du message est enfin bornée (prouvé 433/4368 px). Profite aux 10 vues |
| BUG-122 (définitif) | Dossier IMAP spécial non résolu -> liste vide + avertissement honnête, plus jamais l'INBOX servie sous l'onglet ; la sélection est purgée (fin du « Message introuvable ») |
| État vide volet mail | Tuile + titre + aide, standards 0.40 |
| Board complet | Portraits officiels des 5 conseillers (desktop + mobile), strip androïde SUPPRIMÉ (asset retiré), halo neutre, hauteur au contenu compensée (pb-10), Échap via la pile des focus traps dans la coquille animée (useIsPresent : la protection tient pendant l'exit), aria-label |
| Contacts / Mémoire | text-left sur les lignes (le <button> centrait société/email), badge « RGPD ? » explicite, entête standalone avec actions responsive (wrap + libellés masqués sous sm), footer réservé au tiroir |
| BUG-139 (définitif) | Navigation déterministe du chat : événement revendicable therese:client-action (coque -> vue embarquée, repli registre classique), exécution différée en fin de flux dans le finally (survit à une erreur réseau), annulée sur interruption volontaire (AbortError) ou message en file, garde blockStreamingNavigation sur l'état vivant du store. Prouvé 6/6 vues |
| Calendrier dates civiles | localDateKey remplace 9 toISOString : cellules en date UTC vs événements en date civile = décalage d'un jour en UTC+ (anneau « aujourd'hui » sur demain, RDV du vendredi dans la case samedi). Prouvé : anneau revenu sur le 17 |
| Avatar unifié | 3 usages du PNG pré-0.40 -> CharacterPortrait index 0 (atlas), PNG supprimé |
| Vestiges | DataTab orphelin supprimé, advisors-bg.png supprimé |

## Revue Soso (unique, fin de chantier)

NO-GO initial : F1 Échap du Board hors pile des traps (critique), F2a prompt en
file écrasé par la navigation, F2b navigation perdue sur erreur de flux, F2c
entête Mémoire non responsive, F3a débordement translateY de l'arc, F3b
sélection non purgée au warning IMAP. Contre-vérification : F2b/F2c/F3a/F3b
fermés, F1 (trap lâché pendant l'exit AnimatePresence) et F2a (AbortError)
fermés ensuite sur ses prescriptions exactes (BoardDialogShell + useIsPresent,
annulation sur interruption volontaire). Coût : ~400k + ~127k tokens.

## Gates

- Frontend vitest : 681 tests, 0 échec ; tsc clean ; ESLint 0 erreur
- Backend pytest : 1625 tests, 0 échec (1 flake environnemental Ollama local,
  vert avec OLLAMA_BASE_URL mort - la CI n'a pas d'Ollama)
- mypy fresh 999 = baseline ; ruff clean
- Recette visuelle : captures avant/après dans docs/plans/captures-harmonisation/

## Reste connu (hors périmètre, suivi)

- Boutons à l'ancienne dans Facturation (pilule cyan) - passe fine ultérieure.
- Vues fichiers/accueil non repassées en profondeur (entêtes déjà standards).
- Mode classic à retirer en 0.42 après recette de la coque.
- BUG-140 updater Windows : à confirmer avec lcjp après install manuelle.
