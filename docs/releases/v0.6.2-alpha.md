# Release v0.6.2-alpha - 12 mars 2026

## PRs incluses

| PR | Type | Titre | Décision |
|---|---|---|---|
| #37 | fix | Message d'erreur OpenRouter 403 lisible | MERGER (PASS 3/3) |
| #40 | fix | Accents français + métadonnées + sécurité | MERGER (WARNING : inclut #38+#39) |
| #41 | perf | Lazy loading React + syntax highlighter light + ErrorBoundary + ARIA | MERGER (WARNING : inclut #43) |
| #42 | ci | Audit sécurité dépendances + fix timeout masqué | MERGER (WARNING : pip-audit fix post-merge) |
| #44 | fix | DialogShell réutilisable avec role=dialog et focus trap | MERGER (PASS avec fixes overlay+disabled) |
| #38 | fix | Cohérence métadonnées | FERMÉE (incluse dans #40) |
| #39 | fix | Corrections sécurité | FERMÉE (incluse dans #40) |
| #43 | fix | ErrorBoundary + ARIA + skip link | FERMÉE (sous-ensemble de #41) |

## Audit multi-agents (mode complet)

### Agent 1 - Chercheur Web (Sécurité)
- **Verdict global** : PASS
- Path traversal delete_backup : validation regex + resolve correcte
- /api/shutdown retiré des exemptions auth : OK
- RGPD old_name supprimé du log : OK
- Parsing JSON OpenRouter robuste (try/except, isinstance, borne 200 chars)
- Bug identifié : pip-audit CI `pip install uv` dans `<(...)` mélange logs
- **Corrigé post-merge** : séparation install uv + shell: bash explicite

### Agent 2 - Expert UX/UI
- **Verdict global** : 7.5/10
- Accents : gain réel pour l'expérience francophone
- ErrorBoundary fallback : cohérent charte, bouton cyan, role="alert"
- Skip link : bien implémenté (sr-only + focus visible)
- Lazy loading : risque flash faible (app desktop, réseau local)
- **Point soulevé** : DialogShell overlay bg-black/50 vs convention bg-black/60
- **Corrigé post-merge** : overlay aligné sur bg-black/60 backdrop-blur-sm

### Agent 3 - Avocat du diable
- **Verdict global** : PASS avec réserves
- Conflit PR #41 vs #43 identifié : recommandation merger #41 seule (appliquée)
- Conflit PR #40 vs #38+#39 identifié : recommandation merger #40 seule (appliquée)
- DialogShell : éléments disabled dans focus trap non filtrés
- **Corrigé post-merge** : :not([disabled]) ajouté au sélecteur
- CI || true sur pip-audit/npm audit : intentionnel (alpha, informatif)
- Langages hljs manquants (rust, go, markdown) : planifié P1

### Décision Claude Code (arbitre)
- **MERGER AVEC RÉSERVES** : 5 PRs mergées, 3 fermées, 3 corrections post-merge
- Aucun bloquant technique
- Ordre de merge optimisé pour éviter les conflits (37→42→40→41→44)

## Résumé technique

- **Version** : 0.6.1 → 0.6.2
- **Fichiers modifiés** : 34 fichiers, +626/-212 lignes
- **Nouveaux fichiers** : DialogShell.tsx, GlobalErrorBoundary.tsx
- **Tests de régression** : +100 lignes (test OpenRouter 403)

## Corrections post-merge

1. CI pip-audit : séparation `pip install uv` du process substitution
2. DialogShell overlay : `bg-black/50` → `bg-black/60 backdrop-blur-sm`
3. DialogShell focus trap : ajout `:not([disabled])` au sélecteur

## Warnings et dette technique

- [ ] Retirer `|| true` sur pip-audit/npm audit quand les deps sont assainies
- [ ] Ajouter langages hljs manquants (rust, go, markdown, dockerfile)
- [ ] Script de bump version unique (4 fichiers à synchroniser)
- [ ] Migrer les 13 modales restantes vers DialogShell
- [ ] ErrorBoundary : envisager inline styles pour résilience maximale
- [ ] Focus trap : restauration du focus sur l'élément d'origine à la fermeture

## Contexte

Cette release implémente les 10 quick wins P0 identifiés par l'UltraJury du 12 mars 2026 (score initial 64.1/100). Score estimé post-release : ~74/100.

Rapport UltraJury complet : `docs/ULTRAJURY-AUDIT-2026-03-12.md`
