# Release v0.69.0-alpha - 09/09/2026

## Nature

Version issue du cinquième cycle de la boucle autonome d'amélioration
(skill `boucle-amelioration-app`), en mode économe : les onze propositions
acceptées par Ludo au portail humain du cycle 4 (P-045 à P-054 sauf P-055, et
P-056), développées en TDD par l'orchestrateur, une par commit, design de la
plus risquée (P-056) challengé par COCO avant le code (NO-GO, refonte),
contrôle Chrome des sept surfaces sur une pile jetable. Aucune PR : 23 commits
directement sur `main`.

Décision de Ludo (09/09/2026, 07 h 30) : « go release 0.69.0-alpha ». Mode :
revue adversariale COCO du diff complet à la place de l'audit à trois agents,
comme pour la 0.67.0 et la 0.68.0.

## Contenu

| Mesure | Valeur |
|---|---|
| Commits depuis `v0.68.0-alpha` | 23 (9 `feat`, 5 `fix`, 2 `test`, 2 `style`, 2 `chore`, 3 `docs`) |
| Fichiers modifiés | 75 (2 782 insertions, 165 suppressions) |
| Propositions livrées | 11 (P-045 à P-054 sauf P-055, P-056) |
| Bugs fermés avant le tag | 8 (B-659 à B-666, revue COCO du diff : 3 critical, 5 high) |
| Tests de régression ajoutés | 56 dans 21 fichiers nouveaux (pytest 3 323, vitest 1 921 au total) |
| Bump | MINOR (neuf `feat`) |

### Ce qui change (détail dans les notes GitHub et le changelog Discord)

- Atelier documentaire : la génération de trame est un traitement annulable
  (P-056 ; registre des traitements, porteuse détachée, annulation avant ou
  pendant l'écriture, codes 409 `outline_in_progress`, `outline_cancelled`,
  503 `suivi_indisponible`).
- Livrables : création et changement de statut depuis « Livrables et suivi
  client » (P-048), validation côté moteur, `completed_at` posé et effacé.
- Composeur : formulaire des variables inconnues d'un prompt, enregistrement
  comme variables (P-049).
- Agents d'action : la fiche s'ouvre avant le lancement (P-051).
- Réglages : mention d'effort pour les GPT-5 avec outils, parité
  interface/moteur testée (P-045).
- Palette, ⌘⇧F, tiroir nommé et sans conversation vide, « Par où commencer »,
  casse des kanbans, plancher 14 px (P-050, P-052, P-053, P-054, P-047, P-046).
- CI : marge de 14 min pour le job Windows « Backend autonome » et garde du
  harnais alignée.

## Revue adversariale COCO (avant le tag)

`codex exec` en lecture seule, gpt-6-astra, effort max, `code_executor.py`
exclu (filtre cyber), 55 fichiers relus, 17 min. **Verdict NO-GO : 3 P1 et
5 P2, tous vérifiés dans le code et tous réels.** Corrigés en TDD (test
rouge, correctif, sabotage lu dans le JUnit ou vitest), un commit par sujet :

| Bug | Finding | Correctif |
|---|---|---|
| B-659 (P1) | la relecture dans la transaction d'écriture supprimait une section ajoutée à la main pendant la génération | `_ecrire_la_trame` ne remplace que les sections vides connues au lancement ; les ajouts sont conservés après la trame (`76a755e1`) |
| B-661 (P1) | une demande d'arrêt rejouée par `lier_adaptateur` annulait la porteuse avant son premier pas : `try` jamais entré, traitement figé `cancel_requested`, document bloqué | l'adaptateur est enrôlé avant le premier pas, la porteuse fait elle-même queued → running, un rappel de fin de tâche clôt une porteuse annulée avant d'avoir couru (`76a755e1`) |
| B-663 (P2) | annulation pendant la clôture DONE → CANCELLED et `TRAME_ANNULEE` alors que la trame est écrite | clôture DONE sous `asyncio.shield`, la persistance acquise vaut succès (`76a755e1`) |
| B-666 (P2) | un échec de clôture du suivi laissait une ligne `running` sans producteur, que `actif_pour` considérait active pour toujours | `actif_pour` ignore une ligne running ou cancel_requested sans adaptateur vivant ; clôture retentée une fois (`76a755e1`) |
| B-660 (P1) | le filtre P-054 du tiroir cachait une conversation sans message mais avec un brouillon local, devenu injoignable | listée avec « Brouillon en attente » (`cd58ee29`) |
| B-662 (P2) | la clé React du formulaire de variables dépendait de la liste des inconnues : un succès partiel remontait le formulaire et effaçait les refus | formulaire stable, qui suit la liste (`86fb37a4`) |
| B-664 (P2) | un livrable créé pendant le chargement initial était ignoré puis effacé par la liste lue avant | écritures en vol retenues et fusionnées (`37018ef7`) |
| B-665 (P2) | rouvrir le document réactivait « Générer la trame » ; le second clic recevait 409 et effaçait le bouton d'annulation du premier | le store refuse un second lancement suivi, le document rouvert garde son état de travail (`4ecbaa82`) |

Leçon : après six portes vertes, un contrôle Chrome et une revue de design,
la revue du diff trouve encore huit défauts dont trois pertes de données ou
blocages. Elle reste obligatoire avant chaque tag.

## Vérifications locales sur `4ecbaa82` (dernier commit fonctionnel)

| Porte | Résultat |
|---|---|
| ruff | vert |
| pytest `tests/` (JUnit) | 3 137 tests, 0 rouge, 3 sautés |
| pytest `src/backend/tests/` (JUnit) | 186 tests, 0 rouge |
| vitest (JUnit) | 1 921 tests ; 2 rouges sous charge (timeouts 5 s, load 24) verts seuls, verts en CI |
| tsc | vert |
| eslint | 27 warnings (plafond 27) |
| mypy, cache vidé, options de la CI | 954 = baseline |
| CI de main | verte sur `4ecbaa82` (V2, E2E, Windows deux jobs) |

## Chronologie (Paris, 09/09/2026)

- 07 h 30 : GO de Ludo. Pre-flight vert (lock, gh, dépôt propre sur `f1943c48`, VPS Principal et VPS Agents joignables en direct, tag libre, 0 PR).
- 07 h 34 : revue COCO du diff lancée en fond ; notes de release et changelog Discord rédigés.
- 07 h 51 : verdict NO-GO (8 findings). Vérification de chacun dans le code : tous réels.
- 07 h 55 à 08 h 20 : quatre tests backend rouges puis verts, sabotages ; quatre tests frontend rouges puis verts, sabotages ; portes complètes.
- 08 h 22 : cinq commits de correctifs poussés (`76a755e1`, `cd58ee29`, `86fb37a4`, `37018ef7`, `4ecbaa82`), B-659 à B-666 enregistrés.
- 08 h 27 : CI verte sur `4ecbaa82`.
- 08 h 29 : bump `b74e3848`, tag `v0.69.0-alpha`, draft pré-créée, build 34319286721 lancé.
- 08 h 42 : gate de release vert (8 jobs CI + bout en bout, backend 12 min) ; builds lancés.
- 08 h 52 macOS, 08 h 57 Windows, 09 h 03 Linux : build vert, 10 assets + `latest.json` (7 plateformes signées).
- 09 h 03 : release publiée (pré-release), landing `synoptia.fr/therese/alpha/` en 0.69.0 (sauvegarde `therese-landing-pre-0.69.0.tar.gz`), `latest.json` en ligne.
- 09 h 04 : app installée (`/Applications/THERESE.app` = 0.69.0, ancienne à la Corbeille, quarantaine retirée).
- 09 h 06 : smoke sur l'app installée, dossier de données jetable : `/health` sain en 17 s (`version 0.69.0`, base et Qdrant vrais), quit en 2 s, aucun processus restant, port libre.
- 09 h 08 : changelog posté dans #changelog (deux messages, ids 1547141275080331344 et 1547141287508181053) depuis le VPS Agents en direct ; Katia (`therese-src` en 0.69.0, `memory/CHANGELOG.md`) et Zézette (`MEMORY.md`) à jour ; landing commitée dans `synoptia-site` (`df4235a`).

## Contrôle post-release sur la version installée

| Contrôle | Résultat |
|---|---|
| Version affichée par `/health` | 0.69.0, `database` et `qdrant` vrais |
| Démarrage à froid (dossier vierge, Qdrant créé) | sain en 17 s |
| Fermeture après démarrage complet | 2 s, `Shutdown demandé via /api/shutdown`, sidecar terminé (signal 15), aucun processus, port 17293 libre |
| Fermeture PENDANT le démarrage (premier essai, quit à 20 s) | **backend orphelin** : `/api/shutdown` sans réponse, `child.kill()` tue le bootloader PyInstaller, `pkill -9 -P` ne trouve plus l'enfant déjà réattaché à PID 1, le serveur reste en écoute sur 17293 avec l'ancien dossier de données. Tué à la main. Pré-existant (lib.rs, hors diff 0.69.0) : **B-667 candidat**, à reproduire au cycle suivant (collecter les enfants AVANT de tuer le parent) |
| Updater | `latest.json` servi en 0.69.0, 7 plateformes |

Le port 17293 de l'application réelle de Ludo n'était pas en service pendant le smoke (le sidecar du smoke l'a pris) ; l'app installée n'a pas d'autre port. À noter pour les prochains smokes : vérifier que rien n'écoute sur 17293 avant de lancer.

## Warnings et dette

- [ ] Le job Windows « Backend autonome » varie de 5 min 42 à plus de 10 min pour la même suite : marge portée à 14 min, cause (runner partagé) non traitée.
- [ ] Deux tests vitest expirent sous forte charge locale (5 s) : `EcrireOuvreUnBrouillon`, `EmailSetupWizard.retourMcp` ; verts seuls et en CI, à surveiller.
- [ ] Une ligne de traitement orpheline (clôture en échec) reste affichée « en cours » dans « Travaux » jusqu'au redémarrage (récupération au démarrage) ; elle ne bloque plus rien.
- [ ] P-040 à P-044 (Contacts et agenda), P-055 et B-420 restent différés.
