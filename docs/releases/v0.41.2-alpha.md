# Release v0.41.2-alpha - 22/07/2026

Lot du triage Discord des 20-22/07 (Dr_logic + Capov sur la 0.41.1), mené sur
main par mac-claude (8 commits), rituel fafa-et-soso : une revue adversariale
unique en fin de lot, NO-GO 10 findings (4 bloquants), remédiation complète,
contre-vérification. Confirmation du triage : BUG-122 (rubriques mail) validé
FERMÉ par Dr_logic sur la 0.41.1 Windows 10.

## Livré

| Fiche | Détail |
|---|---|
| BUG-153 (CRITIQUE, fichée par mac-claude - signalée en #discussion, jamais fichée) | « Supprimer toutes mes données » effaçait SQLite/Qdrant/disque mais PAS la persistance frontend : les anciennes conversations (localStorage `therese-chat`) étaient rechargées au démarrage et renvoyées au LLM, y compris cloud. Nouveau `purgeLocalPersistence` : clés `therese-*`/`therese:*` du localStorage ET du sessionStorage + stores sans préfixe (`crm-storage` - le pipeline clients -, `calendar-storage`, `task-storage`), kill switch du stockage débouncé (sinon le flush de fermeture RÉÉCRIVAIT les données pendant le reload), purge immédiate puis redémarrage |
| BUG-146 | Recherche de contact insensible aux accents + rapprochements orthographiques sur zéro résultat (« Voulais-tu dire BODIN (Forge SA) ? », difflib >= 0.7, max 3, bornés 64 chars contre le blocage de la boucle asyncio, labels neutralisés) - répond au cas « Baudin »/BODIN de Dr_logic et réduit le risque de vrille du modèle sur résultat vide |
| BUG-149 | La clé legacy `agent_therese_model`, lue APRÈS `agent_katia_model`, écrasait à l'affichage le modèle choisi (OpenRouter) dans l'Atelier - le runtime lisait la bonne clé, bug d'affichage pur. Repli réordonné + purge de la legacy à l'écriture (testé : legacy seule conservée si on ne touche pas au modèle) |
| BUG-152 | La carte « Consulter mes emails » de l'accueil triait « non-lus d'abord » : figée sur de vieux messages. Tri par TIMESTAMP décroissant (Date.parse : les dates Gmail RFC 2822 cassaient un tri lexicographique), date illisible en fin |
| BUG-151 | Bandeau « Afficher les images » par email (opt-in DÉRIVÉ du messageId - un reset post-commit laissait partir le pixel du mail suivant -, jamais persisté). CSP `img-src` élargie à `https:` en conséquence, compensée : le rendu Markdown du chat ne charge plus AUCUNE image externe (placeholder inerte - une réponse LLM `![](https://tracker/x)` déclencherait sinon une requête sans consentement) ; DOMPurify interdit déjà `srcset` et `style` |
| BUG-150 | Identité des agents de l'Atelier en variables `--agent-*` theme-aware : assombries en clair (AA texte, magenta pink-700), vives en sombre, pastel AAA en mode contraste élevé (qui n'écrasait pas ces variables) |
| Scroll chat (suggestion Dr_logic) | Envoyer un message (envoi standard ET recherche approfondie) ramène en bas de la conversation ; la lecture de l'historique pendant la réponse reste respectée |

## Revue Soso (unique, fin de lot) : NO-GO -> remédiation -> contre-vérification

NO-GO initial, 10 findings dont 4 bloquants - tous confirmés par
contre-vérification de ma part puis fermés (commit e53a433) :
- **F1 (critique)** : le flush `pagehide` du stockage débouncé réécrivait la
  conversation purgée pendant le reload - kill switch global + purge immédiate.
- **F3/F4 (élevées)** : opt-in images non isolé par message (reset post-commit)
  et CSP `https:` profitant aux images Markdown du chat - état dérivé + filtre
  de sources locales.
- **F5 (majeure)** : tri des dates Gmail RFC 2822 par nom de jour - timestamp.
- F2 (sessionStorage), F6 (DoS difflib), F7 (injection sémantique des labels),
  F8 (contrastes magenta + mode contraste élevé), F9 (scroll recherche
  approfondie), F10 (test legacy manquant).

## Gates (après remédiation)

- Backend pytest : 1660 tests, 0 échec, 3 skips (`OLLAMA_BASE_URL` mort)
- mypy fresh 999 = baseline ; ruff clean (src/backend + tests, invocation CI)
- Frontend vitest : 714 tests, 0 échec ; tsc clean ; ESLint 0 erreur / 25 warnings (seuil 27)

## Dette et suivi

- BUG-146 volet corruption : le rapprochement réduit le risque ; le diagnostic
  du modèle du testeur (salve de questions Katia) reste attendu.
- BUG-142 (menu contextuel mission « terminer »), BUG-140 (updater, lcjp).
- Suggestions produit consignées (TODO) : temps passé par projet, vue graphe
  des fils d'emails + export FreeMind .mm, dossier local du chat (piste preset
  MCP Filesystem), renommages dossiers, UX Atelier sans Git (Windows).
- Volet 2 BUG-148 (pièce jointe facture : attachments Gmail), mode classic 0.42.
