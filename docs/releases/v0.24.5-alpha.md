# Release THÉRÈSE v0.24.5-alpha

**Date** : 2026-06-14
**Type** : PATCH (fiabilisation backend, fixes uniquement)
**Version** : 0.24.4 → 0.24.5
**Mode** : direct (aucune PR ouverte — corrections déjà mergées sur `main` au fil de la session, chacune testée)

## Contenu

Intégration des **12 P1 backend** du rapport de tests de Syn sur 0.24.4 (94 findings : 12 P1, 44 P2, 38 P3 — exécuté sur le vrai code `origin/main`, 10 personas API + parcours UI Playwright). Rapport source : `/tmp/RAPPORT-TESTS-THERESE-2026-06-14.md`.

| # | P1 | Commit |
|---|----|--------|
| 1 | Chat non-stream : filtre anti-injection + exfiltration prompt système (n'était appliqué qu'en streaming) | `36922e9` |
| 2 | Gemini 3 + outils cassé : `toolConfig mode=VALIDATED` + `include_server_side_tool_invocations` | `b22fcf3` |
| 3 | Escalation division par zéro (limites=0 → 500) : gardes token_tracker + validation Pydantic `gt=0` | `df98a7c` |
| 4 | `InstalledToolSkill` sans `get_markdown_prompt_addition()` → 500 sur modèle non-code (Gemini) | `74bf34e` |
| 5 | OAuth `redirect_uri` sur port 8000 au lieu de 17293 → Google rejette | `49b35c1` |
| 6 | Chat non-stream avale les erreurs provider (message vide) : flag opt-in `raise_on_error` | `3e93b7f` |
| 7 | MCP `/tools/call` ignore `server_id` explicite (homonymes → mauvais serveur) | `8710041` |
| 8 | BUG-027 volet board (token tracking) + synthèse deep-research qui avale les erreurs | `62a4d8c` |

Commit de fix de tests associé (régression introduite par le #5, non couverte par `test_regression.py`) : `2bee463` (`test(oauth)` : fixture `redirect_uri` dérivée de `RUNTIME_PORT`).

## Décision (arbitrage Claude Code)

Pas d'audit multi-agents formel : les corrections ont été réalisées une à une dans la session avec test de régression dédié et recherche web préalable (consigne Ludo : « fais des recherches web avant »). La veille a confirmé que Gemini exigeait `mode VALIDATED` en plus du flag, et que l'usage réel des tokens des providers existe (mieux que l'estimation mots×2 retenue, noté en dette).

**Verdict : MERGER / RELEASER.** Aucun risque critique. Les changements à surface large (flag `raise_on_error`) sont opt-in pour préserver le comportement des appelants existants.

## Porte qualité

- `uv run pytest tests/ --ignore=tests/e2e` : **1194 tests, 0 échec, 2 skip** (image providers, déjà connus).
- Régression spécifique : `test_regression.py` étendu (TestOAuthRedirectPort, TestStreamResponseRaiseOnError, TestMCPToolCallServerId, TestBoardTokenTracking, TestDeepResearchSurfacesSynthesisError, TestEscalationZeroDivision, TestInstalledToolMarkdownPrompt, TestChatNonStreamP1, TestV3GenerateTemplateLLMCall).
- **Incident rattrapé par la porte qualité** : le fix OAuth (#5) cassait 2 tests existants de `test_services_oauth.py` (redirect_uri en `:8000` codé en dur), non vus pendant la session (seul `test_regression.py` était lancé). Corrigé avant le tag.

## Build & publication

| Étape | Résultat |
|-------|----------|
| Preflight signature updater | OK (2s) |
| Build macOS ARM64 | OK (8m04s) |
| Build Windows x64 | OK (18m39s) |
| Build Linux x64 | OK (17m36s) |
| Assets release | 9 (dmg, app.tar.gz, exe, msi, deb + .sig + latest.json) |
| Landing `synoptia.fr/therese/alpha/` | OK (Alpha v0.24.5) |
| Updater `latest.json` | OK (v0.24.5, 7 variantes plateformes) |

Release : https://github.com/ludovicsanchez38-creator/Synoptia-THERESE/releases/tag/v0.24.5-alpha

## Warnings & dette technique

- [ ] **`/api/shutdown` sans token (CSRF)** : non corrigé (nécessite changement Rust `lib.rs` + rebuild signé). Reporté.
- [ ] **Usage tokens estimé (~2 tokens/mot)** sur chat non-stream ET board, au lieu de l'usage réel des providers. Refactor providers requis.
- [ ] **3 appelants de `stream_response` avalent encore les erreurs** (entity_extractor, action_agents, agents/runtime) : `raise_on_error` non activé (dégradation silencieuse tolérée).
- [ ] **`conversation_id="board"`** : étiquette générique pour le token tracking du board.
- [ ] **Node.js 20 deprecation (GitHub Actions)** : `actions/checkout@v4`, `setup-node@v4`, `setup-python@v5`, `sccache-action` passeront en Node 24 le 16/06/2026. Mettre à jour les actions avant cette date.

## Chronologie (UTC, 2026-06-14)

- ~14:00-14:27 : corrections P1 (8 lots) + commits/push sur main
- 14:31 : fix tests OAuth (porte qualité) + push
- 14:32 : bump 0.24.5 + lockfiles + tag + draft release pré-créée
- 14:32-14:51 : build 3 OS (succès)
- ~14:51 : publication release + landing + latest.json + vérifs endpoints
