# Release v0.29.0-alpha - 10/07/2026

## Contexte

Programme « suggestions testeurs » (GO Ludo : tout sauf multi-Thérèse), même
journée que la 0.28.3. MINOR : que des feat. Rituel par chantier : design ->
challenge Codex/GPT-5.6-sol -> GO Ludo -> TDD -> vérification navigateur
réelle (backend isolé sur THERESE_DATA_DIR jetable, launch.json posé).

## Contenu

| Lot | Commits | Livré |
|---|---|---|
| Chantier 1 - fichiers générés visibles | 89909b1 | Auto-exec AVANT `done` (le client coupait la lecture sur done : `skill_file` n'atteignait JAMAIS l'UI en direct - chat.py:1254 vs 1256, chat.ts:165, trouvé par Codex en analyse produit, confirmé source). Échec -> `skill_file_error` + notification (avant : logs seulement). Bloc « Fichier généré » (nom, taille fr, Télécharger, « Afficher dans le dossier » via local_dir + plugin-shell, repli notification hors Tauri). |
| Chantier 2.1a - navigation | 4690919 | `{action: ouvrir <vue>}` : message-action PUR exécuté localement, ZÉRO appel LLM (garanti par test : provider mocké qui échoue s'il est instancié). Allowlist stricte (chat_actions.py), action inconnue -> liste locale jamais transmise au modèle. Événement `client_action` (stream avant done + champ ChatResponse non-stream) -> `actionRegistry.runAction`. 7 entrées « ouvrir » au menu /. TODO navigate du CommandExecutor câblé (navigate_target). |
| Chantier 2.1b - production | fe49391 | `{action: produire docx\|xlsx\|pptx "sujet"}` : skill FORCÉ sans détection d'intention, boucle d'outils désactivée (rédaction pure), création/statut/erreur = chemin déterministe du chantier 1. Trous couverts : contenu LLM vide et skill introuvable -> `skill_file_error`. 3 entrées menu /. Répond structurellement à BUG-130. |
| Chantier 2.1c - découvrabilité | 5ca0413 | ActionChips (4 puces sur conversation vide, insertion de la syntaxe, disparition dès saisie), `{action: aide}` (liste locale = doc intégrée). A11y menu vérifiée (preventDefault Enter/Tab quand ouvert). |

Design du chantier 2 : `docs/plans/2026-07-10-actions-deterministes-design.md`
(V2 après revue Codex « ajustements requis » : noyau = CommandRegistry V3
existant, interception dans send_message avant le court-circuit slash,
message-action pur seulement, contrat Pydantic/TS synchronisé).

## Vérifications navigateur réelles (2 sessions, backend isolé)

1. (1b) Onboarding traversé, `/ouvrir` -> 7 entrées, sélection insère
   `{action: ouvrir crm}`, envoi -> navigation effective vers CRM Pipeline.
2. (1c) 4 puces affichées sur conversation vide, clic « Document Word » ->
   syntaxe insérée, puces masquées dès saisie.

Gotcha : le frontend Vite seul reste sur le splash (attend le backend) ;
lancer un backend isolé `THERESE_DATA_DIR=<jetable> uvicorn app.main:app
--port 17293 --app-dir src/backend` (vérifier d'abord que 17293 est libre -
sidecar de l'app installée).

## Gates (tous verts avant tag)

Backend 1454/1454 (hors e2e), vitest 417/417 (65 fichiers), tsc, ESLint
0 erreur/26 warnings, ruff clean, mypy fresh 999 = baseline.

## Suites (préparées, en attente)

- Designs V2 chantiers 3-4-5 (`docs/plans/2026-07-10-chantiers-3-4-5-designs.md`),
  revue Codex intégrée (Ollama : résolveur unique + provenance par message +
  anti-SSRF strict ; Variables : substitution backend, zéro effet de bord ;
  ExportProfile : Pydantic versionné + allowlist backup). Ordre proposé
  5 -> 4 -> 3. Implémentation prévue avec Codex (quota abo épuisé le 10/07).
- Tranches suivantes chantier 2 (hors périmètre 1) : créations typées,
  actions sensibles avec confirmation, variables dans les paramètres.
- BUG-113 toujours en attente des infos Dr_logic (fix ciblé).
