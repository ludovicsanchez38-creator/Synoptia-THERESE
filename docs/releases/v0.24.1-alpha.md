# Release v0.24.1-alpha

> Date : 11/06/2026 - Type : PATCH (0.24.0 → 0.24.1)

## Contenu

Pas de PRs externes : correctifs des 3 bugs remontés par **lcjp** (testeur alpha,
Windows + Mistral) le matin même sur le Discord #bugs, plus un fix mypy.

| Commit | Fix |
|---|---|
| 675e209 | **Multi-tours d'outils** (le bloquant) : les tours précédents sont rejoués dans le contexte de continuation. Avant : read_emails exécuté 3x en 1 s, résultats perdus, le modèle inventait une « erreur d'authentification ». ToolTurn + prior_turns sur les 9 providers (OpenAI-like factorisé, Anthropic blocks, Gemini tri positionnel, Ollama tool_name), accumulation à la récursion chat.py, résultats rejoués tronqués 4000c |
| fcc9b48 | **Calendrier Google 403 actionnable** : API Calendar non activée dans le projet GCP du testeur → message qui guide (activer l'API, reconnecter) au lieu d'un 500 « ça coince ». Guide alpha corrigé : client OAuth « Application Web », activer Gmail API ET Google Calendar API |
| c6ce235 | **Score CRM expéditeurs réparé** : qdrant.search(entity_type=) n'existait pas (TypeError avalé) et le payload Qdrant n'a jamais contenu le score. Lookup SQL get_crm_contact_by_email (par email, insensible à la casse) pour classify et generate-response |
| 32f8dae | **mypy** : les 4 erreurs de typage introduites par les fixes (998 < baseline 999). Le gate CI a fait son travail : run main rouge AVANT le tag, corrigé avant la release |

## Audit

Revue adversariale du fix multi-tours (le seul risqué) faite AVANT le commit :
formats des 9 providers vérifiés fidèles au code remplacé, 2 fakes de test mis
à jour, test Anthropic ajouté, troncature des résultats rejoués (budget
contexte) suggérée et appliquée. TDD sur les 3 fixes (RED vérifié avant chaque
implémentation).

Décision Claude Code : **MERGER** (commits sur main, CI 6 jobs verte avant tag).

## Résumé technique

- Version : 0.24.0 → 0.24.1 (7/7 fichiers + lockfiles npm/cargo/uv)
- Tests : backend 1169 verts (+13), frontend 221 verts, lint vert, mypy 998 < 999
- Build : 3 OS verts du premier coup, draft pré-créée (anti-403 désormais standard)
- Updater : latest.json 0.24.1 publié (7 plateformes signées)
- Landing : synoptia.fr/therese/alpha/ → 0.24.1 (serveur + CDN)

## Warnings et dette

- [ ] Validation RÉELLE du fix multi-tours avec Mistral : attendre le retour de lcjp après sa mise à jour (ses scénarios « lis mes emails non lus » / requête calendrier).
- [ ] Relaunch auto post-update toujours en fallback manuel (dette 0.24.0, inchangé).
- Reste : CLAUDE.md section « Dette technique ».

## Chronologie

- 16:10 - Pre-flight OK (0 PR, tag libre, repo propre)
- 16:12 - CI main ROUGE détectée avant tag : mypy 1003 > baseline 999 (4 erreurs
  introduites par les fixes lcjp). Diff par contenu vs worktree 9e2b08e →
  4 fixes triviaux (annotation get_crm_contact_by_email, list[dict] paramétrés)
  → 32f8dae → CI verte
- 16:25 - Bump 0.24.1 (7/7 + 3 lockfiles), tag poussé, draft pré-créée
- 16:44 - Build SUCCESS (3 OS + preflight signature)
- 16:45 - Release publiée (10 assets), landing 0.24.1, latest.json déployé
- 16:46 - Test updater 0.24.0 → 0.24.1 : mené par Ludo lui-même sur sa machine
