| Constat V3 | Statut | Où la V4 le traite / ce qui reste |
|---|---|---|
| 1 (P1) | partiel | l.190-203 et l.164-170 ; le contrôle est placé avant `begin()` (n° 2) et la recherche par Message-ID redépose ce qu'elle ne trouve pas (n° 1) |
| 2 | partiel | l.197-199, l.117-119 ; « en-tête purgé aussitôt » contredit l.180-181 et rend `compte_absent` injoignable (n° 4) |
| 3 | partiel | l.111-119 ; revalidation écrite, mais l'empreinte se périme à presque tout geste et la suite du 409 fabrique un second lot (n° 3) |
| 4 | partiel | l.176-186 ; planificateur non nommé, travail purgé avant `purge_apres`, lot sans travail jamais échu (n° 6) |
| 5 | traité | l.131-143 ; restent la phase du délai et l'élément déjà réservé (n° 8) |
| 6 | partiel | l.161-170 ; usage écrit, mais « absent = non_tenté » réintroduit le doublon (n° 1) et l'échec de la recherche n'a pas de sort (n° 5) |
| 7 | traité | l.150-155 |
| 8 | traité | l.245-254 |
| 9 | traité | l.255-262, l.45-46 |
| 10 | traité | l.22-28 |
| 11 | traité | l.74-79 |
| 12 | traité | l.210-213 |
| 13 | traité | l.182-189 |
| 14 | traité | l.171-175 ; le filet du GET perd sa source quand le travail est purgé (n° 6) |
| 15 | traité | l.238-244 |
| 16 | traité | l.86-88, plus de signature dans l'empreinte |
| 17 | traité | l.43-52 ; reste l'ordre qui désigne le « second » contact (n° 11) |

1. P1 - docs/plans/2026-09-24-p096-brouillons-en-lot.md:166-168 - « trouvé = `créé`, absent = `non_tenté` » redépose des brouillons qui ont existé : l'absence dans les Brouillons ne prouve pas que le dépôt n'a pas eu lieu, et la reprise recrée un brouillon pour un contact déjà servi (promesse « reprise sans doublon » trahie) - preuve : l.148-149, l'incertain reçoit la consigne « vérifie tes Brouillons » : le design envoie lui-même l'utilisateur dans sa messagerie, où il envoie ou supprime ce brouillon, qui quitte alors le dossier ; l.166-168 le reclasse ensuite en `non_tenté` et la reprise le redépose ; la V3 excluait l'incertain sans condition (revue V3 n° 6, « l'incertain est exclu de la reprise ») ; même trou pour un `non_tenté` restauré (l.199-201) dont le brouillon a été envoyé entre la sauvegarde et la restauration, et pour un fil de session abandonné (l.133-136) qui achève son APPEND après la recherche ; aucun geste de l'utilisateur dans sa messagerie n'est envisagé dans le design - correction : la recherche ne fait que promouvoir (trouvé dans Brouillons = `créé`) ; absent, un `incertain` reste `incertain`, exclu de la reprise, avec sa consigne et un geste explicite par élément « Recréer ce brouillon » ; la restauration, après l'extraction, passe en `incertain` les `non_tenté` des lots restaurés dont un travail a déjà tourné ; tests : brouillon incertain envoyé puis reprise (aucun dépôt), brouillon incertain supprimé puis reprise (aucun dépôt sans le geste).
2. P2 - docs/plans/2026-09-24-p096-brouillons-en-lot.md:191-197 - Le contrôle « lot en cours » placé AVANT `maintenance_mode.begin()` laisse passer un lot lancé entre les deux, et `begin()` attend même que ce lancement aboutisse - preuve : src/backend/app/services/maintenance.py:35-48, tant que `_active` est faux, `admit` compte tout `POST /api/...` comme TRACKED, dont `POST …/batch` et `…/reprendre` ; maintenance.py:52-58, `begin()` attend `_active_requests == 0`, donc la fin de ces requêtes, qui créent le `ProcessingTask`, lancent le travail et répondent 202 ; un POST encore en validation au moment du contrôle n'a aucun travail visible, et le contrôle, lecture asynchrone de la base, cède la boucle ; src/backend/app/routers/data.py:1443, puis 1453 (`close_db()`) et 1464-1474 (extraction) s'enchaînent alors avec un lot vivant : le scénario du P1 V3 ; la purge totale, elle, n'entre pas en maintenance (data.py:564-642) et `DELETE /api/data/all` n'est pas en BYPASS dans `admit` (seule la restauration l'est, maintenance.py:39-42) : lui appliquer `begin()` la ferait s'attendre elle-même ; l'arrêt proposé à l'écran est coopératif (src/backend/app/services/task_registry.py:88-99, `AnnulationCooperative.annuler` rend False), le travail reste `cancel_requested`, donc actif, jusqu'à 30 s en IMAP (src/backend/app/services/email/imap_smtp_provider.py:38) : « relancer le geste » tout de suite reprend un 409 - correction : dans la restauration, contrôle APRÈS `begin()` (plus aucune requête admise, toutes les admises terminées) : un travail `brouillons_lot` actif au sens d'`actif_pour` (src/backend/app/services/traitements.py:304-322) donne `end()` puis 409 ; pour la purge totale, garder le 409 en contrôle d'entrée sans maintenance et écrire que la réservation conditionnelle (l.123-124) arrête un lot lancé pendant la purge, avec un test (si la purge doit passer par la maintenance, ajouter `DELETE /api/data/all` au BYPASS d'`admit`) ; l'écran attend l'état terminal du travail avant de proposer de relancer ; test : `POST …/batch` en cours de validation quand la restauration arrive.
3. P2 - docs/plans/2026-09-24-p096-brouillons-en-lot.md:112-115 - La reprise revalide contre une empreinte qui se périme à presque tout geste, et la suite du 409 n'est pas définie : elle fabrique un second lot et laisse le premier reprenable à vide ; avec les n° 1 et 4, trois décisions manquent encore, le design n'est pas codable sans elles - preuve : l.86-88, l'empreinte porte l'étape (`stage`) de chaque contact du lot et la révision des Variables ; src/backend/app/services/variables_service.py:346-352, `variables_revision` hache TOUTES les Variables (`name:updated_at`) : une Variable absente du modèle, modifiée par le chat, périme la reprise ; passer en « client » un contact déjà servi (le geste attendu après un e-mail) la périme aussi ; supprimer une fiche efface ses éléments (l.182-184) et change pour toujours l'ensemble « des mêmes contacts » ; après le 409, « l'écran rouvre l'Aperçu sur les contacts restants » (l.115), et l'Aperçu ne conduit qu'à `POST …/batch`, qui crée un NOUVEAU lot (l.96-99) avec de nouveaux Message-ID (l.161-162, hachage de `batch_id`) ; l'ancien lot garde ses `non_tenté` et son « Reprendre » (l.243-244), qui rendra 409 à chaque clic et rouvrira l'Aperçu sur des contacts déjà servis par le second lot, sans signalement au-delà de 24 h (l.49-52) - correction : l'empreinte de reprise ne porte que sur les contacts restants et les Variables citées par le modèle ; après un 409, la reprise se confirme sur le MÊME lot (`POST …/reprendre` reçoit l'empreinte du nouvel Aperçu, les Message-ID restent stables) ; à défaut, l'ancien lot est clos (restants en `abandonné`, `purge_apres` posé, plus de « Reprendre ») ; tests : Variable hors modèle modifiée, étape d'un contact servi changée, fiche servie supprimée, puis reprise.
4. P2 - docs/plans/2026-09-24-p096-brouillons-en-lot.md:197-199 - « son en-tête est purgé aussitôt » contredit la purge qui épargne les lots actifs et la cause `compte_absent`, et peut effacer le signalement « déjà écrit » - preuve : l.180-181, la purge ne touche « jamais un lot dont un travail est actif » ; l'arrêt demandé est coopératif (src/backend/app/services/task_registry.py:88-99) et le travail reste `cancel_requested`, état actif (src/backend/app/models/processing.py:45-46), jusqu'à la fin du dépôt en cours, qui écrit ensuite son statut dans un lot purgé ; src/backend/app/routers/email.py:721-771, `disconnect_account` ne connaît ni travail ni lot et répond sans rien attendre ; l.117-119, `compte_absent` et « Compte déconnecté » supposent que le lot existe encore ; si la purge emporte les éléments, l'utilisateur qui reconnecte la même boîte (nouvel `account_id`) et relance ne voit plus le « déjà écrit il y a moins de 24 h » (l.49-52) des contacts qui ont déjà un brouillon - correction : la déconnexion demande l'arrêt, attend l'état terminal du travail (délai borné, sinon 409 « un lot se termine »), puis vide seulement l'objet et le corps du modèle dans l'en-tête ; en-tête et éléments restent jusqu'à `purge_apres` (compte rendu, `compte_absent`, « déjà écrit ») ; test : déconnexion pendant le 2e dépôt, puis GET du lot et liste des candidats.
5. P2 - docs/plans/2026-09-24-p096-brouillons-en-lot.md:164-170 - La recherche par Message-ID n'a ni lieu, ni dossier, ni sort en cas d'échec - preuve : l.166-167, « Avant toute reprise, chaque élément … est cherché » ne dit pas si c'est dans la requête `POST …/reprendre` (délai client de 30 s, src/frontend/src/services/api/core.ts:120) ou dans le travail ; chaque opération du fournisseur IMAP ouvre sa propre connexion (src/backend/app/services/email/imap_smtp_provider.py:513, 605, 647, `with self._connect_mailbox(...)`) : 50 éléments = 50 connexions si `chercher_brouillon` suit ce patron au lieu de la session de dépôt ; le dossier cherché n'est pas nommé ; une recherche en échec réseau n'est ni « trouvé » ni « absent », le design ne lui donne aucun sort, et un `except` qui conclut « absent » redépose ; faisabilité : IMAP possible (critère `Header` d'imap_tools, .venv/lib/python3.13/site-packages/imap_tools/query.py:12) ; Gmail : conservation de l'en-tête par `drafts.create` à vérifier comme le dit l.168-169, et le repli écrit ne couvre que l'incertain, pas le `non_tenté` restauré (l.199-201) - correction : la recherche tourne dans le travail, par la session de dépôt (une connexion), dans le dossier Brouillons résolu ; un échec de recherche arrête le lot en `reseau_avant_envoi` sans rien déposer ni reclasser ; le repli Gmail vaut aussi pour le `non_tenté` restauré (qui passe en `incertain`).
6. P3 - docs/plans/2026-09-24-p096-brouillons-en-lot.md:176-181 - La purge « une fois par jour de session » n'a pas de lieu, et sa source se désynchronise de la purge des travaux - preuve : le seul planificateur quotidien est `_rgpd_purge_scheduler` (src/backend/app/main.py:369-383, `asyncio.sleep(86400)`), sous `if not skip_services` (main.py:317) ; c'est une tâche de fond que le verrou de maintenance ne voit pas (maintenance.py:30-48 ne compte que les requêtes), elle peut donc écrire pendant une extraction de restauration ; `purger_les_terminees` efface le `ProcessingTask` à `created_at` + 30 jours (src/backend/app/services/traitements.py:376-384), avant `purge_apres` (fin + 30 jours), et le filet du GET (l.174-175, « dernier travail interrupted ») perd alors sa source ; un en-tête écrit sans travail (arrêt entre l'écriture et `creer_traitement`) n'a jamais de fin, donc jamais de `purge_apres` - correction : nommer le planificateur (tâche jumelle de `_rgpd_purge_scheduler`, annulée au shutdown) et sauter la passe si `maintenance_mode.active` ; poser `purge_apres` dès la création (création + 30 jours), réécrit à chaque fin ; fonder le filet du GET sur « élément `en_cours` sans travail actif » plutôt que sur la ligne du travail.
7. P3 - docs/plans/2026-09-24-p096-brouillons-en-lot.md:116-117 - Le refus 409 « travail actif » n'est pas écrit sous le verrou du lot - preuve : « puis, sous le verrou du lot, remet explicitement les échecs réessayables à `non_tenté` et relance. Refus 409 si un travail du lot est actif » ; `actif_pour` est une simple lecture (src/backend/app/services/traitements.py:304-322) : deux `POST …/reprendre` simultanés la passent tous deux avant que l'un ait créé son travail, et deux travaux se partagent le lot (la réservation évite le double dépôt, pas deux fins qui réécrivent `purge_apres` et le compte rendu) - correction : contrôle, remise en `non_tenté` et `creer_traitement` sous le même verrou ; test : deux reprises simultanées, un seul travail.
8. P3 - docs/plans/2026-09-24-p096-brouillons-en-lot.md:136-137 - « un dépôt dont la fonction n'a jamais démarré reste `non_tenté` » alors que la réservation l'a déjà passé `en_cours`, et le type d'exception ne dit pas la phase en IMAP - preuve : l.123-124, « UPDATE conditionnel non_tenté vers en_cours » AVANT chaque dépôt ; src/backend/app/services/email/imap_smtp_provider.py:303-316, `_run_imap_operation` lève `TimeoutError` quand `wait_for` expire, et un délai de socket (15 s, l.37) est aussi un `TimeoutError` depuis Python 3.10 : connexion expirée et lecture expirée après APPEND ont le même type - correction : le fil pose un drapeau « APPEND émis » juste avant la commande ; sans drapeau, l'élément est remis explicitement de `en_cours` à `non_tenté` ; avec drapeau, `incertain` ; test sur les deux phases.
9. P3 - docs/plans/2026-09-24-p096-brouillons-en-lot.md:108-110 - « fiche supprimée » ne peut jamais s'afficher - preuve : l.182-184, les éléments d'un contact partent à la suppression de la fiche ; les seules suppressions de fiche sont src/backend/app/routers/memory.py:976 et src/backend/app/routers/data.py:632, toutes deux couvertes - correction : retirer « fiche supprimée » du GET (décision 8, « effacés avec le contact »), ou dire quel chemin garde un élément sans fiche.
10. P3 - docs/plans/2026-09-24-p096-brouillons-en-lot.md:124-126 - Les tests « deux POST simultanés » et « même POST après la fin du lot (nouveau lot) » n'ont pas de résultat attendu qui protège d'une double soumission - preuve : le moteur génère `batch_id` (l.97-99), deux POST identiques font donc deux lots et 2N brouillons ; « nouveau lot, pas de réécriture » écrit ce doublon comme attendu ; seule la garde d'écran (l.234-236) protège d'un double envoi du même formulaire (le « déjà écrit il y a moins de 24 h », décision 6, reste un signalement d'écran et n'est pas en cause ici) - correction : le moteur refuse (409) un `POST …/batch` dont l'empreinte est identique à celle d'un lot actif ou créé dans la même minute ; tests avec ce résultat.
11. P3 - docs/plans/2026-09-24-p096-brouillons-en-lot.md:43-46 - « un second contact à la même adresse est bloqué » sans ordre qui désigne le second - preuve : l'empreinte trie les tuples (l.86-87) mais la liste `contact_ids` reçue n'a pas d'ordre imposé ; si l'Aperçu et la création reçoivent les identifiants dans deux ordres, l'Aperçu bloque B et montre A, la création bloque A et dépose pour B, avec la même empreinte, donc sans 409 - correction : le moteur trie les identifiants avant d'appliquer la règle (le premier par identifiant est gardé), ou bloque tous les contacts de l'adresse ; test à ordre inversé.
12. P3 - docs/plans/2026-09-24-p096-brouillons-en-lot.md:80-85 - La règle Python de `jetonsMalFormes` perd l'exclusion des accolades vides - preuve : src/frontend/src/services/api/variables.ts:120, `if (!contenu.trim() || …) continue;` écarte `{   }` ; la règle écrite l.82-83 (`1 <= len(c) <= 40 and all(ch.isalnum() or ch in "_- " …)`) le classe mal formé : le lot est refusé sur un modèle que l'écran déclare propre - correction : ajouter `c.strip() != ""` à la règle et le cas `{   }` au jeu de cas commun.

VERDICT : NO-GO
