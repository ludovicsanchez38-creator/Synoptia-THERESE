# Revue adverse B des RFC P-109, P-110, P-121, P-125 (25/09/2026)

Base relue : RFC du commit 33e0955d (25/09, 15:45) et `main` à 99fa03c7 (40 commits plus tard ; 55b18b42, arrivé pendant la relecture, n'ajoute que de la documentation et un instrument de test). Les numéros de code « à HEAD » sont ceux de 99fa03c7, sauf mention « (33e0955d) ». « Périmé » = exact à l'écriture, dépassé depuis ; « faux » = déjà inexact à 33e0955d. Chemins de code relatifs à `src/backend/app/` et `src/frontend/src/`.

## RFC P-109 : conversation vocale (docs/plans/2026-09-25-rfc-p109-conversation-vocale.md)

1. P1 - rfc-p109:104 - « L'audio n'est jamais conservé ; les fichiers temporaires sont effacés comme aujourd'hui » est faux pour le chemin de l'app packagée, qui est aussi le repli de la RFC (l.178, l.223) : chaque dictée laisse un WAV sur disque, sans limite de durée - Preuve : le greffon écrit `app_data_dir/tauri-plugin-mic-recorder/{horodatage}.wav` (~/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/tauri-plugin-mic-recorder-2.0.0/src/commands.rs:237-249) ; `hooks/useVoiceRecorder.ts:159-168` lit le fichier et ne l'efface jamais ; sur le Mac de Ludo, 11 WAV conservés depuis février dans `~/Library/Application Support/fr.synoptia.therese/tauri-plugin-mic-recorder/`, dont `20260216132146.wav` de 1,5 Go ; aucune occurrence de « mic-recorder » dans `src/backend/app`, donc « Effacer toutes mes données » ne purge pas ce dossier - Correction : au lot 0, effacer le WAV juste après sa lecture, purger le dossier au démarrage et dans la purge RGPD, test qui compte les fichiers après deux dictées ; corriger la phrase ; ficher le défaut existant de la dictée.
2. P2 - rfc-p109:77, :103, :154-157 - La RFC ne traite que l'accord Groq ; or l'accord cloud du modèle (finalités `llm` ou `documents`, par fournisseur réel) n'est contrôlé que dans le composeur, et la session vocale appelle `streamMessage` en dehors de lui : le texte partirait chez le fournisseur en ligne sans accord - Preuve : `components/chat/ChatInput.tsx:550-576` (finaliteCloud, `fournisseurDAccord`, `hasCloudConsent(finaliteCloud, destinationMessage)`) ; aucune occurrence de « consent » dans `routers/chat.py` ; rfc-p109:77 « Groq et le fournisseur du modèle ont déjà leurs accords » - Correction : au démarrage de la session, avant la première parole (plus d'écran ensuite), vérifier les trois accords réels (écoute, modèle, `documents` si la conversation en porte) en factorisant la garde du composeur ; test « modèle en ligne sans accord : rien n'est envoyé ».
3. P2 - rfc-p109:156, :209 - La RFC nomme une « catégorie » (audio de la conversation vocale) là où il faut une finalité : l'accord ne se distingue que par sa finalité, et une catégorie ajoutée sous `voice` serait satisfaite par l'accord de la dictée ; trois fichiers ne connaissent que `voice` - Preuve : `lib/consent.ts:26` (`CloudPurpose` = llm, voice, images, documents), `:40-42` et `:82-88` (la clé est `finalité:fournisseur`, `dataCategories` n'est jamais testé) ; `services/api/voice.ts:154-161` contrôle `'voice'` en dur ; `components/settings/PrivacyTab.tsx:56-61` et `:84-93` ne connaissent que `voice` - Correction : nouvelle finalité (ex. `voice_conversation`) dans `CloudPurpose`, `PURPOSE_LABELS` et la révocation de PrivacyTab, finalité passée en paramètre à `transcribeAudio` ; le test l.209 doit s'appuyer sur elle.
4. P2 - rfc-p109:110, :89 - La lecture phrase par phrase repose sur des WAV joués par URL `blob:`, jamais vérifiée dans l'app packagée : sans `media-src`, la CSP retombe sur `default-src 'self'`, qui ne couvre pas le schéma `blob:` - Preuve : `src/frontend/src-tauri/tauri.conf.json:31` `default-src 'self'` sans `media-src` ni `blob:` ; la lecture actuelle est `<audio controls src={speechUrl}>` sur une URL blob (`components/prototype/VoiceWorkspaceCanvas.tsx:192`) ; elle échouait déjà en amont jusqu'à B-1410 (commit 2d70c92d, après la RFC), donc personne n'a pu l'observer - Correction : ajouter au lot 0 un essai de lecture dans l'app packagée sur les trois systèmes ; soit `media-src 'self' blob:`, soit lecture par Web Audio (`decodeAudioData`) ; même contrôle pour `audioWorklet.addModule` (script-src, pas de blob:).
5. P2 - rfc-p109:116, :130 - Le cycle de vie de la session face à la coque n'est pas défini : Échap est déjà consommé par la pile de la coque, qui ferme l'espace Voix ; rien ne dit ce que deviennent le micro, le flux et la lecture quand le panneau se ferme ou qu'on navigue, et Espace en arrêt global entre en conflit avec l'activation des boutons - Preuve : `components/prototype/ConversationCanvasPrototype.tsx:1583` (gestionnaire clavier), `:1600` (`consommeEchapUnifie()`), `:1613` (`else if (voiceOpen) collapseToolPanel('voice')`) ; rfc-p109:116 « touche Espace ou Échap » - Correction : inscrire la session dans la pile d'Échap unifiée (premier Échap = stop, second = fermer), règle écrite « fermer l'espace Voix ou changer d'écran arrête la session » (pistes micro stoppées, `cancelGeneration`, file vidée), Espace limité au focus de l'espace Voix ; test « panneau fermé : aucune piste micro active ».
6. P2 - rfc-p109:103, :117 - La session possède un flux de chat hors du composeur sans dire comment elle vit avec l'état global `isStreaming`, le verrou de navigation, la création et l'adoption d'identité de la conversation ; P-125 refond au même moment la propriété des flux (registre des réponses en cours) - Preuve : `stores/chatStore.ts:81` et `:340` (un seul `isStreaming`) ; `ChatInput.tsx:602-628` et `:666-679` (bulles, persistance, identité avant le flux) ; RFC P-125 § 4.1 - Correction : déclarer la dépendance (la session vocale passe par le registre de P-125, lots 1 et 2 de P-125 avant le lot 2 de P-109), ou préciser qu'elle pose `isStreaming` et subit le verrou ; dire qui crée et persiste la conversation.
7. P3 - rfc-p109:61-70, :176, :196 - Le préalable du lot 0 est déjà corrigé : B-1410 (2d70c92d, 25/09 à 15:48) fait accepter `voice: "fr"` ; le test rouge prévu ne peut plus rougir - Preuve : `services/voice_local.py:99` `_VOIX_PAR_LANGUE = {"fr": DEFAULT_PIPER_VOICE}` ; `tests/test_b1410_voix_fr_acceptee.py` - Correction : retirer ce point du lot 0 en citant B-1410 ; garder la mise en mémoire de la voix (`voice_local.py:292`, `PiperVoice.load` à chaque synthèse).
8. P3 - rfc-p109:57, :177, :197 - Voix gardée en mémoire et file de lecture : les synthèses partent chacune dans un fil (`to_thread`) sur une instance partagée, le cache Whisper n'a pas de verrou, et une synthèse lancée ne s'annule pas ; Piper, Whisper et Ollama se disputent aussi le CPU pendant la première phrase - Preuve : `routers/voice.py:317` (`asyncio.to_thread(synthesize_local, ...)`) ; `services/voice_local.py:239` (`_whisper_cache`, dict sans verrou) ; rfc-p109:117 « file audio vidée » ne dit rien des synthèses en vol - Correction : une synthèse à la fois (verrou serveur ou file côté client), verrou au chargement des modèles, mesure de latence au lot 0 avec Ollama et Piper en parallèle, interruption qui abandonne les réponses de synthèse en vol.
9. P3 - rfc-p109:171, :225 - « Le garde-fou de mémoire existe déjà (`services/system_resources.py`) » surestime l'appui : le module détecte la RAM totale pour dimensionner le contexte Ollama, pas la mémoire libre ni la charge Whisper, Piper et modèle local - Preuve : `services/system_resources.py:10-20`, `:76` ; seul appelant `routers/config.py:1707-1709` - Correction : prévoir un contrôle réel (mémoire libre, modèles chargés) ou retirer l'appui.
10. P3 - rfc-p109:119, :144 - « Aucun outil » n'est pas garanti par construction : les commandes déterministes (`/contact`, `/projet`) et les directives `[contact: ...]` s'exécutent avant et indépendamment de `disable_tools`, et créent sans carte - Preuve : `routers/chat.py:1451-1470` (`parse_slash_command` puis `execute_slash_command_outcome`), `:1538-1552` (`parse_inline_commands`) ; `disable_tools` n'agit qu'à `:2470-2471` - Correction : en `mode_vocal`, ne jamais interpréter une transcription comme commande ou directive ; test « transcription /contact nom=X : rien n'est créé ».
11. P3 - rfc-p109:31, :45 - Références décalées d'environ 30 lignes dès le commit de la RFC, et 40 commits depuis (dont P-118, ligne d'état « ce qui sort de la machine », et P-111, local proposé d'abord, qui touchent l'affichage des moteurs) - Preuve : à 33e0955d, `handleComposerTranscript` est à ConversationCanvasPrototype.tsx:1645 (RFC : 1617-1624) et `<VoiceWorkspaceCanvas` à :2314 (RFC : 2285-2294) - Correction : rebaser les appuis sur HEAD et relier « moteurs affichés et dits » à la ligne d'état de P-118.

Questions pour Ludo :
- Les échanges vocaux vont-ils dans une conversation neuve à chaque session, ou dans la conversation ouverte (qui peut porter des documents et un projet) ?
- En hybride, l'accord pour envoyer la conversation au modèle en ligne est-il demandé une fois au démarrage de la session, puisqu'aucun écran n'est regardé ensuite ?
- Les enregistrements de dictée déjà conservés chez les testeurs sont-ils purgés à la mise à jour, et le dit-on dans les notes de version ?
- Espace comme arrêt, ou seulement Échap et le grand bouton ?

VERDICT : NO-GO

## RFC P-110 : un Word reçu par mail, intégré dans un site PHP (docs/plans/2026-09-25-rfc-p110-word-vers-site-php.md)

1. P1 - rfc-p110:59-60, :101, :112 - `nh3` ne neutralise pas `<?php` dans les valeurs d'attribut que la liste blanche autorise : un texte de remplacement d'image ou une info-bulle de lien du Word arrive tel quel dans le `.php`, et le serveur de l'association l'exécute ; le test prévu ne vise que le texte - Preuve : `services/html_sanitizer.py:15-21` (a : href, title, target ; img : src, alt) ; sonde en lecture seule avec le nh3 0.3.4 du dépôt (`.venv`, sans écriture) : `<img src="i.png" alt="<?php system(1); ?>">` ressort identique, de même `title="<?php system($_GET[1]); ?>"` et `href="https://e.fr/?q=<?=1?>"` ; seul `<?php` en texte est retiré ou échappé - Correction : après nh3, échapper `<` et `>` dans toutes les valeurs d'attribut et refuser toute sortie qui contient encore `<?` ; test sur alt, title, href, nom d'image et légende ; sabotage ciblé sur cette fonction.
2. P2 - rfc-p110:59, :71, :113 - La garantie « déterministe, le texte passe tel quel » s'arrête à l'Atelier : c'est son modèle qui recopie le fragment dans la page ; il peut le réécrire, décoder `&lt;?php`, ou suivre une consigne cachée dans le Word, et la seule barrière est un diff PHP relu par un bénévole - Preuve : `services/agents/tools.py:730-735` (`write_file` écrit le `content` fourni par le modèle, sans contrôle du contenu) ; rfc-p110:113 s'en remet au « diff relu » - Correction : le fragment converti est inclus tel quel (`include` du fichier produit), le modèle ne touche qu'au gabarit et au menu ; contrôle automatique avant fusion (refus de tout `<?` ajouté hors gabarit, fragment inchangé) ; test de consigne cachée dans le Word.
3. P2 - rfc-p110:65 - Ajouter `*.php` à la recherche de l'Atelier expose au modèle, souvent en ligne, les secrets d'un site PHP sans framework (mot de passe de base en clair dans `config.php` ou `connexion.php`, `.htpasswd`) : le filtre des fichiers sensibles ne les connaît pas - Preuve : `services/agents/tools.py:88-95` (DOSSIERS_SENSIBLES et NOMS_SENSIBLES : `.ssh`, `id_rsa`, `.netrc`, `credentials.json`...) et `:178-192` (`.env*` et suffixes) ; aucun motif PHP ni Apache - Correction : étendre les noms sensibles (`config*.php`, `wp-config.php`, `*connexion*.php`, `.htaccess`, `.htpasswd`) ou masquer les affectations de mot de passe dans les résultats ; le guide demande de sortir les secrets de la copie ; test.
4. P2 - rfc-p110:66, :69-72 - Le guide échoue sur le code actuel : après `git init` sans commit, tous les fichiers sont non suivis et `ensure_clean` refuse la mission ; déposer le dossier converti (étape 2) salit à nouveau l'arbre ; sans commit il n'existe pas de branche de base pour l'espace isolé ; le lancement exige `main`, pas seulement la fusion ; git n'est pas embarqué et Dr_logic est sous Windows 10 - Preuve : `services/agents/git_service.py:380-388` (`git status --short`, qui liste les non suivis) et `:422-432` ; `services/agents/swarm.py:193-217` (« Le dépôt contient des changements non enregistrés ») et `:232` ; `routers/agents.py:232-235` et `:553-556` (lancement), `:870-873` et `:947-950` (fusion, annulation), la RFC ne cite que 870-874 ; `git_service.py:20` (B-958, git absent du PATH) - Correction : réécrire le guide (installer Git pour Windows, identité, `git add -A` et commit initial, commit du dossier converti avant la mission), inventorier toutes les exigences de `main`, recetter le guide mot pour mot sous Windows 10.
5. P3 - rfc-p110:116 - « sous la finalité `documents` » : l'Atelier ne passe pas par les finalités de `lib/consent.ts`, il a sa carte de confirmation d'appel ; et ce n'est pas seulement le Word qui part, c'est le site lu par l'agent - Preuve : aucune occurrence de `hasCloudConsent` ni `grantCloudConsent` sous `components/atelier` ; `components/atelier/AgentSession.tsx:243` (carte de consentement propre) - Correction : décrire la garde réelle et dire que les fichiers du site lus par l'agent partent chez le fournisseur de l'Atelier.
6. P3 - rfc-p110:61, :71 - Les images « aux noms normalisés » n'ont pas d'emplacement convenu dans le site : un fragment posé dans `pages/` casse ses chemins relatifs - Preuve : rfc-p110:61-63 et :71 muets sur le dossier cible - Correction : convention (dossier d'images par page, `src` calculé depuis la page) et test.
7. P3 - rfc-p110:57-72, :90-96 - Le lot 1 mêle la brique de conversion, livrable seule, et trois ajustements de l'Atelier qui dépendent de la question 1 (git) encore ouverte - Preuve : rfc-p110:85 (question 1) face à rfc-p110:64-67 (ajustements dans le même lot) - Correction : lot 1 = brique seule (aperçu et téléchargement, déjà utiles pour un collage à la main) ; ajustements de l'Atelier après la réponse de Ludo.
8. P3 - rfc-p110:77, :115 - Le signalement des données personnelles est renvoyé au lot 2 « sur demande », alors que le lot 1 publie déjà des documents de membres - Preuve : rfc-p110:77 et :115 - Correction : signalement minimal dans l'aperçu dès le lot 1, ou question explicite à Ludo.

Questions pour Ludo :
- THÉRÈSE peut-elle demander à un bénévole sous Windows d'installer Git, ou faut-il une voie sans git (copie avant et après, différence calculée par THÉRÈSE) ?
- Le site lu par l'Atelier, avec ses éventuels mots de passe, peut-il partir chez un fournisseur en ligne, ou ce cas exige-t-il un Atelier local ?
- La sécurité du site repose-t-elle sur la relecture du diff par le bénévole, ou exige-t-on un contrôle automatique bloquant avant fusion ?
- Signalement des données personnelles dès le premier lot ?

VERDICT : NO-GO

## RFC P-121 : un seul formulaire de devis (docs/plans/2026-09-25-rfc-p121-un-formulaire-de-devis.md)

1. P2 - rfc-p121:56, :61, :75, :95, :146, :162-166, :208 - L'inventaire qui porte la contrainte « aucune perte de fonctionnalité » est périmé : trois défauts qui fondent la recommandation ont été corrigés moins d'une heure après la RFC, chacun dans un seul hôte ; la question 1 est déjà tranchée par le code, les gardes « rouges aujourd'hui » du lot 0 sont vertes, le lot 4 est à moitié fait, les comptes de tests sont périmés - Preuve : B-1411 (46d6b12d, total au centime dans la modale, `InvoiceForm.tsx:46`), B-1413 (24b4ec96, date civile dans la modale), B-1412 (152caa71, retouche = `updateInvoice`, `InvoiceConversationCard.tsx:348`, `:453-454`, `usePrototypeInvoiceData.ts:78`) ; comptes exacts à 33e0955d (19 fichiers `InvoiceForm.*`), mais à HEAD 21 fichiers et 60 tests (RFC : 58 dans 19), 8 fichiers `InvoiceConversationCard.*` (RFC : 7) - Correction : rebaser § 2.2, § 2.3, § 4.4, les lots 0 et 4 et la question 1 sur HEAD ; noter que B-1411 a posé une troisième copie de l'arrondi (`arrondirAuCentime` à côté de `arrondirCentimes`), que la garde du lot 5 doit viser.
2. P2 - rfc-p121:118, :136 - `totauxDePiece` « miroir de `_montants_de_ligne` » est faux aux demi-centimes : le serveur arrondit par `round()` de Python (au pair, sur le binaire), les deux formulaires par `Math.round` (demi vers le haut) ; 2,5 × 1,25 € = 3,125 donne 3,12 au serveur et 3,13 à l'écran ; le test prévu (33,33 €) ne le voit pas - Preuve : `routers/invoices.py:206-210` ; `components/prototype/InvoiceConversationCard.tsx:137-139` et `components/invoices/InvoiceForm.tsx:46-48` ; exécution : `python3 -c "print(round(2.5*1.25,2))"` rend 3.12, `node -e "console.log(Math.round(2.5*1.25*100)/100)"` rend 3.13 ; quantité décimale supérieure ou égale à 1 acceptée (rfc-p121:60) - Correction : décider la règle d'arrondi (commerciale au demi supérieur, en centimes entiers) et l'appliquer au serveur comme au module commun ; ajouter 2,5 × 1,25 et 1,005 aux tests unitaires et à `tests/test_f1_coherence_des_couches.py`.
3. P2 - rfc-p121:133, :148, :154, :201 - L'union des règles appliquée à la modale peut rendre inenregistrables des pièces existantes : le serveur n'impose pas échéance postérieure à l'émission, des pièces créées par l'outil du chat, l'import ou l'API peuvent la violer, et la modale enregistre statut, notes et lignes par le même envoi ; la contrainte « aucune perte de fonctionnalité » serait rompue sur ces pièces - Preuve : `routers/invoices.py:438` et `:556-557` (dates reprises sans contrôle d'ordre) ; `InvoiceForm.tsx:346-445` (un seul `handleSubmit`, statut compris, puis `updateInvoice`) ; la validité ne vaut que pour un devis (`InvoiceForm.tsx:415`) - Correction : règles nouvelles seulement à la création ou quand le champ est modifié, validité conditionnée au type devis ; caractérisation au lot 0 « facture existante à échéance antérieure : on peut encore la marquer payée ».
4. P3 - rfc-p121:59, :128, :210 - « Taux nommés partout, 0% (exonéré) » ignore P-119 (7d1cbfc8, après la RFC) : le profil déclare maintenant un régime (assujetti, franchise, exonération formation) et « une ligne taxée n'est jamais dite exonérée » ; pour Claire, en franchise (art. 293 B), « exonéré » est juridiquement faux - Preuve : message du commit 7d1cbfc8 ; `InvoiceForm.tsx:55` `{ value: 0.0, label: '0% (exonéré)' }` - Correction : libellé du taux 0 dérivé du régime du profil (store partagé), question 3 à reformuler.
5. P3 - rfc-p121:146, :204 - La mise à jour après confirmation traite le brouillon supprimé (404) mais pas le devis passé entre-temps à envoyé, accepté ou converti dans Devis et factures : la mise à jour réécrit alors les lignes d'une pièce que le client a pu accepter - Preuve : `usePrototypeInvoiceData.ts:78` (`updateInvoice(idExistant, ...)` sans lecture du statut) ; `routers/invoices.py:500-560` ne contrôle que la validité du statut demandé, pas le statut courant avant de réécrire les lignes - Correction : relire le statut avant la mise à jour et refuser hors brouillon (client et serveur), test.
6. P3 - rfc-p121:23, :217 - Références décalées dès le commit de la RFC - Preuve : à 33e0955d, `setSelectedInvoiceId('new-devis')` est à ConversationCanvasPrototype.tsx:2045 (RFC : 2015-2018) et la troisième sortie retenue à :1341 (RFC : 1332) - Correction : rebaser sur HEAD.

Questions pour Ludo :
- Règle d'arrondi d'une ligne au demi-centime : arrondi commercial (3,125 donne 3,13) partout, serveur compris ?
- Les règles plus strictes (échéance, validité) s'appliquent-elles aux pièces déjà enregistrées, ou seulement aux nouvelles ?
- Le taux 0 suit-il le régime déclaré dans le profil (franchise, exonération) plutôt qu'un « exonéré » unique ?
- Un devis déjà envoyé ou accepté peut-il encore être retouché depuis Facturer ?

VERDICT : NO-GO

## RFC P-125 : la réponse se termine en fond (docs/plans/2026-09-25-rfc-p125-reponse-en-fond.md)

1. P2 - rfc-p125:111, :173 - La RFC ne plafonne pas les réponses de fond en ligne (« En ligne, les réponses partent en parallèle ») alors que chaque flux SSE tient une connexion HTTP/1.1 ouverte des minutes : les webviews (WebView2 sous Windows, WKWebView) plafonnent les connexions par hôte (6 en Chromium, à mesurer), et flux de fond, Board et Atelier en arrière-plan, recherche approfondie et trame documentaire peuvent occuper ce pool ; toute autre requête attend puis tombe au délai de 30 s - Preuve : `services/api/core.ts:122` (`API_TIMEOUT_MS = 30_000`) et `:250` ; flux longs lus par `getReader` dans `services/api/chat.ts:165` et `:226`, `board.ts:112`, `agents.ts:169` et `:431`, `documents.ts:278` - Correction : plafond explicite (par exemple deux réponses de fond, les suivantes en file avec mention), recette « trois flux ouverts, l'Agenda se charge encore » sous Windows, question à Ludo.
2. P2 - rfc-p125:117-123 - Les interdits du § 5 oublient la purge RGPD, la restauration et l'import : pendant une réponse de fond on peut ouvrir Paramètres et « Effacer toutes mes données », et la génération écrit sa réponse à la fin sans vérifier que la conversation existe encore ; un message personnel réapparaît après l'effacement, ou tombe dans la base restaurée - Preuve : `routers/chat.py:2815-2816` (`session.add` puis `commit`, sans relecture) ; `routers/data.py:584-588` et `services/memory_tools.py:1256-1266` : la purge ne suspend que les créations du chat, pas les générations ; aucun `PRAGMA foreign_keys` dans `src/backend/app` (l'insertion orpheline passe) ; `ConversationCanvasPrototype.tsx:1996` (bouton Paramètres sans garde) ; le verrou actuel bornait la fenêtre à une réponse regardée, P-125 l'étend à des minutes d'usage libre - Correction : purge, restauration et import refusés tant qu'une réponse court (avec « Arrêter les réponses »), ou arrêt des générations en tête de purge et de restauration côté serveur ; test moteur « purge pendant un flux : aucune ligne `messages` après ».
3. P2 - rfc-p125:50, :100 - L'inventaire oublie qu'un échec du flux remet aussi les pièces jointes dans le composeur affiché : une réponse de fond d'Orion qui échoue dépose ses documents dans Veille, et l'envoi suivant les ferait partir dans une autre conversation, voire un autre projet (cloison) ; le brouillon ne garde que le texte - Preuve : `components/chat/ChatInput.tsx:825-829` (`setAttachedFiles(...)` rend `sentFiles`) ; rfc-p125:50 ne cite que `setInput` et `saveDraft`, rfc-p125:100 « le message rejoint le brouillon d'Orion » - Correction : sur échec hors écran, les pièces jointes restent rattachées à Orion (ou sont abandonnées avec mention), jamais au composeur affiché ; test « échec d'Orion : Veille n'a aucune pièce jointe ».
4. P3 - rfc-p125:35 - Inventaire du store incomplet : `clearMessageEntities` ne cherche que dans la conversation courante, contrairement aux quatre actions citées - Preuve : `stores/chatStore.ts:280-283` (`c.id === state.currentConversationId`) ; `docs/application-map/risques.md` (constat WP-073-c9t-T1) - Correction : l'ajouter au § 2.4 et au lot 2.
5. P3 - rfc-p125:60, :105, :136 - Travaux mène déjà à la conversation : P-140 (71a9687b, 25/09 à 16:51) ouvre la conversation d'une ligne chat ou recherche approfondie, et P-142 (49629e28) rouvre l'écran quitté après un rechargement ; le § 2.5 (« ses lignes ne mènent nulle part ») et le lot 5 sont périmés, et l'étape 7 de la recette doit composer avec P-142 - Preuve : `lib/destinationDuTravail.ts` (`case 'chat': case 'deep-research':` rend `{ kind: 'conversation' }`) ; `components/traitements/TraitementsPanel.tsx:19`, `:82` - Correction : rebaser § 2.5 et le lot 5 sur HEAD.
6. P3 - rfc-p125:19, :194 - Inventaire des gardes décalé et incomplet : à 33e0955d, une garde de plus (ouverture d'une séance, P-117, ConversationCanvasPrototype.tsx:1568) et des lignes décalées (1442 devient 1451, 1480 devient 1489, 1506 devient 1515, 1666 devient 1694, 1858 devient 1887, 1873 devient 1902, pastilles 1919-1920) ; à HEAD, 13 appels contre 11 - Preuve : `git show 33e0955d:...ConversationCanvasPrototype.tsx` et grep de `blockStreamingNavigation()` à HEAD - Correction : lot 4 conduit par la fonction commune et un test qui énumère ses appelants, pas par une liste de lignes.
7. P3 - rfc-p125:111 - La mise en série locale ne regarde que le chat : Décision souveraine, Atelier local, trame et rédaction documentaires se partagent aussi le modèle local, et la mention « Partira à la fin de la réponse d'Orion » tait ces attentes - Preuve : pastilles « Board en arrière-plan » et « Atelier en arrière-plan » (ConversationCanvasPrototype.tsx:1919-1920 à 33e0955d) - Correction : la file locale consulte tous les traitements locaux en cours (Travaux les connaît), ou la mention reste générique.
8. P3 - rfc-p125:115, :157 - Le test moteur du § 4.7 ne prouvera l'absence de « database is locked » que sur une vraie base SQLite en fichier, avec deux fins de flux simultanées (à vérifier) - Preuve : CLAUDE.md du dépôt, v0.41.3 : « SQLite étant mono-écrivain (busy_timeout 5 s), une écriture concurrente pouvait échouer » - Correction : préciser le banc (fichier, deux commits finaux concurrents, une écriture utilisateur au même moment).

Questions pour Ludo :
- Combien de réponses de fond en même temps en ligne (coût, connexions), et que devient la suivante ?
- Pendant une réponse de fond, « Effacer toutes mes données » et la restauration sont-ils refusés, ou arrêtent-ils d'office les réponses ?
- Une réponse de fond qui échoue avec des pièces jointes : on les garde attachées à la conversation d'origine, ou on les abandonne en le disant ?

VERDICT : NO-GO

## Récapitulatif

| RFC | P1 | P2 | P3 | Verdict | Point bloquant principal |
|---|---|---|---|---|---|
| P-109 conversation vocale | 1 | 5 | 5 | NO-GO | WAV de dictée conservés sans limite, accord du modèle contourné |
| P-110 Word vers site PHP | 1 | 3 | 4 | NO-GO | `<?php` conservé par nh3 dans alt, title et href |
| P-121 un formulaire de devis | 0 | 3 | 3 | NO-GO | inventaire périmé (B-1411, B-1412, B-1413), arrondi non miroir |
| P-125 réponse en fond | 0 | 3 | 5 | NO-GO | connexions non plafonnées, purge RGPD pendant un flux, pièces jointes égarées |
