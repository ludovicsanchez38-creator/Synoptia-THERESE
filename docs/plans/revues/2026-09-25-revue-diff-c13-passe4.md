# Revue adverse du diff, cycle 13, passe 4 (58d217a5 contre ecd1df7c)

Relecteur : rôle Claude indépendant (da-relecteur), environ 170 000 tokens. Verdict : NO-GO, 10 constats (1 P1, 2 P2, 7 P3). Les contre-épreuves sont restées hors dépôt.

## Tri

| Constat | Bug | Suite donnée |
|---|---|---|
| 1 (P1, profil réécrit après la purge) | B-1251 | corrigé |
| 2 (P2, « Non appliqué : status ») | B-1252 | corrigé |
| 3 (P2, second contrat du projet réutilisé) | B-1253 | corrigé |
| 4 (P3, import pendant l'arrêt) | B-1254 | rejeté : constat antérieur à B-1249, l'arrêt précède désormais les suppressions |
| 5 (P3, test B-1234 sans preuve) | B-1255 | corrigé (test de durée de la purge) |
| 6 (P3, délai client de la purge) | B-1250 | déjà corrigé |
| 7 (P3, budget inf à l'import de fichier) | B-1256 | corrigé ; budget négatif laissé à B-1244 (règle à trancher) |
| 8 (P3, /projet budget illisible) | B-1257 | corrigé |
| 9 (P3, test B-1239 sur un chemin impossible) | B-1258 | corrigé, et une seule trace journalisée |
| 10 (P3, test B-1235 en OSError) | B-1259 | corrigé (CancelledError, archive vérifiée) |

## Rapport brut

1. P1 - src/backend/app/routers/data.py:664-667 (et src/backend/app/services/user_profile.py:277-283) - Préexistant, même classe que B-1234, laissé hors du correctif : la purge n'arrête que les indexations de fiches. L'indexation de fond du profil passe par le même `asyncio.to_thread` et écrit `owner_profile` (nom, entreprise, rôle, courriel, localisation) dans la collection recréée APRÈS la réponse 200 de « Effacer toutes mes données ». Le commentaire data.py:664 « aucune indexation de fond ne doit réécrire après la purge » promet ce que le code ne fait pas - contre-épreuve hors dépôt (scratchpad/p4/test_scratch_p4b.py : POST /api/config/profile, puis DELETE /api/data/all, avec un vecteur de 1,5 s calculé dans un fil) : `{"status_purge": 200, "ecritures_apres_la_reponse": ["owner_profile"], "delai_s": 1.16}`. delete_user_profile a déjà la bonne garde (user_profile.py:454 `_GENERATION_PROFIL += 1`, puis :459 `async with _VERROU_INDEXATION`), alors que delete_all_data se contente de `set_cached_profile(None)` (data.py:657) - dans delete_all_data et restore_backup, avant la purge Qdrant, avancer `_GENERATION_PROFIL` puis prendre `_VERROU_INDEXATION` (patron de user_profile.py:451-462), ou faire de l'arrêt un arrêt de TOUTES les indexations de fond. Ajouter le test du profil en vol pendant la purge.
2. P2 - src/backend/app/services/slash_commands.py:204 (et src/backend/app/services/memory_tools.py:699) - Régression de B-1238 : `/projet Nom` sur un projet existant répond toujours « Non appliqué : status. », même quand aucune option n'a été donnée. `_do_projet` injecte `"status": kw.get("status", "active")`, et le nouveau filtre compte comme demandée toute valeur non vide. Le même faux signal part vers le modèle quand l'outil reçoit `status="active"` (le schéma memory_tools.py:115 dit « defaut: active ») ou une valeur identique à celle du projet. La clé anglaise « status » est en plus affichée telle quelle - mesure hors dépôt (scratchpad/p4/test_scratch_p4.py, cas C) : première commande « Projet **Site Web** créé en mémoire. », seconde « Projet **Site Web** déjà en mémoire, je le réutilise (pas de doublon). Non appliqué : status. » - passer `kw.get("status")` (None prend déjà « active » par défaut, sans passer par `ecarte`), ne garder dans `ignore` que les valeurs qui diffèrent de celles du projet existant, et afficher des libellés français (statut, budget, description). Ajouter le cas « /projet deux fois, sans option ».
3. P2 - src/backend/app/services/memory_tools.py:689-703 - B-1238 crée un second contrat pour le même cas. Un projet réutilisé renvoie la clé `ignore` avec `success: True`, alors qu'un contact réutilisé renvoie `champs_ignores` avec `success: not ignores` (memory_tools.py:531-538). Le garde déterministe execution_truth.py:75 ne lit que `champs_ignores` : le « Récap réel » affiché sous la réponse ne dit pas que rien n'a été écrit. Le but de B-1238 (« le modèle ne doit pas l'annoncer comme fait ») repose donc sur le seul modèle - mesure hors dépôt (cas D) : l'outil renvoie `{"success": true, "already_existed": true, "ignore": ["status", "budget", "description"]}` et `summarize_executions` donne « Récap réel : 1 déjà existant(s). » - reprendre le contrat du contact (`champs_ignores`, `success` faux dès qu'une valeur est ignorée, message « Ne dis pas que le projet a été mis à jour »), ou faire lire la clé par execution_truth. Tester le récap.
4. P3 - src/backend/app/routers/memory.py:139 et :178-184 - Concurrence ouverte par B-1234 : l'événement reste armé pendant tout le `gather`, soit la durée d'un vecteur (jusqu'à 19 s selon memory.py:128). La purge n'est pas sous mode maintenance. Un import de contacts qui arrive dans cette fenêtre crée sa tâche, qui lit `is_set()` à sa première itération et rend la main : fiches créées après le `delete(Contact)` de la purge, jamais indexées, rien au journal (symptôme de B-1180). Avec l'ancienne annulation, la fenêtre était quasi nulle - lecture : memory.py:139 `if _ARRET_DES_INDEXATIONS.is_set(): return` sans trace ; data.py:648 commit des suppressions, puis :667 attente. Non mesuré, à vérifier - n'arrêter que les tâches présentes au moment de l'appel (jeton d'arrêt par tâche, ou numéro de génération relevé à la création comme user_profile.py:323), et journaliser l'abandon.
5. P3 - tests/test_b1222_indexation_de_fond_sans_course.py:67 - Le test de B-1234 ne prouve pas l'arrêt entre deux fiches : il reste vert sans `_ARRET_DES_INDEXATIONS.set()`. Sur la purge, l'événement est redondant. Les fiches y sont supprimées et commitées (data.py:648) avant l'attente (:667), et la relecture B-1222 saute donc les suivantes. L'événement n'agit que dans la restauration (data.py:1495, base intacte), qu'aucun test ne couvre avec plusieurs fiches en cours d'indexation - sabotage hors dépôt (cas B, arrêt sans `set()`) : `{"status": 200, "duree_purge_s": 0.52, "ecritures_apres": 0, "assertion_du_test_B1234_verte": true}` - ajouter un test de restauration avec un carnet de N fiches et un fil lent : la restauration attend au plus un vecteur, et aucune écriture ne suit sa réponse.
6. P3 - src/frontend/src/services/api/data.ts:123-124 - B-1234 fait attendre à la purge la fin du vecteur en cours de calcul (jusqu'à 19 s, memory.py:128). `deleteAllData` garde pourtant le délai client par défaut de 30 s (core.ts:120), alors que `restoreBackup` a `timeoutMs: null` (data.ts:115). Si le délai expire, handleDeleteAll sort en erreur et `purgeLocalPersistence` (PrivacyTab.tsx:197) ne tourne pas : le serveur est purgé, le stockage local reste intact (scénario BUG-153). La marge tombe à une dizaine de secondes sur une machine déjà dite modeste. Dépassement non mesuré, à vérifier - mesure hors dépôt (cas A) : la durée de la purge suit le temps restant du vecteur, soit 5,72 s pour un vecteur de 6 s lancé 0,3 s avant, et 0,52 s pour un vecteur de 0,8 s (cas B) - `timeoutMs: null` sur deleteAllData. Ou bien calculer le vecteur dans le fil et écrire depuis la boucle après contrôle de l'arrêt, pour pouvoir de nouveau annuler sans attendre.
7. P3 - src/backend/app/services/crm_import.py:914-915 - B-1238 étend à « inf » et « NaN » l'effacement silencieux d'un budget existant. Sur un projet mis à jour, une cellule de budget non vide mais non finie écrit None par-dessus l'ancien budget, sans erreur au rapport. Le tableur, lui, ne remplace rien (crm_utils.py:453 `if budget is not None and budget_raw`). Un budget négatif, refusé par la règle B-1219 (memory_tools.py:261), passe toujours à l'import de fichier - mesure hors dépôt (cas E, projet à 1200, CSV `p-e,Chantier,inf`) : `{"updated": 1, "errors": [], "budget_apres": null}` - n'affecter le budget que si la valeur lue n'est pas None, signaler la cellule écartée dans `result.errors`, et appliquer la règle de `_budget_de_projet` (fini et positif).
8. P3 - src/backend/app/services/slash_commands.py:195-199 - Le commit B-1238 dit que /projet annonce ce qu'il écarte. Pourtant, un budget illisible (`budget=beaucoup`) est jeté par `_do_projet` avant l'outil : l'outil reçoit None et n'écarte rien, la réponse reste « Projet **X** créé en mémoire. ». Le test B-1238 ne couvre que le statut - lecture : `except ValueError: budget = None` puis `"budget": budget`. La condition memory_tools.py `arguments.get("budget") not in (None, "") and budget_retenu is None` ne peut donc jamais se déclencher sur ce chemin - transmettre la chaîne brute à l'outil (qui sait déjà l'écarter et le dire), et ajouter le cas au test.
9. P3 - tests/test_b1222_indexation_de_fond_sans_course.py:101-122 - Le test B-1239 fait lever `_embed_contact`, qui ne lève jamais dans le vrai code (memory.py:212-213 `except Exception as e:` puis `logger.warning`). Le cas « calcul du vecteur » de sa docstring n'existe pas. Seule la lecture en base de `_indexer_une_fiche` peut lever, et le test ne l'exerce pas. Effet de bord du correctif : une panne de base persistante produit maintenant une trace complète pour chaque fiche restante (memory.py:146, `exc_info=True`) - lecture : test :116 `raise RuntimeError("panne du modèle d'embedding")` et :119 `monkeypatch.setattr(memoire, "_embed_contact", embed)` - faire lever `session.get` (ou `get_session_context`) sur la première fiche, et ne journaliser qu'une fois un échec qui se répète.
10. P3 - tests/test_b1179_rollback_en_echec_dit_la_verite.py:168 - Le test B-1235 simule une `OSError` que `arreter_les_indexations_de_fiches` ne peut pas lever (`set()` puis `gather(..., return_exceptions=True)`, memory.py:178-182). Le chemin réel est l'annulation (déconnexion, arrêt), soit `CancelledError`, que le test n'exerce pas. Le `finally` couvre les deux cas, d'où la gravité P3 - lecture : `raise OSError("interruption simulée")` - lever `asyncio.CancelledError` dans le faux, et vérifier aussi l'effacement de l'archive déchiffrée (data.py:1569-1570).
VERDICT : NO-GO
