# Revue adverse du diff, cycle 13, passe 5 (5dc3a0c6 contre 58d217a5)

Relecteur : rôle Claude indépendant (da-relecteur), environ 240 000 tokens. Verdict : NO-GO, 12 constats (1 P1, 3 P2, 8 P3), dont six régressions des correctifs de la passe 4. Contre-épreuves hors dépôt.

## Tri

| Constat | Bug | Suite donnée |
|---|---|---|
| 1 (P1, archive en clair après annulation de begin) | B-1269 | corrigé |
| 2 (P2, gather annule les gestes protégés) | B-1270 | corrigé (asyncio.wait, aussi pour les fiches) |
| 3 (P2, « Non appliqué : email » à l'identique) | B-1271 | corrigé (comparaison avec la fiche) |
| 4 (P2, étiquettes non textuelles à l'import CRM) | B-1272 | corrigé (règle unique etiquettes_lues) |
| 5 (P3, budget illisible sur projet sans budget) | B-1273 | corrigé |
| 6 (P3, budget numérique 0 à l'import) | B-1274 | corrigé |
| 7 (P3, étiquettes en texte jetées) | B-1275 | corrigé (même règle) |
| 8 (P3, création lancée pendant les attentes) | B-1276 | corrigé (créations du chat suspendues pendant la purge) |
| 9 (P3, attentes non bornées) | B-1277 | corrigé (plafond 120 s, 503 sans rien supprimer) |
| 10 (P3, import signalé en échec) | B-1278 | corrigé (message qui compte les lignes) |
| 11 (P3, dénominateur du décompte) | B-1279 | corrigé |
| 12 (P3, test de B-1249 en OSError) | B-1280 | corrigé (CancelledError, deux attentes) |

## Rapport brut

1. P1 - src/backend/app/routers/data.py:1488-1493 (et src/backend/app/services/maintenance.py:57-65) - B-1263 rend bien le verrou quand la restauration est annulée pendant `begin()`, mais sur ce même chemin l'archive déchiffrée en clair `.{nom}.restore.tar.gz` (écrite data.py:1447-1452, AVANT `begin()`) n'est jamais effacée. `except RuntimeError` ne voit pas `CancelledError`, et le `finally` qui fait `decrypted_temp.unlink` (data.py:1575-1579) n'englobe pas `begin()`. US-003 (« jamais de clair persistant » ; l'archive contient la clé de chiffrement) est violée sur le scénario même que B-1263 déclare réel (arrêt de l'application, déconnexion) ; après un arrêt, le fichier en clair survit au processus mort. Q6 : `except BaseException` ne peut pas libérer le verrou d'une AUTRE restauration, `begin()` lève `RuntimeError` avant de poser `_active` si le verrou est déjà tenu (maintenance.py:53-54), seule la restauration qui l'a posé entre dans le `try` (lecture) - mesuré (scratchpad/p5/test_zz_scratch_p5.py, cas A : sauvegarde chiffrée, une requête suivie en vol, appel réel de `restore_backup`, annulation pendant l'attente de `begin()`) : `{"actif_pendant_begin": true, "issue": "CancelledError", "maintenance_active_apres": false, "archive_en_clair_apres_annulation": [".../backups/.therese_backup_20260925_012934.restore.tar.gz"]}` - placer `begin()` dans le `try` dont le `finally` efface l'archive (en n'appelant `end()` que si `begin()` a abouti), ou `except BaseException:` qui efface `decrypted_temp` puis relance ; tester l'annulation pendant `begin()` au niveau de la route, avec l'assertion US-003 déjà écrite pour B-1259.
2. P2 - src/backend/app/services/memory_tools.py:1188-1190 - B-1260 attend les gestes par `asyncio.gather(*gestes, return_exceptions=True)`. Or un `gather` annulé annule ses enfants : une purge ou une restauration interrompue (arrêt de l'application ; ou déconnexion, database.py:1202-1205 rappelant que « la tâche est annulée » quand le client abandonne un flux) annule le geste que `_proteger_le_geste` (:1197) protégeait par `shield` pour qu'il finisse entier. La requête de chat qui l'attendait reçoit une `CancelledError` que personne n'a demandée. La ligne SQL est abandonnée, mais le fil `to_thread` écrit quand même le vecteur : le vecteur orphelin que la passe 3 (P3-3) avait éliminé revient. Q1 : pas d'interblocage, la session injectée n'ouvre aucune transaction avant `log_activity` (database.py:1189-1197), et un geste en échec ou annulé ne fait pas échouer la purge grâce à `return_exceptions=True` (lecture) - mesuré (cas B : `execute_create_contact` avec un vecteur de 1 s dans un fil, `attendre_les_gestes_de_creation()` lancée puis annulée) : `{"geste_annule_par_la_purge_annulee": [true], "issue_requete_chat": "CancelledError dans la requête de chat (que personne n'a annulée)", "ligne_contact_en_base": [], "vecteur_ecrit_apres_l_annulation": ["35f88154-b73e-43f1-b78f-8f652ddb3c11"]}` - `await asyncio.wait(gestes)` (n'annule pas ses futures) ou `gather(*(asyncio.shield(g) for g in gestes), return_exceptions=True)`. Le motif préexiste dans `arreter_les_indexations_de_fiches` (memory.py:196), à traiter en même temps.
3. P2 - src/backend/app/services/memory_tools.py:533-541 et src/backend/app/services/slash_commands.py:178-181 - B-1261 fait afficher par `/contact` les `champs_ignores` du contact réutilisé. Mais ce contrat se calcule sur la simple présence d'une valeur (`if arguments.get(champ)`), sans comparaison avec la fiche. Retaper `/contact Marie Exemple email=marie@exemple.fr` (fiche retrouvée PAR cet email) répond désormais à l'utilisateur « Non appliqué : email. ». Le contrat de l'outil (`success` faux, `champs_ignores`) et le Récap réel le disaient déjà au modèle depuis la 0.56 (préexistant) ; ce que B-1261 introduit, c'est de le dire à l'écran. C'est le faux signal que B-1252 vient de retirer de `/projet`, réintroduit côté contact. Q3 : `success: false` sur un projet réutilisé ne change aucune branche de code (lecture) : execution_truth.py:65 teste `already_existed` avant `success`, chat.py:3224 enveloppe tout résultat mémoire en `success=True`, `enforce_create_cap` compte les appels et ToolConfirmationCard.tsx:125 affiche `result` brut ; seul le modèle lit le champ - mesuré (cas C, une session neuve par commande, conversation `conv-p5`) : « Contact **Marie Exemple** déjà en mémoire, je le réutilise (pas de doublon). Non appliqué : email. », y compris pour `MARIE@exemple.fr`, et « ... Non appliqué : téléphone. » pour `tel=0612345678` identique ; outil : `{"success": false, "champs_ignores": ["email"]}` ; récap : « Récap réel : 1 déjà existant(s), rien n'a été écrit sur 1 fiche(s) existante(s). » - calculer les champs du contact comme `_valeurs_non_appliquees` (email en casse pliée, téléphone normalisé, notes, adresse et entreprise comparées à la fiche). Ajouter au test B-1261 le cas « /contact retapé à l'identique ».
4. P2 - src/backend/app/services/crm_import.py:460-462 - Préexistant, hors diff : B-1264 ne ferme qu'une des deux portes d'import de contacts. L'import de fichier CRM (`POST /api/crm/import/contacts`, format JSON) stocke toujours tel quel (`json.dumps(value)`) une liste d'étiquettes qui ne sont pas du texte. La liste des contacts tombe alors exactement comme le décrit le commit de B-1264. Les deux routes n'ont aucun appelant dans l'interface (grep `src/frontend`), d'où P2 - mesuré (cas G) : import de `[{"first_name": "Crm", "last_name": "Nombre", "tags": [1, 2]}]` → 200 « Import termine: 1 crees », puis GET /api/memory/contacts → `ValidationError: 2 validation errors for ContactResponse tags.0 Input should be a valid string` (memory.py:598, soit une 500 en production) - une règle commune pour les deux portes (`_etiquettes_importees` réutilisée par `_parse_value(..., "tags")`), qui signale les valeurs écartées. Ajouter ce cas au test B-1264.
5. P3 - src/backend/app/services/memory_tools.py:273-275 - `_valeurs_non_appliquees` compare `_budget_de_projet(budget) != projet.budget`. Un budget illisible donne None ; sur un projet sans budget, None == None, et rien n'est signalé. Cela contredit la docstring (:267-268 « une valeur illisible compte comme différente ») et le but de B-1257, sur le chemin du projet réutilisé - mesuré (cas D) : `_valeurs_non_appliquees(SimpleNamespace(status="active", budget=None, description=None), {"budget": "abc"}) == []`, idem pour `-5`. `/projet Chantier budget=beaucoup` répond la première fois « ... (budget « beaucoup » illisible, non enregistré) », la seconde « Projet **Chantier** déjà en mémoire, je le réutilise (pas de doublon). », sans un mot sur le budget - compter « budget » comme perdu dès que `_budget_de_projet(budget) is None` (de même pour le statut avec `_statut_de_projet(statut) is None`), et ne comparer qu'ensuite.
6. P3 - src/backend/app/services/crm_import.py:929-930 et :976 - B-1256 lit la cellule par `str(mapped.get("budget") or "")`. Un 0 numérique (cellule XLSX, nombre JSON) devient "", puis `budget_lu = None`, sans erreur au rapport. À la création, le projet reçoit None là où l'ancien code (`_parse_value(0, "float")`) enregistrait 0.0 : c'est une régression. La mise à jour est inchangée, car l'ancien `if mapped.get("budget")` sautait déjà 0. Q5 : « 0 » et « 0,5 » en texte sont bien lus, le nombre 0 ne l'est pas - mesuré (cas E, classeur « Projets » et JSON) : `{"Zero numerique": null, "Zero JSON": null, "Zero texte": 0.0, "Demi texte": 0.5, "Zero virgule cinq numerique": 0.5}`, `errors: []` - `cellule_budget = "" if mapped.get("budget") is None else str(mapped["budget"]).strip()`.
7. P3 - src/backend/app/routers/data.py:1842-1846 - `_etiquettes_importees` jette sans trace une valeur `tags` en texte. Or `_export_row` (data.py:94-101) exporte à dessein tel quel un `tags` historique non JSON (« pourra être examinée/importée ») : l'aller-retour le perd en silence. L'import CRM, lui, découpe ce même texte sur les virgules (crm_import.py:463-465). La réponse reste `{"success": true, "imported": 2}` - mesuré (cas G) : contact « Legacy » importé avec `"tags": "vip,client"`, relu avec `"tags": null` - découper une chaîne comme l'import CRM, et compter dans la réponse les étiquettes écartées.
8. P3 - src/backend/app/services/memory_tools.py:1188 (et src/backend/app/routers/data.py:591-593) - `list(_gestes_en_cours)` n'est qu'un instantané, et la purge ne passe pas en mode maintenance. Un geste de chat lancé pendant les attentes (geste en vol, puis fiche en vol, puis profil) n'est pas attendu : on retrouve la course de B-1260 (verrou d'écriture SQLite, fiche qui survit). En restauration, `begin()` a déjà vidé les requêtes suivies et ce trou n'existe pas. Le test B-1260 est, lui, un vrai garde : saboté sans attente, il échoue (mesuré, cas H : purge 500 en 5,44 s, une fiche restante, un vecteur écrit après la réponse) - mesuré au niveau de la fonction (cas B2) : un second `execute_create_contact`, lancé 0,2 s après le début de l'attente, écrit son vecteur 0,7 s APRÈS la fin de `attendre_les_gestes_de_creation()`. Effet sur la purge complète non mesuré - boucler tant que `_gestes_en_cours` n'est pas vide, ou refuser les créations du chat pendant la purge (drapeau lu par `_proteger_le_geste`).
9. P3 - src/frontend/src/services/api/data.ts:126 (et src/backend/app/routers/data.py:591-593, src/backend/app/services/user_profile.py:363-364) - B-1250 retire tout délai côté écran, alors que le serveur n'en a aucun sur ses trois attentes (`gather` et `async with` nus). Or le calcul d'un vecteur n'est pas borné : le modèle `SentenceTransformer` se charge paresseusement (embeddings.py:33-49) et peut se télécharger au premier appel. Le commentaire « jusqu'à ~19 s » (data.py:585) ne tient plus, les trois attentes se suivant. Q2 : le verrou `_VERROU_INDEXATION` ne peut pas lever en production, uvicorn n'a qu'une boucle et aucun appelant du profil ne tourne hors d'elle (config.py:1120, :1153, :1207 ; le `asyncio.run` de mcp.py:655 ne touche pas ce verrou) ; le verrou neuf posé par le test B-1251 est un artefact de la boucle du TestClient (lecture) - lecture, non mesuré, à vérifier - borner l'ensemble des attentes (`asyncio.wait_for`, par exemple 60 s) et répondre 503 « réessaie » sans rien supprimer : l'attente précède désormais toute suppression.
10. P3 - src/backend/app/services/crm_import.py:699-708, :728-729, :820-821 et :830 - B-1262 et B-1256 ajoutent à `result.errors` des lignes pourtant créées ou mises à jour. `result.success` passe alors à faux, et le message devient « Import avec erreurs: 1 erreurs sur N lignes » sans dire que les lignes sont enregistrées. Q4 : l'aller-retour export puis import garde l'étape. L'export écrit la clé interne en minuscules (crm_export.py:54, `_format_value` :145) et l'import passe `.lower()`. Aucun alias français n'existe : « Découverte » est désormais refusée et signalée - mesuré pour l'aller-retour (cas F) : `{"stage_apres_import": "proposition", "errors": [], "updated": 1, "success": true}` ; message lu, non mesuré - ranger ces signalements dans une liste d'avertissements distincte de `errors`, ou compter dans le message les lignes enregistrées.
11. P3 - src/backend/app/routers/memory.py:156-160 - B-1258 : le décompte final « %d fiches sur %d en échec » utilise `len(identifiants)`, même quand l'arrêt demandé par la purge a fait sortir la boucle avant la fin. Le dénominateur compte alors des fiches jamais tentées - lecture - compter les fiches tentées.
12. P3 - tests/test_b1222_indexation_de_fond_sans_course.py:186-190 - Le test de B-1249 simule encore une `OSError` que `arreter_les_indexations_de_fiches` ne peut pas lever : c'est le défaut que B-1259 vient de corriger pour la restauration. Il ne couvre pas non plus la nouvelle première attente, `attendre_les_gestes_de_creation` - lecture : `raise OSError("interruption simulée pendant l'attente")` - lever `asyncio.CancelledError`, et ajouter le même cas sur `attendre_les_gestes_de_creation`.
VERDICT : NO-GO
Estimation : environ 240 000 tokens consommés.
