1. P1 - src/backend/app/routers/memory.py:123-141 (appelée par memory.py:750 et crm.py:885) - B-1204 introduit une course : l'indexation de fond travaille sur l'instantané des fiches de l'import et rien ne l'arrête. Une fiche supprimée, ou une purge « Effacer toutes mes données », voit ses vecteurs (nom, courriel, société en payload) réécrits dans Qdrant APRÈS la réponse 200. Une fiche modifiée pendant la fenêtre est réindexée avec son ancien état. À 19 s par vecteur (BUG-172), un carnet de 50 fiches ouvre une fenêtre d'environ 16 min - contre-épreuve hors dépôt (scratchpad/pt/test_scratch_b1204_course.py, 3 fiches à 0,8 s) : DELETE /api/memory/contacts/{id} = 200, puis `('add', 'adfd7467', +2.4 s)` sur la fiche supprimée ; DELETE /api/data/all = 200, 0 fiche restante, puis « 3 ajouts après la purge ». `_INDEXATIONS_DE_FICHES` n'est lu nulle part ailleurs (grep) - dans `_indexer`, relire chaque fiche par id dans une session neuve juste avant `_embed_contact` et la sauter si elle n'existe plus. Dans delete_all_data (data.py:664) et restore_backup, annuler puis attendre `_INDEXATIONS_DE_FICHES` AVANT la purge Qdrant. Ajouter au dépôt le test de course (suppression et purge).
2. P2 - src/frontend/src/components/onboarding/LLMStep.sondeLente.b1211.test.tsx:64 - Le test de garde est un faux vert, et B-1211 a été rejeté sur sa foi alors que le défaut est réel. La radio est un `<button role="radio">` : `(radio as HTMLInputElement).checked` vaut toujours undefined, donc le test reclique TOUJOURS sur Ollama après la revérification, ce qui relance la sélection et masque le défaut - contre-épreuve hors dépôt (scratchpad/vt/b1211.scratch.test.tsx, même scénario sans ce reclic) : `{"ariaChecked":"true","tag":"BUTTON","modele":"qwen3:8b","continuerActif":false}`, soit exactement « le menu montre un modèle Ollama mais Continuer reste grisé ». Cause : handleSelectProvider (LLMStep.tsx:258-266) garde le modèle précédent quand `ollamaModels` est vide (catalogueModeles.ts:320 `models: []`), et loadState (LLMStep.tsx:119-158) ne resynchronise jamais `selectedModel` - rouvrir B-1211. Tester `radio.getAttribute('aria-checked')` sans reclic. Au succès de loadState, si le fournisseur est Ollama et que le modèle retenu n'est pas dans la liste chargée, appliquer la règle de handleSelectProvider (premier modèle local capable, sinon '').
3. P2 - src/frontend/src/components/settings/PrivacyTab.tsx:285 - Le nouveau texte de B-1217 donne un critère faux : « un modèle Ollama Cloud (nom en « :cloud ») ». La détection reconnaît aussi « -cloud », qui est la forme des modèles cloud les plus courants. Une personne qui utilise « qwen3-coder:480b-cloud » applique le critère affiché et croit rester en local - lib/ollamaCloud.ts:8 `return n.endsWith(':cloud') || n.endsWith('-cloud');`, et son propre exemple ollamaCloud.ts:2 « gpt-oss:120b-cloud ». Le test B-1217 (PrivacyTab.fournisseursNommes.b1175.test.tsx) vérifie seulement que « Ollama Cloud » apparaît - écrire « nom finissant par :cloud ou -cloud », et faire vérifier les deux formes par le test.
4. P3 - src/backend/app/routers/data.py:675 - B-1199 fait dépendre la recréation de la collection d'un appel qui lève sur une collection absente. `client.delete` lève ValueError quand la collection n'existe pas, alors que l'ancien `delete_collection` ne levait pas. L'exception saute alors `_ensure_collection()` (data.py:689), et le service reste sur une collection absente jusqu'au redémarrage : c'est le symptôme de B-1130 - .venv qdrant_client/local/qdrant_local.py:604 `collection = self._get_collection(collection_name)`, qui fait :183 `raise ValueError(f"Collection {collection_name} not found")`, contre :766 `self.collections.pop(collection_name, None)` ; aucun test ne couvre ce cas - isoler `delete` dans son propre try, ou appeler `_ensure_collection()` dans un finally.
5. P3 - src/backend/app/services/memory_tools.py:244 - `_statut_de_projet` remplace en silence tout statut inconnu par « active » et ignore la table française que le tableur applique déjà. `/projet Chantier statut=terminé` crée donc un projet actif, et répond « Projet **Chantier** créé en mémoire. ». Un budget illisible est de même abandonné sans le dire - memory_tools.py:244 `... in VALID_PROJECT_STATUSES else "active"` contre crm_utils.py:31-43 `PROJECT_STATUS_MAP` (`"termine": "completed"`, `"en_pause": "on_hold"`) ; slash_commands.py:100 et :204 passent le statut brut ; memory_tools.py:763-767 ne renvoie ni le statut ni le budget retenus - passer la valeur par `cle_de_statut` puis `PROJECT_STATUS_MAP`, et signaler dans le résultat de l'outil toute valeur remplacée ou écartée.
6. P3 - src/frontend/src/components/settings/EnvVarModal.tsx:264-265 - B-1218 fait porter l'état deux fois au bouton œil : son nom change (« Afficher X » puis « Masquer X ») ET il porte `aria-pressed`. Un lecteur d'écran annonce « Masquer X, bouton bascule, enfoncé », ce qui est ambigu - la ligne 264 fait varier le nom selon `show`, la ligne 265 ajoute `aria-pressed={show}`. Or le guide ARIA des boutons bascule demande un nom fixe quand `aria-pressed` est présent - garder un nom fixe (« Afficher la valeur de X ») avec aria-pressed, ou retirer aria-pressed.
7. P3 - src/frontend/src/components/board/BoardPanel.tsx:151 - Le motif ajouté par B-1215 n'est jamais remis à zéro en cas d'échec. Après une sonde « seulement des modèles Cloud », une revérification qui échoue laisse le titre « Aucun modèle local installé… » alors que l'état est inconnu. Et un Ollama qui tourne sans aucun modèle affiche toujours « Ollama non disponible » - `.catch(() => setOllamaAvailable(false));` ne touche pas `setSeulementDesModelesCloud`, et la ligne 148 exige `(statut.models ?? []).length > 0` - remettre le motif à faux dans le catch, et donner un motif pour « Ollama disponible, aucun modèle local ».
8. P3 - src/frontend/src/components/settings/ToolsPanel.tsx:605 - La promesse de B-1214 (« tapé tel qu'affiché ») n'est pas tenue pour « CRM & Ventes » : la recherche compare la clé « crm », pas le libellé affiché - ToolsPanel.tsx:616 `crm: 'CRM & Ventes'`. Taper « CRM & Ventes » ne trouve rien. Taper « Ventes » ne trouve que Pipedrive, grâce à sa description (routers/mcp.py:449 « CRM ventes »), et laisse HubSpot de côté - replier aussi `CATEGORY_LABELS[cat]` dans le filtre, et ajouter le cas au test.
9. P3 - src/frontend/src/components/settings/PrivacyTab.tsx:219 et :687 - À vérifier : B-1209 bloque aussi le geste protecteur. Quand la lecture des réglages échoue alors que l'écriture passerait encore (lecture expirée, par exemple), on ne peut plus DÉSACTIVER une purge automatique qui anonymise des fiches. L'interrupteur et le curseur restent manipulables, et « Enregistrer » est grisé sans explication à côté. Cas non mesuré : si la lecture échoue, l'écriture échoue probablement aussi - `if (purgeIndisponible) return;` et `disabled={saving || purgeIndisponible}` ; l'interrupteur (PrivacyTab.tsx:639) n'est pas désactivé - griser aussi les commandes, avec un message à côté du bouton, ou n'autoriser que l'envoi explicite de la désactivation.
10. P3 - tests/test_b1185_corbeille_survit_pas_a_la_restauration.py:33 - Forcer `sys.platform` à « darwin » retire de la CI Linux la couverture de B-1185 (sauvegarde, suppression, restauration puis purge) sur le chemin Linux (freedesktop). Ce chemin écrit dans le même index que celui que la restauration écrase. La couverture ne se retrouve pas ailleurs : tests/test_p102_corbeille_freedesktop.py:64 teste la purge Linux sans restauration intermédiaire - user_commands.py:256 (branche Linux), puis :313-314 `self._noter_depot_en_corbeille(fichiers / nom)` et `(info)` ; le test ne passe plus que par la branche ~/.Trash - paramétrer darwin et linux, avec XDG_DATA_HOME dans tmp_path.
11. P3 - src/backend/app/services/crm_utils.py:550 - B-1212 annonce « même règle que la route des tâches » mais n'en applique que la moitié. Une tâche passée à « done » par le tableur, avec la cellule CompletedAt vide, reste sans date de fin. Une tâche neuve (branche else) garde, elle, une date de fin même si elle n'est pas « done » - tasks.py:238-239 `if request.status == "done" and not task.completed_at: task.completed_at = datetime.now(UTC)`, absent de upsert_task - ajouter la branche « done sans date » et appliquer la règle à la création.
12. P3 - src/backend/app/routers/memory.py:139 - Rien ne garde l'indexation de fond de B-1204. Une fermeture de l'application pendant la fenêtre perd les fiches non encore indexées, sans reprise au démarrage (B-1206 différé), et l'écran a déjà répondu « N contacts créés » - grep : `_INDEXATIONS_DE_FICHES` n'est référencé qu'à memory.py:120, :140 et :141, ni attendu ni signalé dans le lifespan de main.py - attendre ou journaliser les tâches restantes à l'arrêt, ou marquer les fiches à indexer pour une reprise au démarrage.
13. P3 - src/frontend/src/lib/libellesFournisseurs.ts:34 - B-1216 nomme l'accord « Nano Banana 2 », alors que l'écran où cet accord est donné affiche « Nano Banana » - ImagesWorkspaceCanvas.tsx:34 `{ id: 'nanobanan-pro', label: 'Nano Banana', ... }`, accord posé à :247 `grantCloudConsent('images', snapshot.request.provider, ...)` - lire les libellés depuis une seule table, partagée avec ImagesWorkspaceCanvas.
VERDICT : NO-GO
