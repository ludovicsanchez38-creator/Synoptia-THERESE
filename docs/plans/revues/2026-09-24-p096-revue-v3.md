| Constat V2 | Statut | Où la V3 le traite / ce qui reste |
|---|---|---|
| 1 (P1) | traité | l.21-24, l.90-92, l.100-102 (en-tête avec le modèle non résolu, `reprendre` sans charge) ; la reprise ne revalide rien (n° 3) |
| 2 | partiel | l.114-121 ; `_run_imap_operation` n'accepte pas d'exécuteur, et un fil unique par lot bloque la « session neuve » derrière le dépôt expiré (n° 5) |
| 3 | partiel | l.122-129 ; changer `_request` touche les 15 appels Gmail, la raison du 403 est perdue (n° 7) |
| 4 | traité | l.49-58 |
| 5 | traité | l.191-197 ; restes : masque « côté moteur », texte du modèle et Variables, « Reprendre » en démo (n° 9) |
| 6 | partiel | l.185-190 ; deux fermetures sans question non citées : l'effet du store et le redimensionnement (n° 8) |
| 7 | traité | l.86-89, l.107-109 |
| 8 | traité | l.147-152 ; `a7b8c9d0e1f2` est bien la tête (alembic/versions/a7b8c9d0e1f2_planning_pert_gantt.py:10, database.py:617), aucun autre design du 24/09 ne touche Alembic |
| 9 | traité | l.8-14 et arbitrages l.50-66 |
| 10 | traité | l.139-141 ; la fonction tombe dans le `try` fail-open existant (n° 14) |
| 11 | traité | l.76-80 (`variables_revision` existe, variables_service.py:346-353) ; « choix de signature » reste (n° 16) |
| 12 | traité | l.64-69 ; la borne des 60 000 caractères porte sur le texte résolu (n° 11) |
| 13 | traité | l.122-134 |
| 14 | traité | l.103-105, l.180-184 ; `entity_id` absent du type `Traitement` de l'écran (n° 15) |
| 15 | partiel | l.142-146 ; « fin du lot », `purge_apres` et purge « avec les travaux » ne tiennent pas ensemble (n° 4) |
| 16 | traité | l.70-75 |
| 17 | traité | l.25-26 |
| 18 | partiel | l.135-138 ; forme écrite, usage absent (n° 6) |
| 19 | traité | l.35-38 |
| 20 | traité | l.176-178 |
| 21 | traité | l.117-118, l.178-179 ; `append(..., flag_set=...)` existe (imap_tools/mailbox.py:307-310) |
| 22 | traité | l.39-45 ; une imprécision reste (n° 17) |
| 23 | traité | l.8-9 (1 P1 et 22 P2 ou P3, exact) |

1. P1 - docs/plans/2026-09-24-p096-brouillons-en-lot.md:142-146 - Aucune règle pour un lot en cours pendant une restauration : le travail continue d'écrire (réservation, statut, progression) pendant que `therese.db` est remplacé, puis « Reprendre » redépose des brouillons qui existent déjà - preuve : src/backend/app/services/maintenance.py:30-58, le verrou ne compte que les requêtes HTTP (`admit`, `_active_requests`), jamais les travaux ; src/backend/app/routers/data.py:1450-1453, « Aucune session n'est injectée à cette route : tous les appels API admis avant le verrou sont terminés », puis `close_db()` avant l'extraction (data.py:1464-1474, `_safe_extractall(tar, data_dir)`) ; src/backend/app/models/database.py:1174-1186, `close_db` dispose le moteur mais `AsyncSessionLocal` (l.1093-1097) reste lié à lui, et `get_session_context` (l.1227-1241) rouvre une connexion à la demande : le lot écrit donc dans la base en cours d'extraction ; aucune annulation de travail dans data.py (les seules occurrences d'« annul » sont des messages d'erreur, l.1035, 1085, 1180, 1455) ; une sauvegarde prise pendant un lot garde des éléments `non_tenté` dont le brouillon est déjà dans la boîte, et `reprendre` (l.100-102) les redépose après le redémarrage demandé par la note de restauration - correction : la restauration (et la purge totale, n° 2) refuse avec 409 « un lot de brouillons est en cours » tant qu'un travail `brouillons_lot` est actif, ou demande l'annulation coopérative et attend la fin du dépôt en cours AVANT `maintenance_mode.begin()` et `close_db()` ; pour un lot dont un travail a déjà tourné, chaque élément `non_tenté` est cherché par Message-ID dans les Brouillons avant d'être redéposé (n° 6), sinon il passe en `incertain` ; test : lot de 5, restauration pendant le 2e dépôt.
2. P2 - docs/plans/2026-09-24-p096-brouillons-en-lot.md:142-146 - La déconnexion du compte et la purge totale ne coupent pas un lot en cours, et la reprise d'un lot dont le compte a disparu n'est pas prévue - preuve : src/backend/app/routers/email.py:720-778, `disconnect_account` supprime `EmailAccount` et `EmailMessage` sans toucher aux travaux ni aux lots ; en IMAP, le lot garde un fournisseur construit avec le mot de passe déjà déchiffré (email.py:61-74, `decrypt_value`) et continue à déposer dans une boîte que l'utilisateur vient de retirer ; en Gmail, la révocation (email.py:736-756) donne un 401 classé `auth`, qui remet l'élément en `non_tenté` (design l.130-131) et laisse « Reprendre » proposé sur un compte absent : `_compte_email` répond 404 « Email account not found » (email.py:113-118), en anglais, et aucune cause n'est prévue pour ce cas (design l.122-123) ; `delete_all_data` efface `EmailAccount` (data.py:611) et `ProcessingTask` (data.py:642) sans demander d'arrêt, et `annulation_demandee()` rend faux sur une ligne effacée (src/backend/app/services/traitements.py:115-119) : seule la réservation conditionnelle (l.106-107) arrête le lot, ce que le design ne dit pas - correction : la déconnexion d'un compte et la purge totale annulent d'abord les travaux `brouillons_lot` concernés (annulation coopérative, attente du dépôt en cours) ; cause `compte_absent` à la reprise, et « Reprendre » remplacé par « Compte déconnecté » ; dire si l'en-tête d'un compte déconnecté est purgé tout de suite (son modèle ne sert plus) ; tests : déconnexion IMAP pendant le 2e dépôt, reprise après déconnexion, purge totale pendant un lot.
3. P2 - docs/plans/2026-09-24-p096-brouillons-en-lot.md:100-102 - La reprise ne revalide rien : ni règles de proposabilité, ni contacts bloqués, ni empreinte ; elle dépose des brouillons que l'utilisateur n'a jamais vus en aperçu, et peut trahir la décision 3 - preuve : l.33-34, « Le moteur réapplique ces règles aux identifiants reçus, à la prévisualisation comme à la création » (la reprise n'y est pas) ; l.100-102, « sans charge ; relit le modèle de l'en-tête ; relance les seuls `non_tenté` et échecs réessayables » ; la résolution relit les Variables courantes (src/backend/app/services/variables_service.py:337-343, `resolve_message`) ; un contact archivé entre-temps (décision 3, docs/plans/2026-09-24-arbitrages-par-delegation.md:56), une adresse modifiée, une Variable changée ou supprimée, une seconde fiche à la même adresse passent sans question ; la réservation l.106-107 ne fait passer que `non_tenté` vers `en_cours` : la remise des échecs réessayables à `non_tenté` sous le verrou n'est pas écrite - correction : `reprendre` recalcule l'empreinte sur les mêmes contacts et la compare à celle de l'en-tête, réapplique proposabilité, blocages, doublons d'adresse et préfixe `contact_` ; un écart donne 409 « l'aperçu n'est plus à jour » et l'écran rouvre l'Aperçu sur les seuls contacts restants ; remise explicite des échecs réessayables à `non_tenté` sous le verrou ; tests : contact archivé, adresse modifiée et Variable modifiée entre l'arrêt et la reprise.
4. P2 - docs/plans/2026-09-24-p096-brouillons-en-lot.md:90-92 - La conservation promise par la décision 5 (« jusqu'à la fin du lot plus 30 jours ») n'a aucun mécanisme qui la tienne - preuve : `purge_apres` est dans l'en-tête écrit à la création (l.86-92), quand la fin du lot n'existe pas encore ; un lot interrompu par un arrêt brutal (src/backend/app/services/task_registry.py:162-187, seul `ProcessingTask` passe en `interrupted`) ou annulé avec des `non_tenté` n'a jamais de fin écrite, donc son modèle reste sans limite ; l.142-143, « avec les travaux (traitements.py:376-393) » renvoie à une purge calculée sur `ProcessingTask.created_at < seuil` (traitements.py:383-384), pas sur la fin, et lancée seulement au démarrage (src/backend/app/main.py:227-229) ; rien ne dit qu'une reprise au 29e jour repousse l'échéance, et la purge peut effacer l'en-tête sous un travail actif ; un en-tête dont tous les éléments sont partis avec leurs fiches (l.143-145) reste - correction : « fin du lot » = fin du dernier travail du lot, quel que soit l'état final (DONE, FAILED, CANCELLED, INTERRUPTED, bascule du lifespan comprise) ; `purge_apres` réécrit à chaque fin et vidé au lancement d'une reprise ; purge dédiée qui ne touche jamais un lot dont un travail est actif ; en-tête purgé avec son dernier élément ; écrire « au plus tard au premier démarrage après l'échéance », ou purger aussi en cours de session ; tests : lot interrompu, reprise au 29e jour.
5. P3 - docs/plans/2026-09-24-p096-brouillons-en-lot.md:114-121 - « Un exécuteur à un seul fil par lot » ne permet pas la « session neuve » promise après un délai dépassé - preuve : src/backend/app/services/email/imap_smtp_provider.py:305-306, `_run_imap_operation` passe `None` à `run_in_executor`, c'est-à-dire l'exécuteur par défaut partagé, et n'accepte aucun exécuteur ; quand `wait_for` expire, le fil continue (délai socket de 15 s par lecture, `IMAP_CONNECT_TIMEOUT` l.37, `_create_mailbox` l.190-198) et occupe le seul fil du lot : l'ouverture de la session neuve et le dépôt n° 4 attendent derrière lui, expirent sans avoir rien envoyé et seraient classés `incertain` (l.126-127) ; le test « dépôt n° 3 expiré, le n° 4 part sur une session neuve » (l.120-121), joué avec des doublures, ne le voit pas - correction : un exécuteur à un fil PAR SESSION, abandonné avec elle (`shutdown(wait=False, cancel_futures=True)`), et `_run_imap_operation` qui reçoit l'exécuteur ; un dépôt dont la fonction n'a jamais démarré reste `non_tenté` ; test avec un vrai fil bloqué sur un `threading.Event`.
6. P3 - docs/plans/2026-09-24-p096-brouillons-en-lot.md:135-138 - Le Message-ID n'a pas d'usage écrit, et sa phrase contredit l'exclusion de l'incertain - preuve : l.126-127, l'incertain est « exclu de la reprise » ; l.137-138, sans Message-ID « la reprise d'un incertain reste manuelle », ce qui suppose une reprise automatique qui n'est décrite nulle part ; `EmailProvider` n'a aucune recherche par en-tête (src/backend/app/services/email/base_provider.py:99-357), `SendEmailRequest` n'a pas de champ pour l'en-tête (base_provider.py:85-96), et ni `_message_du_brouillon` (imap_smtp_provider.py:529-545) ni `_encoder_message` (src/backend/app/services/gmail_service.py:259-316) n'en posent - correction : soit `chercher_brouillon(message_id)` pour les deux fournisseurs (IMAP `SEARCH HEADER Message-ID`, Gmail `q=rfc822msgid:` sur les brouillons), qui reclasse un `incertain` en `créé` ou `non_tenté` avant la reprise et sert au n° 1, avec un champ ajouté au contrat commun ; soit retirer le Message-ID du design.
7. P3 - docs/plans/2026-09-24-p096-brouillons-en-lot.md:124-129 - « Gmail remonte l'exception httpx d'origine » change le contrat de `_request`, et le 403 `rateLimitExceeded` n'est pas reconnaissable - preuve : src/backend/app/services/gmail_service.py:150-187, `_request` sert aux 15 appels du service, dont les appelants attendent une `HTTPException` ; l.175-181, seul `error.message` est gardé, la `reason` (`rateLimitExceeded` ou `insufficientPermissions`) est perdue ; l.177, le corps d'erreur du fournisseur part tel quel au journal, hors de la règle l.133-134 - correction : un chemin propre au lot (une exception dédiée qui porte le statut, la raison, « avant ou après l'envoi » et l'exception httpx en `__cause__` par `raise ... from e`), sans toucher aux autres routes ; garder `error.errors[0].reason` ; test : 403 `rateLimitExceeded` face à 403 `insufficientPermissions`.
8. P3 - docs/plans/2026-09-24-p096-brouillons-en-lot.md:185-190 - Deux fermetures du canevas ne passent ni par `collapseScenarioPanel` ni par `rangerPourLAccueil`, et jettent la sélection sans question - preuve : src/frontend/src/components/prototype/ConversationCanvasPrototype.tsx:1173-1182, quand le store ne désigne plus de vue, l'effet fait `setCanvasOpen(false)` sans consulter le registre (la garde B-994, l.1161, ne vaut que si `embeddedView !== null`), cas atteint par `fermerLeChat` et son `goBack()` (l.1275-1285) quand le chat était ouvert à côté du canevas (à vérifier sur l'historique réel) ; l.1345-1347, rétrécir la fenêtre avec le chat ouvert ferme le canevas, et ce geste ne peut pas poser de question - correction : l'état du panneau (contacts cochés, modèle, étape) vit dans un store non persisté qui survit au démontage du canevas, et l'effet l.1173 consulte le registre comme la garde l.1161 ; tests : fermeture du chat ouvert à côté, rétrécissement de la fenêtre.
9. P3 - docs/plans/2026-09-24-p096-brouillons-en-lot.md:191-197 - Le mode démo reste incomplet sur quatre points - preuve : l.98, « masqué en démo » pour `GET …/batch/{batch_id}`, alors que le moteur ignore le mode démo (aucune occurrence de `demo_mode`, `mode_demo`, `demoMode` ou `X-Demo` dans src/backend/app) ; l.192-194, seule la table du panneau est citée pour l'aperçu, rien ne dit que le texte du modèle et les valeurs des Variables passent aussi par `maskText` (src/frontend/src/hooks/useDemoMask.ts:65-71), donc un autre contact ou un projet nommé dans le modèle reste en clair ; l.183-184, « Reprendre » crée des brouillons mais n'est pas rendu inactif en démo comme « Créer » (l.195-196) ; l.40-41, « adresse en double avec <contact masqué> » : si le moteur rend un nom, l'écran l'affiche en clair - correction : masque côté écran, dit comme tel ; aperçu passé par la table du panneau PUIS par la table globale ; « Reprendre » inactif en démo avec sa raison ; le moteur rend des identifiants, jamais des noms, dans ses messages de refus.
10. P3 - docs/plans/2026-09-24-p096-brouillons-en-lot.md:21-24 - « Aucun corps personnalisé en base » est trop absolu : la messagerie de THÉRÈSE met en cache le brouillon ouvert, sans lien au contact, et l'anonymisation ne l'efface pas - preuve : src/backend/app/routers/email.py:1232-1243 (IMAP) et 1265-1277 (Gmail), `get_message` enregistre `body_plain` et `body_html` ; dossier « Brouillons » dans src/frontend/src/components/email/EmailPanel.tsx:263 ; `_message_cache_depuis_dto` (email.py:77-110) ne pose pas `contact_id`, alors que l'anonymisation n'efface que `EmailMessage.contact_id == contact.id` (src/backend/app/services/rgpd_auto.py:212, src/backend/app/routers/rgpd.py:302) - correction : écrire « le lot n'écrit aucun corps personnalisé ; un brouillon ouvert dans THÉRÈSE entre dans le cache comme tout message » et dire si ce cache est rattaché au contact pour partir avec la suppression ou l'anonymisation.
11. P3 - docs/plans/2026-09-24-p096-brouillons-en-lot.md:68-69 - « Plus de 20 jetons ou plus de 60 000 caractères au modèle » ne correspond pas aux bornes réelles - preuve : src/backend/app/services/variables_service.py:284-285 et 302-307, 20 jetons PAR appel, et l'objet et le corps sont résolus séparément (l.67-68), soit 20 chacun ; l.323-327, les 60 000 caractères portent sur le texte RÉSOLU : une Variable liste en mode bloc (100 éléments de 500 caractères, l.25-26) fait dépasser un modèle court, avec une `VariableError` par contact dont le sort n'est pas dit - correction : écrire « 20 jetons pour l'objet et le corps réunis » (ou par partie) ; corps résolu de plus de 60 000 caractères = lot refusé à l'aperçu (les Variables sont les mêmes pour tous) ; test.
12. P3 - docs/plans/2026-09-24-p096-brouillons-en-lot.md:153-155 - L'espion « aucun envoi » ne couvre pas tout le SMTP - preuve : src/backend/app/services/email/imap_smtp_provider.py:915-924, `aiosmtplib.SMTP(...)` est instancié directement ; un espion sur `aiosmtplib.send` ne voit ni `SMTP.send_message` ni `SMTP.sendmail` - correction : espion sur `aiosmtplib.SMTP.connect` et `smtplib.SMTP.__init__` (le lot n'a aucune raison d'ouvrir du SMTP), en plus des espions déjà prévus.
13. P3 - docs/plans/2026-09-24-p096-brouillons-en-lot.md:142-146 - Les deux tables n'apparaissent dans aucun export RGPD - preuve : src/backend/app/routers/data.py:429-430 (export global, assemblé par `_assembler_export_rgpd`, l.171, qui couvre déjà les Variables, l.217-273) et src/backend/app/routers/rgpd.py:46-60 (export d'un contact) ne sont pas cités ; l'élément du lot est une donnée de traitement du contact (« a reçu tel lot ») ; l'en-tête garde un modèle en texte libre, qui peut nommer des tiers (« suite à notre échange avec … ») et vit jusqu'à 30 jours après la fin du lot (design l.21-24, l.90-92) : la purge totale le couvre (l.145-146), mais ni l'export ni la suppression ou l'anonymisation d'une fiche (l.143-145) ne le touchent, même s'il nomme ce contact en clair - correction : l'export global inclut en-têtes et éléments ; l'export d'un contact inclut ses éléments (lot, date, statut), sans `adresse_hash` ; écrire que le modèle suit le régime des conversations (purge totale et export, aucun nettoyage par fiche) et le dire dans l'aide de l'étape Modèle.
14. P3 - docs/plans/2026-09-24-p096-brouillons-en-lot.md:139-141 - « juste après `recuperer_taches_orphelines` » place la fonction dans un `try` fail-open : si la reprise des travaux ou leur purge échoue, les `en_cours` restent `en_cours` pour toujours, ni reprenables ni signalés - preuve : src/backend/app/main.py:217-233, un seul `try` couvre `recuperer_taches_orphelines` et `purger_les_terminees`, et l'`except` journalise « Reprise des traitements orphelins ignorée » - correction : un `try` propre à la fonction, et une bascule à la lecture du GET quand le dernier travail du lot est `interrupted`.
15. P3 - docs/plans/2026-09-24-p096-brouillons-en-lot.md:180-184 - L'écran n'a pas ce que le compte rendu suppose - preuve : src/frontend/src/services/api/processingTasks.ts:6-27, le type `Traitement` n'a pas `entity_id`, alors que le moteur l'envoie (src/backend/app/services/traitements.py:287) ; « état du lot dans un store » ne dit pas « non persisté », or plusieurs stores le sont (src/frontend/src/stores/emailStore.ts:218-223) : des aperçus fusionnés y finiraient sur le disque de la webview - correction : ajouter `entity_id` au type ; store du lot non persisté (ou ne persister que `batch_id`), aperçus jamais dans un store persisté.
16. P3 - docs/plans/2026-09-24-p096-brouillons-en-lot.md:78 - L'empreinte contient encore un « choix de signature » qui n'existe plus - preuve : décisions 2 et 11 (arbitrages l.55 et l.66), « pas de signature ajoutée » ; design l.25-26 - correction : retirer le choix de signature de l'empreinte.
17. P3 - docs/plans/2026-09-24-p096-brouillons-en-lot.md:39-45 - Deux règles ont un sens flou - preuve : l.40-41, « un second contact à la même adresse est refusé à la validation » ne dit pas si le lot est refusé ou si le contact est bloqué (l'Aperçu, l.172-174, sait « Retirer du lot ») ; l.44-45, « Déjà écrit il y a moins de 24 h » ne dit pas quels statuts comptent (`créé` seul, ou `incertain` aussi) ni si un envoi fait hors lot depuis THÉRÈSE compte - correction : adresse en double = contact bloqué, avec « Retirer du lot » ; déjà écrit = éléments `créé` ou `incertain` de moins de 24 h, envois hors lot exclus (et le dire à l'écran).

VERDICT : NO-GO
