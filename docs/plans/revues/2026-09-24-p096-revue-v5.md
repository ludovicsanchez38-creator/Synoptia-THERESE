| Constat V4 | Statut | Où la V5 le traite / ce qui reste |
|---|---|---|
| 1 (P1) | partiel | l.183-190 (la recherche ne fait que promouvoir, l'absent reste `incertain`, geste « Recréer ») et l.222-224 ; la restauration ne reclasse que les `non_tenté`, pas les échecs réessayables (n° 2), et ce reclassement n'a pas de lieu d'exécution (n° 3) |
| 2 | traité | l.218-230 ; reste le placement exact dans la route et la variante d'`actif_pour` (n° 7) |
| 3 | partiel | l.118-131 ; même lot et « Abandonner » écrits, mais l'empreinte de reprise n'a pas de référence stockée (n° 4) et la reprise ignore les autres lots (n° 6) |
| 4 | partiel | l.231-236 ; modèle vidé, en-tête gardé, mais l'attente de 45 s dépasse le délai de l'écran (n° 5) |
| 5 | traité | l.188-191 ; reste le lot qui n'a plus que des `incertain` (n° 10) |
| 6 | traité | l.201-209 et l.197-200 ; restent le vidage de `purge_apres` (n° 11) et la passe déjà lancée à l'entrée en maintenance (n° 14) |
| 7 | traité | l.127-129 |
| 8 | partiel | l.151-154 ; drapeau écrit et faisable, mais sans poignée de main il laisse un APPEND tardif après un retour à `non_tenté` (n° 1) ; ma correction V4 était incomplète |
| 9 | traité | l.115-117 |
| 10 | partiel | l.137-141 ; règle et tests écrits, sans verrou de création (n° 8) |
| 11 | traité | l.46-51 |
| 12 | traité | l.87-91 |

1. P1 - docs/plans/2026-09-24-p096-brouillons-en-lot.md:151-157 - Le drapeau « APPEND émis » est lu par la boucle sans poignée de main avec le fil, et l'ordre entre réouverture de session et réservation n'est pas écrit : un fil encore en train de se connecter quand le délai expire fait remettre l'élément réservé à `non_tenté`, puis achève sa connexion, pose le drapeau et dépose ; la reprise suivante redépose le même contact (doublon) - preuve : le design range lui-même « délai à la connexion » parmi les cas sans drapeau remis à `non_tenté` (l.152-154), il admet donc un élément réservé (réservation « avant chaque dépôt », l.135) pendant une connexion ; l.156-157, « la session est abandonnée et rouverte » ne dit pas si la réouverture précède ou suit la réservation du dépôt suivant ; `_run_imap_operation` n'arrête pas un fil démarré (src/backend/app/services/email/imap_smtp_provider.py:305-306, `asyncio.wait_for(loop.run_in_executor(...))` : l'expiration annule le futur asyncio, et `Future.cancel()` échoue sur un appel déjà en cours) ; `shutdown(wait=False, cancel_futures=True)` (l.149-150) n'annule que les appels encore en file ; 15 s par attente de socket (imap_smtp_provider.py:37) contre 30 s pour l'opération (l.38) : connexion TLS, accueil et LOGIN peuvent dépasser 30 s sans qu'aucune attente de socket n'expire ; la recherche par Message-ID ne porte que sur les `incertain` (l.183-187), ce brouillon n'est donc jamais retrouvé - correction (choix technique, aucune décision produit) : rouvrir la session (connexion, LOGIN, dossier vérifié) dans une opération distincte AVANT de réserver l'élément suivant ; puis poignée de main atomique sous un `threading.Lock` partagé par le fil et la boucle, qui couvre le trajet réservation vers APPEND : juste avant `mailbox.append`, le fil pose « émis » seulement si « abandonné » n'est pas posé, sinon il sort sans rien envoyer ; à l'expiration, la boucle pose « abandonné » sous le même verrou et lit « émis » ; `non_tenté` seulement si « émis » est faux à cet instant (ou si `Future.cancel()` a réussi) ; tests avec un vrai fil : réouverture lente, aucun élément réservé pendant ; fil retenu entre réservation et APPEND au-delà du délai, élément `non_tenté` et AUCUN APPEND reçu par le faux serveur.
2. P1 - docs/plans/2026-09-24-p096-brouillons-en-lot.md:222-224 - Après une restauration, seuls les `non_tenté` passent en `incertain` : un élément `échec` réessayable restauré, déposé entre la sauvegarde et la restauration, est remis à `non_tenté` par la reprise et redéposé (doublon) ; et le critère « dont un travail a déjà tourné » est jugé sur la base restaurée, qui ignore par construction ce qui s'est passé après la sauvegarde - preuve : l.128-129, la reprise remet « explicitement les échecs réessayables à `non_tenté` » ; l.183-188, la recherche par Message-ID ne promeut que les `incertain` ; scénario : lot avec trois `reseau_avant_envoi`, sauvegarde, reprise qui crée les trois, restauration de cette sauvegarde dans les 30 jours de conservation : les trois redeviennent `échec` réessayables et « Reprendre » les redépose ; même trou pour une sauvegarde prise juste après la création (travail `queued` dans la base restaurée, donc « pas encore tourné ») d'un lot servi en entier ensuite ; `refusé` aussi si la reprise le revalide (« restants » n'est défini par aucun statut, l.119-121) - correction (choix technique, aucune décision produit) : après l'extraction, tout élément d'un lot restauré dont le statut n'est ni `créé`, ni `incertain`, ni `abandonné` passe en `incertain`, sans critère sur les travaux ; définir « restants » par statut ; test : échec réessayable, sauvegarde, reprise, restauration, reprise, aucun dépôt.
3. P2 - docs/plans/2026-09-24-p096-brouillons-en-lot.md:222-224 - « Après l'extraction » n'a ni lieu ni moyen d'écriture, et les deux lectures naturelles cassent soit la restauration, soit la promesse - preuve : à ce moment `close_db()` a tourné (src/backend/app/routers/data.py:1453) ; les moteurs posent la clé LOCALE (src/backend/app/models/database.py:813-816, `get_db_key_hex()`), alors que `_verify_restored_db` accepte une base chiffrée avec la clé dérivée du `.encryption_key` restauré (data.py:1063-1068, restauration depuis une autre machine) : une écriture par `get_session_context` y échoue, et, placée dans le `try` (data.py:1462-1478), toute exception déclenche `_rollback()` (data.py:1480-1508) : la restauration serait annulée ; une sauvegarde antérieure à la migration n'a pas les tables (« no such table », même effet) ; le chemin `.db` hérité (data.py:1479) ne passe pas par `_verify_restored_db` ; aucun redémarrage n'est imposé après une restauration (src/frontend/src/components/settings/PrivacyTab.tsx:162-169, simple message), donc le lifespan ne peut pas porter ce reclassement : un « Reprendre » peut partir avant tout redémarrage - correction : dans la route, sous maintenance, juste après `_verify_restored_db` (et après la copie du `.db` hérité), une UPDATE brute par `sqlcipher3` avec la clé qui a ouvert la base (faire rendre cette clé par `_verify_restored_db`), ou par `sqlite3` sans clé quand la base restaurée n'est pas chiffrée (`_verify_restored_db` rend alors la main sans rien ouvrir, data.py:1057-1058), sautée si les tables n'existent pas ; son échec = rollback avec un message qui le dit ; tests : sauvegarde sans les tables, sauvegarde chiffrée par une autre clé, chemin `.db`.
4. P2 - docs/plans/2026-09-24-p096-brouillons-en-lot.md:121-124 - L'empreinte de reprise est comparée à « celle de l'en-tête », qui est une autre grandeur : la comparaison échoue toujours, la reprise « sans charge » n'aboutit jamais, et rien ne dit ce que devient la référence après la confirmation - preuve : l'en-tête stocke UNE empreinte (l.109), celle de l'aperçu de création : tous les contacts, étape et portée comprises, révision de TOUTES les Variables (l.93-95 ; `variables_revision` hache `name:updated_at` de chaque Variable, src/backend/app/services/variables_service.py:346-352) ; l'empreinte de reprise porte sur les restants seuls, sans étape, et sur les Variables citées (l.121-123) ; les éléments ne stockent ni nom ni empreinte par contact (l.110-114) : la valeur de référence n'est pas recalculable ; le test « reprise des seuls réessayables sans charge » (l.305-306) est donc impossible - correction (choix technique, aucune décision produit) : à la création, chaque élément reçoit `empreinte_contact` (hachage de id, prénom, nom, entreprise, adresse normalisée, sans étape) et l'en-tête la révision des seules Variables citées (helper filtré par les jetons du modèle) ; la reprise recalcule `empreinte_contact` pour chaque restant, compare aux valeurs stockées, et compare la révision citée ; après une confirmation, ces valeurs sont réécrites ; tests de la revue V4 n° 3 (Variable hors modèle modifiée, étape d'un contact servi changée, fiche servie supprimée) plus « sans charge, rien changé » sans 409.
5. P2 - docs/plans/2026-09-24-p096-brouillons-en-lot.md:231-233 - La déconnexion attend jusqu'à 45 s, l'écran abandonne à 30 s : il annonce un échec pendant que le serveur peut supprimer le compte ensuite - preuve : `disconnectEmailAccount` appelle `apiFetch` sans `timeoutMs` (src/frontend/src/services/api/email.ts:241-245), donc `API_TIMEOUT_MS = 30_000` (src/frontend/src/services/api/core.ts:120 et 248) ; les appels qui savent attendre le disent (`restoreBackup`, `timeoutMs: null`, src/frontend/src/services/api/data.ts:115) ; un dépôt en vol dure jusqu'à 30 s (IMAP_OPERATION_TIMEOUT, imap_smtp_provider.py:38 ; Gmail `timeout=30.0`, src/backend/app/services/gmail_service.py:169), donc toute attente utile dépasse 30 s ; la route supprime le compte après l'attente (src/backend/app/routers/email.py:758-767) : le nouvel essai de l'utilisateur rend 404 ; l'erreur est réduite à « Impossible de déconnecter le compte » (email.ts:245), le 409 « un lot se termine, réessaie » ne serait jamais lu ; sort du traitement serveur quand le client abandonne (middlewares `@app.middleware("http")`, src/backend/app/main.py:712-853) : à vérifier - correction : `timeoutMs: 60_000` (au-delà des 45 s) pour `disconnectEmailAccount` et lecture du `detail` du 409 à l'écran ; test vitest du délai passé, test de route avec un lot qui finit à 40 s.
6. P2 - docs/plans/2026-09-24-p096-brouillons-en-lot.md:118-122 - La reprise ne regarde pas les autres lots : un contact servi entre-temps par un second lot est redéposé sans avertissement - preuve : la revalidation de la reprise liste « proposabilité, blocages, doublons d'adresse, préfixe `contact_` » (l.119-121), pas le « déjà écrit » ; celui-ci n'est qu'un décochage d'écran à la création (l.54-57), sans équivalent pour des restants que choisit le serveur ; la garde de double soumission ne joue que sur une empreinte identique (l.137-139) ; scénario naturel : lot 1 arrêté au 20e contact (réseau), l'utilisateur relance les 30 restants par un nouveau lot (empreinte différente, accepté), puis voit « Reprendre » encore offert sur le lot 1 et clique : 30 doublons ; même chose pour deux lots actifs qui partagent un contact (le « déjà écrit » ne compte pas les `non_tenté`, l.54-55) - correction (application de la décision 6, pas une décision nouvelle) : à la reprise, un restant qui a un élément `créé`, `incertain` ou `en_cours` dans un autre lot (par contact ou par adresse) est exclu par défaut et montré dans l'aperçu de reprise, réintégrable par la confirmation ; à la création, un contact `non_tenté` ou `en_cours` dans un lot actif est signalé comme « déjà écrit » ; test du scénario.
7. P3 - docs/plans/2026-09-24-p096-brouillons-en-lot.md:218-222 - Le contrôle après `begin()` est faisable, mais son lieu exact et sa requête sont à écrire - preuve : `begin()` (src/backend/app/routers/data.py:1443) puis `close_db()` (data.py:1453) : le contrôle doit tenir entre les deux, `get_session_context` servant encore ; à ce point l'archive de sécurité n'existe pas encore (`_create_archive`, data.py:1456), rien à finaliser, mais l'archive déchiffrée en clair existe (`decrypted_temp`, data.py:1405) : placé hors du `try` de data.py:1449, dont le `finally` fait `end()` et la supprime (data.py:1509-1513), le 409 la laisserait sur le disque (promesse US-003) ; `actif_pour` exige un `entity_id` (src/backend/app/services/traitements.py:304) : il faut une variante « un travail actif de ce type » ; le déchiffrement précède `begin()` et est perdu en cas de 409 - correction : contrôle en tête du `try` de data.py:1449, avant `close_db()` ; variante `actif_de_type(type)` avec la même règle (`queued`, ou `running` et `cancel_requested` à adaptateur vivant) ; contrôle de courtoisie aussi avant le déchiffrement ; test : 409, `end()` fait, aucun `.restore.tar.gz` ni `pre_restore_*` restant.
8. P3 - docs/plans/2026-09-24-p096-brouillons-en-lot.md:135-141 - « Deux POST simultanés (un seul lot) » n'a pas de mécanisme : le verrou est « par lot », et il n'existe pas de lot avant l'écriture de l'en-tête - preuve : l.136, « Verrou asyncio par lot » ; l.104-106, le `batch_id` naît dans la requête de création ; le contrôle « lot actif ou de moins de 10 minutes » est une lecture asynchrone qui cède la boucle : deux POST la passent tous deux puis écrivent deux en-têtes ; la suite d'un 409 à l'écran n'est pas dite (cas : échec `auth` au premier dépôt, mot de passe corrigé, même lot soumis dans les 10 minutes) - correction : contrôle et écriture de l'en-tête sous un verrou asyncio global de création ; le 409 rend le `batch_id` et l'écran ouvre son compte rendu, avec « Reprendre » si des éléments le permettent.
9. P3 - docs/plans/2026-09-24-p096-brouillons-en-lot.md:111-112 - Les énumérations n'ont pas suivi la V5, et les deux nouveaux gestes n'ont ni route ni règle d'accès - preuve : `abandonné` (l.131) manque à la liste des statuts (l.111-112) ; `compte_absent` (l.129-130, l.235) manque à la liste des causes (l.161-162) ; « Abandonner les restants » (l.130-131) et « Recréer ce brouillon » (l.187-188) n'ont ni route, ni verrou du lot, ni contrôle de travail actif ; « Recréer » redépose avec le même Message-ID (l.178-179) sans chercher d'abord - correction : ajouter `abandonné` et `compte_absent` aux énumérations et au type de l'écran ; `POST …/batch/{id}/abandonner` et `POST …/batch/{id}/elements/{contact_id}/recreer`, sous le verrou du lot, 409 si un travail est actif ; « Recréer » lance un travail qui cherche d'abord le Message-ID (trouvé = `créé`, rien déposé), refusé si `compte_absent` ou modèle vidé.
10. P3 - docs/plans/2026-09-24-p096-brouillons-en-lot.md:188-191 - Un lot qui n'a plus que des `incertain` n'est jamais cherché : la recherche ne tourne qu'au début d'une reprise, et la reprise n'est offerte que s'il reste des réessayables - preuve : l.189-190, « La recherche tourne dans le travail, au début de la reprise » ; l.278-279, « Reprendre » quand des éléments le permettent ; l'`incertain` est exclu de la reprise (l.186-187) ; après le reclassement d'une restauration (n° 2), un lot de 50 devient 50 `incertain` à traiter un par un par « Recréer » - correction : geste « Vérifier dans mes Brouillons » (travail de recherche seule, même session, même règle : promouvoir, jamais redéposer), offert dès qu'un élément est `incertain` ; « Recréer les N absents » en un geste après cette vérification.
11. P3 - docs/plans/2026-09-24-p096-brouillons-en-lot.md:203-204 - Vider `purge_apres` au lancement d'une reprise peut laisser un lot sans échéance - preuve : la purge épargne déjà tout lot dont un travail est actif (l.207-208), le vidage n'apporte donc rien ; si `creer_traitement` échoue après le vidage (écriture en base, src/backend/app/services/traitements.py:168-195), aucun travail ne finira pour le réécrire et la bascule du démarrage ne vise que les éléments `en_cours` (l.196-197) : le lot n'est jamais purgé (décision 8) - correction : ne pas vider, seulement repousser `purge_apres` à chaque fin ; ou la purge traite une valeur nulle comme `maj` du dernier élément plus 30 jours.
12. P3 - docs/plans/2026-09-24-p096-brouillons-en-lot.md:151-154 - Avec le drapeau posé, un refus explicite du serveur à l'APPEND serait classé `incertain` alors qu'il prouve l'absence de dépôt - preuve : le drapeau est faisable (tout ce qui précède `self.client.append` est local, .venv/lib/python3.13/site-packages/imap_tools/mailbox.py:319-323) ; `MailBox.append` lève `MailboxAppendError` sur une réponse taguée NO ou BAD (mailbox.py:324-331, `check_command_status`) ; la règle « avec drapeau, `incertain` » (l.153) ne distingue que par le drapeau - correction : le drapeau ne régit que le délai et la coupure ; `MailboxAppendError` = échec prouvé (`quota` si `[OVERQUOTA]`, `dossier_introuvable` si `[TRYCREATE]`, sinon `autre` comme écrit l.174-175).
13. P3 - docs/plans/2026-09-24-p096-brouillons-en-lot.md:225-227 - Un lot peut survivre à la purge totale ou perdre un contact en cours de route, et le travail n'a pas de cause pour « fiche introuvable » - preuve : la purge totale n'est pas en maintenance et tient une seule transaction jusqu'au commit (src/backend/app/routers/data.py:584-648) ; un `POST …/batch` qui a validé ses contacts avant ce commit et écrit après laisse un en-tête et des éléments dont les fiches (data.py:632) et le compte (data.py:611) n'existent plus : la réservation les trouve, contrairement à l.226-227 ; de même une fiche supprimée entre la réservation et la fusion (le lot n'écrit aucun corps, il relit donc la fiche à chaque dépôt, l.25-27) ; la liste des causes (l.161-162) n'a rien pour ce cas - correction : le travail relit le compte au démarrage (`compte_absent`) et la fiche au dépôt ; fiche absente = élément `abandonné`, rien déposé ; `delete_all_data` supprime éléments puis en-têtes ; test : purge commitée entre la validation et l'écriture du lot.
14. P3 - docs/plans/2026-09-24-p096-brouillons-en-lot.md:205-207 - « Sautée si `maintenance_mode.active` » ne protège qu'au départ d'une passe : une passe déjà lancée continue pendant `close_db()` et l'extraction - preuve : `begin()` n'attend que les requêtes admises (src/backend/app/services/maintenance.py:52-58), une tâche de fond n'y est pas comptée ; la restauration remplace la base pendant que la passe écrit (data.py:1453, 1471-1473) ; la passe « au démarrage » hérite de `if not skip_services` si elle est posée à côté de sa jumelle (src/backend/app/main.py:317, 369-383) - correction : la passe prend un verrou asyncio que la restauration acquiert après `begin()` (ou s'enregistre comme requête suivie auprès de `maintenance_mode`) ; écrire si la purge de démarrage tourne aussi sous `THERESE_SKIP_SERVICES`.
15. P3 - docs/plans/2026-09-24-p096-brouillons-en-lot.md:142-143 - L'annulation n'est lue qu'avant chaque dépôt : pendant la recherche par Message-ID du début de reprise (jusqu'à 50 éléments), une demande d'arrêt n'est pas vue, et la déconnexion rend 409 au bout de 45 s - preuve : l.142, « drapeau lu avant chaque dépôt » ; l.188-190, recherche en tête de reprise, une commande par élément sur la session (30 s par opération, imap_smtp_provider.py:38) ; l.232-233, 409 après 45 s - correction : drapeau lu aussi entre deux recherches ; un arrêt pendant la recherche laisse les éléments inchangés.
16. P3 - docs/plans/2026-09-24-p096-brouillons-en-lot.md:231-236 - Pendant l'attente de la déconnexion, rien n'empêche une reprise ou un nouveau lot sur ce compte : le travail lancé garde les identifiants en mémoire et dépose après la suppression du compte - preuve : la déconnexion attend les lots actifs puis supprime (l.231-233 ; src/backend/app/routers/email.py:758-767) ; la reprise ne contrôle que les travaux actifs du lot (l.127-128) et la création que l'empreinte (l.137-139) - correction : ensemble « comptes en déconnexion » consulté sous le verrou de création et sous celui du lot (409 « compte en cours de déconnexion ») ; test.
17. P3 - docs/plans/2026-09-24-p096-brouillons-en-lot.md:299-315 - La liste « Tests prévus » n'a pas suivi la V5 - preuve : elle ne cite ni les deux phases du drapeau, ni « Recréer », ni « Abandonner », ni la confirmation sur le même lot, ni les cas d'empreinte de reprise de la revue V4 n° 3, ni l'incertain envoyé ou supprimé puis reprise (revue V4 n° 1), ni le POST en validation à l'arrivée de la restauration (revue V4 n° 2), ni la purge jumelle en maintenance ; certains sont écrits au fil du texte (l.139-141, l.158-160, l.236-238), d'autres nulle part - correction : liste unique, un test par comportement nouveau, y compris ceux des n° 1 à 16.

VERDICT : NO-GO
