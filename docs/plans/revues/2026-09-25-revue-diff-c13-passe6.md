# Revue adverse du diff, cycle 13, passe 6 (830659bb contre 5dc3a0c6)

Relecteur : rôle Claude indépendant (da-relecteur), environ 220 000 tokens. Verdict : NO-GO, 8 constats (1 P2, 7 P3), dont trois régressions des correctifs de la passe 5 (B-1277, B-1271, B-1272).

Avis pris avant correction : les constats 1 à 3 sont un seul défaut, un signal d'arrêt irréversible envoyé depuis une attente annulable. Deux règles appliquées : un signal doit pouvoir être défait si l'attente meurt, et une seule porte (la suspension des créations) couvre la purge et la restauration.

## Tri

| Constat | Bug | Suite donnée |
|---|---|---|
| 1 (P2, profil perdu sur 503) | B-1282 | corrigé : génération avancée sous le verrou |
| 2 (P3, fiches abandonnées sur 503) | B-1283 | corrigé : fiches rendues puis relancées si l'opération n'a pas lieu |
| 3 (P3, restauration hors suspension) | B-1284 | corrigé : suspension de begin() au finally |
| 4 (P3, valeur non textuelle d'un contact) | B-1285 | corrigé |
| 5 (P3, étiquettes toutes écartées effacent l'existant) | B-1286 | corrigé, et signalées aux deux portes |
| 6 (P3, texte JSON découpé ; virgule en CSV) | B-1287, B-1288 | B-1287 corrigé ; B-1288 (format CSV) différé |
| 7 (P3, « Import terminé » sur fichier illisible) | B-1289 | corrigé |
| 8 (P3, clair partiel au déchiffrement) | B-1290 | corrigé |

Hors revue : B-1281 (sophie-02 instable, effet de montage joué après le clic) trouvé par la CI de 830659bb et corrigé avec un test déterministe.

## Rapport brut

1. P2 - src/backend/app/routers/data.py:602-619 (et src/backend/app/services/user_profile.py:361-364) - B-1277 répond 503 « rien n'a été modifié », mais c'est faux quand le plafond expire pendant `arreter_l_indexation_du_profil`. Cette fonction avance `_GENERATION_PROFIL` AVANT de prendre le verrou, et l'annulation par `wait_for` ne revient pas en arrière. L'indexation du dernier profil enregistré, qui attendait le verrou, renonce donc. L'index garde l'ancien profil (« qui suis-je ? » répond l'ancien nom) jusqu'à la prochaine sauvegarde. Le numéro de génération devait justement empêcher ce scénario (user_profile.py:305-306, « le testeur corrige son prénom, la correction est perdue »). Or le plafond se déclenche précisément dans le cas visé par B-1277 : premier vecteur, modèle chargé, voire téléchargé, à l'onboarding. Une annulation (déconnexion, arrêt) pendant la même attente produit la même perte (lecture) - mesuré (scratchpad/p6, cas A : deux `set_user_profile`, « Ancien Nom » puis « Nouveau Nom », vecteur de 0,8 s, plafond abaissé à 0,3 s) : sans purge, `["Ancien Nom", "Nouveau Nom"]` sont indexés ; avec la purge, on obtient `[503, "Des travaux de fond (création ou indexation) ne sont pas terminés : rien n'a été modifié. Réessaie dans un instant."]`, puis `["Ancien Nom"]` seul - n'avancer la génération qu'une fois le verrou tenu : `async with _VERROU_INDEXATION: _GENERATION_PROFIL += 1`. Le verrou étant FIFO, les indexations déjà en file passent avant, et celles qui suivent renoncent. Un profil enregistré PENDANT l'attente de la purge serait encore perdu sur 503 : refuser aussi les enregistrements de profil pendant la purge, comme B-1276 le fait pour le chat. Tester le 503 sur l'index du profil, pas seulement sur les lignes SQL.
2. P3 - src/backend/app/routers/memory.py:141-142 (et :198-202, src/backend/app/routers/data.py:596-599) - Même défaut côté fiches. `_ARRET_DES_INDEXATIONS` est posé dès l'étape 2. Toute tâche d'indexation qui franchit une frontière de fiche à ce moment s'arrête pour de bon (`return`), et ses fiches restantes ne sont jamais indexées. Si le plafond expire ensuite (étape 2 sur une autre tâche, ou étape 3 sur le profil), la route répond 503 « rien n'a été modifié ». Pourtant ces fiches, bien enregistrées, restent hors de l'index vectoriel, et rien ne les réindexe : aucune route de réindexation des contacts n'existe, `indexer_fiches_en_arriere_plan(` n'est appelée qu'en memory.py:811 et crm.py:885 (grep) - mesuré (cas B : trois fiches A1 à A3 importées, vecteur de fiche de 0,2 s, profil de 1,2 s, plafond de 0,6 s) : `issue_purge [503, "... rien n'a été modifié ..."]`, `fiches_indexees: ["A1"]`, drapeau retombé à faux - sur expiration ou annulation, relancer `indexer_fiches_en_arriere_plan` pour les identifiants abandonnés (la boucle peut les rendre au lieu d'un `return` sec), ou ne plus annoncer « rien n'a été modifié ».
3. P3 - src/backend/app/routers/data.py:1517-1534 (et src/backend/app/main.py:731-734) - B-1276 ne couvre que la purge : la restauration n'appelle pas `creations_du_chat_suspendues()`. Elle compte sur `begin()`, dont la passe 5 (point 8) disait qu'il « a déjà vidé les requêtes suivies » : cette lecture est fausse. Le middleware de maintenance libère son compteur dès que `call_next` a renvoyé les en-têtes. Or le chat en flux exécute ses outils dans le corps de la réponse : `StreamingResponse(_stream_response(...))` (chat.py:1669), puis `_stream_response` qui consomme `_do_stream_response` (chat.py:1982), où `_execute_tools_and_continue` est appelé (chat.py:2618). Pendant tout ce corps, la requête n'est plus suivie. Une création du chat lancée après la boucle d'attente n'est ni refusée ni attendue, qu'elle survienne aux étapes 2 et 3, pendant `close_db()` ou pendant l'extraction. `AsyncSessionLocal` reste lié au moteur disposé, et ce moteur se reconnecte (database.py:1174-1180, 1237-1240) - mesuré au niveau du middleware (cas D, route de flux ajoutée en mémoire) : `{"dans_la_route": 1, "dans_le_corps_avant_premier_morceau": 1, "dans_le_corps_apres_premier_morceau": 0}` ; écriture pendant l'extraction non mesurée - envelopper aussi la restauration, de `begin()` au `finally`, dans `creations_du_chat_suspendues()`.
4. P3 - src/backend/app/services/memory_tools.py:270-286 - B-1271 : `_texte` rend "" pour toute valeur qui n'est pas du texte, et le filtre `if _texte(arguments.get(champ))` écarte alors le champ. Sur un contact réutilisé, un téléphone passé en nombre, des notes en liste ou une adresse en objet ne sont plus signalés : l'outil répond `success: true`, « je le réutilise », sans `champs_ignores`. C'est la fausse promesse de la 0.56, rétablie pour ces formes, alors que l'ancien test de présence les signalait. Le schéma déclare `"type": "string"` (memory_tools.py:73-74), mais rien n'oblige un modèle local à le respecter - mesuré (cas E) : `{"phone": 612345678}` donne `[]` (ancien : `["téléphone"]`) ; `{"notes": ["Rappeler lundi"]}` donne `[]` (ancien : `["notes"]`) ; `{"address": {"rue": "1 rue X"}}` donne `[]` (ancien : `["adresse"]`) - compter comme perdue toute valeur présente, non vide et qui n'est pas du texte, et ne comparer que les textes, comme B-1273 le fait pour le budget.
5. P3 - src/backend/app/services/crm_import.py:737-738 (et src/backend/app/services/crm_utils.py:199-210) - B-1272, en mise à jour : `if "tags" in mapped: existing.tags = _parse_value(...)`. Quand `etiquettes_lues` écarte toutes les valeurs, `_parse_value` rend None, et les étiquettes existantes de la fiche sont effacées sans rien au rapport. Avant, la liste était stockée telle quelle et faisait tomber la liste des contacts. Désormais, la perte est silencieuse. La correction de la passe 5 (points 4 et 7) demandait aussi de signaler les valeurs écartées, ce qui n'est fait à aucune des deux portes - mesuré (cas H : fiche `["vip", "client"]`, import JSON CRM de la même fiche avec `"tags": [1, 2]`) : `{"updated": 1, "success": true, "errors": [], "message": "Import terminé : 0 créés, 1 mis à jour, 0 ignorés", "tags_apres": null}` - ne rien remplacer quand toutes les valeurs sont écartées, et ajouter un signalement « étiquettes écartées », comme pour le budget (B-1256) ; faire le même décompte dans la réponse de /api/data/import/contacts.
6. P3 - src/backend/app/services/crm_utils.py:206-207 - `etiquettes_lues` découpe tout texte sur les virgules. Selon la porte, la « règle unique » ne donne donc pas le même résultat. Une étiquette qui contient une virgule casse à l'aller-retour CRM en CSV, alors que le chemin liste la garde (préexistant pour le CSV : l'ancien code découpait déjà). Par ailleurs, un texte en forme de tableau JSON donne des étiquettes corrompues. Avant B-1275, l'import JSON des sauvegardes jetait ce texte ; désormais, il le découpe - mesuré (cas F et I, fiche `["Paris, France", "vip", " btp "]`) : export CSV puis import CRM donne `["Paris", "France", "vip", "btp"]` ; export JSON CRM puis import donne `["Paris, France", "vip", "btp"]` ; export RGPD puis import JSON rend `["Paris, France", "vip"]` à l'identique ; `etiquettes_lues('["vip", "client"]') == ['["vip"', '"client"]']` - pour un texte, tenter d'abord `json.loads` (liste attendue) avant de découper ; à l'export CSV, choisir un séparateur absent des étiquettes, ou refuser la virgule dans une étiquette.
7. P3 - src/backend/app/services/crm_import.py:52-58 - B-1278 : le message annonce « Import terminé » même quand rien n'a été lu. Un fichier illisible (`success=False`, erreur de lecture en ligne 0, crm_import.py:651-655) produit « Import terminé avec 1 signalement(s) : 0 créés, 0 mis à jour, 0 ignorés, sur 0 lignes ». Le message n'a pas d'autre consommateur que la réponse de l'API et le journal (crm.py:792, 809, 915, 932, 955, 972 ; aucun appel frontend ni test e2e, grep), d'où P3 - mesuré (cas G) : `{"success": false, "message": "Import terminé avec 1 signalement(s) : 0 créés, 0 mis à jour, 0 ignorés, sur 0 lignes", "errors": ["File is not a zip file"]}` - distinguer l'échec de lecture (« Import impossible : ... ») et compter les lignes en échec à part des signalements.
8. P3 - src/backend/app/routers/data.py:1476-1482 - Préexistant, hors diff : B-1269 ne couvre que les sorties de `begin()`, et l'écriture même de l'archive en clair reste exposée. `decrypt_backup_archive` écrit le clair par `write_bytes` (encryption.py:568), et seule une `ValueError` est rattrapée. Une `OSError` en pleine écriture (disque plein) laisse donc sur le disque un clair partiel, contre US-003 - lecture, non mesuré - `except BaseException:` qui efface `decrypted_temp` puis relance, ou écriture dans un fichier temporaire renommé une fois complet.
VERDICT : NO-GO
Estimation : environ 200 000 tokens consommés.
