| Constat V1 | Statut | Où la V2 le traite / ce qui reste |
|---|---|---|
| 1 | traité | l.58-61 (202 {task_id, batch_id}, GET du lot) |
| 2 | traité | l.64-67 (table des éléments, UNIQUE) ; la règle « relance = nouveau ProcessingTask » n'est pas écrite (n° 14) |
| 3 | traité | l.68-71 (réservation conditionnelle, verrou, test) ; second POST d'un lot déjà terminé non couvert (n° 7) |
| 4 | partiellement | l.121-125 ; `populateMap` écrase la table globale, un nom de moins de 3 lettres reste en clair (n° 5) |
| 5 | traité | l.40-42 |
| 6 | traité | l.75-76 |
| 7 | partiellement | l.37-39 ; préfixe `contact_` non réservé, `{prenom}` se résout en silence sur la Variable de l'utilisateur (n° 4) |
| 8 | traité | l.47-49, l.105-106 ; portage de `\p{L}` en Python non dit (n° 16) |
| 9 | traité | l.29-30 (`adresse_unique_valide`, présent dans schemas.py:68) |
| 10 | traité | l.43-44, l.84-85 |
| 11 | partiellement | l.82-85 ; pas de code « autre », dossier introuvable indétectable, auth et dossier laissent l'élément perdu (n° 13) |
| 12 | partiellement | l.86-89 ; une coupure après envoi et un délai Gmail restent classés « réseau », donc réessayables (n° 3) |
| 13 | partiellement | l.72-76 ; granularité incompatible avec une boucle dans le fournisseur (n° 2), mention « les brouillons créés restent dans la boîte » absente (n° 21) |
| 14 | partiellement | l.115-118 ; pas d'en-tête de lot (n° 1), `batch_id` client sans règle (n° 7), entrée depuis Travaux sans mécanisme (n° 14) |
| 15 | partiellement | l.108-109, l.119-120 ; saisie en ligne du champ manquant abandonnée sans le dire (n° 9), inscription qui ne retient ni la fermeture ni Échap (n° 6) |
| 16 | traité | l.25-28 |
| 17 | traité | renvoyé à Ludo, l.29, l.155-157 ; incohérence règle ou question (n° 19) |
| 18 | traité | l.111-114 ; test devenu sans objet (n° 20) |
| 19 | traité | renvoyé à Ludo, l.149-152 |
| 20 | partiellement | l.100-107 ; annonce de la progression de la création absente (n° 21) |
| 21 | traité | l.47-49, l.140-141 ; tranché sans Ludo (n° 9) |
| 22 | partiellement | l.77-81 ; faisabilité avec `_run_imap_operation` non écrite (n° 2) |
| 23 | partiellement | l.62-63, l.90, l.144-145 ; la reprise n'a ni modèle ni compte (n° 1), bascule en_cours vers incertain sans propriétaire (n° 10) |
| 24 | traité | l.32-33 ; tranché sans Ludo (n° 9) |
| 25 | partiellement | l.50-52 ; empreinte aveugle à l'ensemble des contacts et à un import (n° 11) |
| 26 | partiellement | l.43-46 ; sort d'un dépassement (lot ou contact) non dit (n° 12) |
| 27 | traité | l.98-99, l.136, l.149-152 |
| 28 | traité | renvoyé à Ludo, l.45, l.153-154 ; l'option « signature texte » n'existe pas (n° 17) |
| 29 | partiellement | clé (batch_id, contact_id) reprise l.66-67 ; drapeau `\Draft` absent (n° 21) |
| 30 | traité | l.18-20 |
| 31 | traité | l.124-125 |
| 32 | traité | l.91-93 |

1. P1 - docs/plans/2026-09-24-p096-brouillons-en-lot.md:62-63 - La reprise n'a rien pour recréer les brouillons : ni modèle (objet, corps), ni compte, ni empreinte ne sont gardés côté moteur, et la route `reprendre` ne reçoit rien ; la survie promise à un redémarrage est donc impossible pour tout lot arrêté par le disjoncteur - preuve : l.18-19 « Le lot n'écrit aucun objet ni corps dans la base … ne garde que des identifiants et des codes » ; l.64-67 colonnes `batch_id`, `contact_id`, `statut`, `draft_id`, `cause_code`, `maj`, « ni objet ni corps » ; l.62-63 `POST …/batch/{batch_id}/reprendre` sans charge alors que `POST …/batch` (l.58-59) porte `account_id`, `subject`, `body`, `empreinte` ; l.90 et l.144-145 promettent la reprise après fermeture et redémarrage ; le store de l'écran (l.115) ne survit pas à un redémarrage - correction : table d'en-tête `brouillons_lot` (batch_id PK, account_id, created_at, total, hachage du modèle, empreinte) et décision de Ludo à ajouter en fin de document : (a) garder le modèle NON résolu (jetons compris) dans l'en-tête jusqu'à la fin du lot plus N jours, en reformulant la promesse l.18-19 ; ou (b) `reprendre` exige `subject`, `body`, `empreinte` renvoyés par l'écran, refusés si le hachage diffère de celui de l'en-tête, et l'écran dit qu'après un redémarrage il faut recoller le modèle.
2. P2 - docs/plans/2026-09-24-p096-brouillons-en-lot.md:77-81 - « Réservation avant chaque appel au fournisseur » (l.68) et « `create_drafts(demandes)` ouvre UNE session IMAP … dépose en boucle » ne tiennent ensemble que si chaque dépôt est un appel séparé ; une liste passée au fournisseur met la boucle hors de portée de la réservation, du drapeau d'annulation et du classement par contact - preuve : src/backend/app/services/email/imap_smtp_provider.py:282-315 `_run_imap_operation` = un seul `asyncio.wait_for(run_in_executor)` de 30 s par appel : si la boucle y tourne en entier, le délai tombe à 30 s pendant que le thread continue de déposer les brouillons suivants ; si la `MailBox` (l.188-198, une seule socket ; sûreté d'imaplib entre threads : à vérifier) est gardée entre deux appels, un dépôt expiré occupe encore la socket et le suivant s'y entrelace - correction : interface `ouvrir_session_brouillons()` qui rend un objet à `deposer(demande)` asynchrone, un `_run_imap_operation` par dépôt, exécuteur à un seul thread par lot ; après un délai dépassé ou une erreur réseau, la session est abandonnée puis rouverte (ou le lot s'arrête) ; les 3 échecs réseau consécutifs se comptent sur ces reconnexions ; test : dépôt n° 3 expiré, le n° 4 part sur une session neuve.
3. P2 - docs/plans/2026-09-24-p096-brouillons-en-lot.md:82-86 - « Seuls réseau et quota sont réessayables » recrée le doublon que le statut incertain devait empêcher : une coupure après l'envoi de la requête est aussi incertaine qu'un délai, et côté Gmail le moteur ne peut pas distinguer l'un de l'autre - preuve : src/backend/app/services/gmail_service.py:185-187 `except httpx.HTTPError` (délai de lecture compris) devient `HTTPException(status_code=500, detail="Gmail API request failed: …")`, même code qu'une vraie erreur 500 de Google ; imap_smtp_provider.py:600-608 une socket coupée après l'envoi de l'APPEND laisse le brouillon peut-être enregistré - correction : réessayable = échec prouvé AVANT l'envoi (connexion refusée, DNS, 401, 429) ; toute erreur après l'envoi (délai, coupure, 5xx) = incertain ; pour Gmail, remonter l'exception httpx d'origine ou un code dédié au lieu de la chaîne 500 ; tests : `ReadTimeout` Gmail et coupure IMAP après APPEND classés incertain.
4. P2 - docs/plans/2026-09-24-p096-brouillons-en-lot.md:37-39 - L'espace de noms `contact_` n'est réservé nulle part : une Variable `contact_prenom` reste créable, et `{prenom}` tapé par habitude se résout en silence sur la Variable de l'utilisateur dans les N brouillons, sans jeton inconnu donc sans refus - preuve : src/backend/app/services/variables_service.py:28-29 `RESERVED_NAMES = frozenset({"action", "aide", "variable", "variables"})`, `_NAME_RE = [a-z0-9_]{1,32}` ; `validate_name` (l.46-53) accepte `contact_prenom` ; un utilisateur qui a une Variable `prenom` (son propre prénom, cas du constat V1 n° 7) obtient « Bonjour Ludovic » chez chaque contact, et la règle l.47-49 ne se déclenche pas - correction : réserver le préfixe `contact_` dans `validate_name` et dire le sort d'une Variable `contact_*` déjà existante (lot refusé tant qu'elle existe) ; à l'étape Modèle, avertissement quand le modèle contient `{prenom}`, `{nom}`, `{entreprise}` ou `{email}` (« veux-tu dire {contact_prenom} ? ») ; tests.
5. P2 - docs/plans/2026-09-24-p096-brouillons-en-lot.md:121-123 - `populateMap` sur les seuls contacts du lot REMPLACE la table de masquage de toute l'application : en démo, les autres contacts et les projets repassent en clair dans les textes libres passés par `maskText` (chat, titres ; les fiches, elles, passent par `maskContact`, demoMask.ts:108) ; et un nom de deux lettres reste en clair dans l'aperçu - preuve : src/frontend/src/hooks/useDemoMask.ts:33-34 `buildReplacementMap(contacts, projects)` puis `useDemoStore.setState({ replacementMap: map })`, écrasement et non fusion ; src/frontend/src/lib/demoMask.ts:179 `last_name.length >= 3`, règle conservée par le commit 191c2f5f (« La règle des trois lettres pour un nom seul reste ») - correction : table propre au panneau, ou fusion additive dans le store, jamais un remplacement ; l'aperçu rend par contact les valeurs substituées et l'écran masque exactement ces valeurs, quelle que soit leur longueur ; test : lot ouvert en démo puis message du chat qui cite un contact hors lot, toujours masqué.
6. P2 - docs/plans/2026-09-24-p096-brouillons-en-lot.md:119-120 - L'inscription dans `saisieEnCours` ne retient pas les sorties propres au canevas : fermer le panneau, Échap et le retour à l'accueil jettent la sélection et le modèle sans question - preuve : src/frontend/src/components/prototype/ConversationCanvasPrototype.tsx:1295-1298 `collapseScenarioPanel` ferme sans consulter `sortieRetenueParUneSaisie` ; c'est lui qu'appellent Échap (l.1381) et le bouton de fermeture (l.2198-2199), et le canevas est alors démonté (l.2194 `canvasOpen && scenario !== 'today' && <ContextCanvas`) ; `rangerPourLAccueil` (l.1394-1406) remet `redactionLibre` et `scenario` à zéro sans question ; src/frontend/src/lib/saisieEnCours.ts:25 exige que la garde pose elle-même sa question - correction : le panneau passe par `useAbandonDeSaisie` (pile d'Échap, registre, question) ; `collapseScenarioPanel` et `rangerPourLAccueil` consultent le registre ; la garde se retire après le 202 (le lot vit alors dans le store) ; test : cocher un contact, fermer le panneau, la question apparaît.
7. P2 - docs/plans/2026-09-24-p096-brouillons-en-lot.md:58-60 - `batch_id` fourni par l'écran sans règle : format, collision, et second POST d'un lot déjà terminé (le verrou l.70-71 ne couvre qu'un lot actif) - preuve : l.58 `batch_id` dans la charge alors que la réponse le rend aussi (l.60) ; UNIQUE(`batch_id`, `contact_id`) (l.66-67) : réécrire les éléments d'un lot existant lève une IntegrityError (500) ou, si l'implémentation remplace les lignes, remet des « créé » en `non_tenté` et recrée les brouillons ; src/backend/app/services/traitements.py:181-182 précédent de refus explicite (`IdentifiantDejaPris`) - correction : le moteur génère `batch_id` (UUID) ; ou, s'il reste un identifiant d'idempotence venu de l'écran, un POST d'un `batch_id` connu rend le `task_id` existant ou 409, jamais une réécriture des éléments ; test : POST, fin du lot, même POST.
8. P2 - docs/plans/2026-09-24-p096-brouillons-en-lot.md:64 - « Migration Alembic » cache quatre gestes obligatoires dans ce dépôt, dont deux qui font tomber la CI ou sauter la migration s'ils manquent - preuve : aucun `alembic upgrade head` au démarrage packagé (src/backend/app/models/database.py:320-322) : la table naît de `create_all` (database.py:1109), donc du modèle SQLModel ; tête épinglée `ALEMBIC_HEAD_REVISION = "a7b8c9d0e1f2"` (database.py:617) contrôlée par tests/test_alembic_stamp.py:144 ; preuve de schéma à étendre (database.py:689 « Toute future révision doit étendre cette preuve »), sinon une base suivie à l'ancienne tête est ré-estampillée sans la table - correction : écrire dans le design : modèle SQLModel avec `UniqueConstraint`, migration `down_revision = "a7b8c9d0e1f2"`, mise à jour de `ALEMBIC_HEAD_REVISION`, extension de la preuve dans `ensure_alembic_stamp` (fonction `tables_de_brouillons_lot()` sur le patron de `tables_de_planning`, database.py:630-636), test de montée depuis une base à l'ancienne tête ; même traitement pour la table d'en-tête du n° 1.
9. P2 - docs/plans/2026-09-24-p096-brouillons-en-lot.md:147-160 - « À trancher » incomplet : des décisions produit manquent, et deux autres sont tranchées dans le corps sans Ludo - preuve : absents de l.147-160 : conservation du modèle pour la reprise (n° 1), signalement 24 h bloquant ou simple avertissement (l.117-118), trace sur la fiche (activité, `last_interaction`, relance) après création d'un brouillon, durée de conservation des éléments du lot (n° 15) ; la saisie en ligne du champ manquant (correction V1 n° 15) a disparu sans être tranchée ; l.140-142 « Réponses aux questions de la V1 » tranche le blocage sans repli (un contact société seule est bloqué dès que le modèle contient `{contact_prenom}`, cas courant en B2B, et Contact n'a pas de civilité, src/backend/app/models/entities.py:26-28) et le plafond 50 : une recommandation de relecteur n'est pas une décision produit - correction : ajouter en tête de « À trancher » la conservation du modèle, le signalement 24 h et la trace CRM, puis la conservation des éléments et la saisie en ligne, chacun avec une recommandation ; marquer Q1 et Q2 de la V1 « recommandé, à confirmer par Ludo ».
10. P3 - docs/plans/2026-09-24-p096-brouillons-en-lot.md:90 - « Redémarrage : un élément en_cours devient incertain » n'a pas de propriétaire - preuve : src/backend/app/services/task_registry.py:162-185 `recuperer_taches_orphelines` ne touche que `ProcessingTask` ; appel au démarrage src/backend/app/main.py:219-229 - correction : fonction dédiée appelée juste après dans le lifespan, avec test ; ou bascule à la lecture du GET quand le travail du lot est INTERRUPTED.
11. P3 - docs/plans/2026-09-24-p096-brouillons-en-lot.md:50-52 - Empreinte contournable : l'ensemble des contacts n'y entre pas, et `max(updated_at)` ne voit pas une fiche modifiée par import - preuve : src/backend/app/services/crm_import.py:692-696 pose `updated_at` depuis le fichier importé (possiblement antérieur) après avoir changé la fiche ; un contact ajouté après l'aperçu passe si sa date est plus ancienne que le maximum ; ni le compte ni le choix de signature n'y figurent - correction : hachage des tuples triés (id, prénom, nom, entreprise, adresse, stage, scope) des contacts du lot, plus `account_id`, choix de signature, révision des Variables et hachage du modèle.
12. P3 - docs/plans/2026-09-24-p096-brouillons-en-lot.md:43-46 - Sort d'un dépassement de borne non dit (lot ou contact), et l'objet du MODÈLE n'est pas assaini - preuve : l.44 « longueur maximale après fusion 250 caractères » dépend des valeurs du contact ; `resolve_text` lève `VariableError` par appel (src/backend/app/services/variables_service.py:304-329) ; la correction V1 n° 26 disait « une erreur de borne refuse le lot, pas un contact » ; l.43 n'assainit que « les valeurs » ; `list_mode` du corps non fixé - correction : objet de plus de 250 caractères pour un contact = contact bloqué « objet trop long » ; plus de 20 jetons = lot refusé ; objet du modèle assaini comme les valeurs ; `list_mode` du corps écrit.
13. P3 - docs/plans/2026-09-24-p096-brouillons-en-lot.md:79-84 - Classement des causes incomplet - preuve : aucun code pour une exception imprévue (400 Gmail, `HeaderParseError`, 403 `rateLimitExceeded` que Gmail rendrait à la place d'un 429 : à vérifier) ni règle arrêt ou suite ; `dossier_introuvable` indétectable à la résolution : `_dossier_special` rend « Drafts » par défaut (src/backend/app/services/email/imap_smtp_provider.py:558-574), l'échec n'arrive qu'à l'APPEND ; un échec auth ou dossier sur l'élément k le laisse en échec non réessayable alors que rien n'a été déposé, et « Reprendre » après réparation du compte l'oublie (l.62-63) - correction : code `autre` qui arrête le lot ; `folder.exists` du dossier résolu avant la boucle ; auth et dossier remettent l'élément en `non_tenté`.
14. P3 - docs/plans/2026-09-24-p096-brouillons-en-lot.md:72-76 - « Relance = nouveau travail » et l'entrée « Voir le compte rendu » depuis Travaux ne sont pas décrites - preuve : `creer_traitement(id=…)` refuse un identifiant déjà pris (src/backend/app/services/traitements.py:181-182) ; src/frontend/src/components/traitements/TraitementsPanel.tsx n'a aucune action par type de travail (seule `annuler`, l.98) - correction : écrire « chaque lancement ou reprise = nouveau ProcessingTask, `entity_id = batch_id` » ; décrire l'entrée : bouton sur les lignes `brouillons_lot` qui ouvre le canevas e-mail en mode compte rendu pour cet `entity_id`.
15. P3 - docs/plans/2026-09-24-p096-brouillons-en-lot.md:64-67 - Table des éléments sans durée de conservation ni sort à la suppression du contact - preuve : les travaux sont purgés à 30 jours (src/backend/app/services/traitements.py:376-393), rien pour les éléments ; aucun `PRAGMA foreign_keys` dans src/backend/app/models/database.py ; suppression de fiche (src/backend/app/routers/memory.py:965) et anonymisation (src/backend/app/routers/rgpd.py:224-233, src/backend/app/services/rgpd_auto.py:196-206) laissent des `contact_id` orphelins et le lien « a reçu tel lot » sans fin ; le signalement 24 h cherche par contact sans index - correction : purge alignée sur les travaux ; suppression des éléments à la suppression et à l'anonymisation d'un contact ; index (`contact_id`, `maj`) ; GET qui affiche « fiche supprimée ».
16. P3 - docs/plans/2026-09-24-p096-brouillons-en-lot.md:47-48 - « Règle de `jetonsMalFormes` portée au moteur » : le motif source n'existe pas en Python - preuve : src/frontend/src/services/api/variables.ts:120 `/^[\p{L}\p{N}_\- ]{1,40}$/u` ; `re` ne connaît pas `\p{L}` et `regex` n'est pas dans pyproject.toml - correction : écrire l'équivalent (`all(c.isalnum() or c in "_- " for c in contenu)`, plus les exclusions `{{…}}`, `{action:` et blocs de code) et un jeu de cas commun aux tests des deux côtés.
17. P3 - docs/plans/2026-09-24-p096-brouillons-en-lot.md:153-154 - Q2 propose une « signature texte du compte » qui n'existe pas - preuve : src/backend/app/models/entities.py:370 seul `signature_html` ; l'envoi convertit le corps texte en HTML pour l'ajouter (src/backend/app/routers/email.py:1299-1310) ; le lot est en texte brut sans échappement des champs (l.45) - correction : poser les options réelles : sans signature ; signature HTML convertie en texte (images et liens perdus) ; corps HTML avec échappement des champs du contact et des Variables.
18. P3 - docs/plans/2026-09-24-p096-brouillons-en-lot.md:87-89 - Message-ID déterministe de forme non dite - preuve : aucun Message-ID posé aujourd'hui (imap_smtp_provider.py:529-545, gmail_service.py:286-287) ; si la messagerie garde l'en-tête du brouillon à l'expédition, l'identifiant part chez le destinataire : à vérifier ; conservation par Gmail `drafts.create` d'un Message-ID fourni : à vérifier - correction : `<hachage opaque(batch_id, contact_id)@domaine de l'adresse du compte>`, jamais les identifiants en clair.
19. P3 - docs/plans/2026-09-24-p096-brouillons-en-lot.md:29 - Archivés exclus et portée globale posés comme règle, puis demandés à Ludo - preuve : l.29 « Proposable : portée globale, `stage` différent de « archive » » ; l.155-157 « archivés (exclus par défaut, recommandé) … Portée : globale seule en V1 (recommandé) ? » - correction : l.29 « sous réserve de la décision 3 ».
20. P3 - docs/plans/2026-09-24-p096-brouillons-en-lot.md:113-114 - Test « sans fournisseur de confirmation : aucun POST » sans objet : la confirmation vit dans le panneau et ne dépend d'aucun fournisseur - preuve : l.111-112 « dans le panneau (pas `requestExternalAction` …) » ; src/frontend/src/components/app/useExternalActionConfirmation.ts:23-33 n'est plus sur le chemin - correction : tester qu'aucun POST ne part avant le clic sur « Confirmer » du panneau (ni par Entrée dans le modèle, ni par double clic).
21. P3 - docs/plans/2026-09-24-p096-brouillons-en-lot.md:100-118 - Trois reliquats de la V1 non repris - preuve : src/backend/app/services/email/imap_smtp_provider.py:606-608 `append` sans `flag_set` (drapeau `\Draft`, correction V1 n° 29) ; « progression annoncée par paliers » (correction V1 n° 20) absente de l'étape Création ; « dire à l'écran que les brouillons déjà créés restent dans la boîte » à l'arrêt (correction V1 n° 13) absent - correction : les ajouter à l'écran et au moteur, avec un test pour le drapeau.
22. P3 - docs/plans/2026-09-24-p096-brouillons-en-lot.md:29-33 - Deux fiches à la même adresse donnent deux brouillons au même destinataire, et le signalement 24 h, tenu par `contact_id`, ne le voit pas - preuve : src/backend/app/models/entities.py:29 `email` indexé mais non unique ; doublons fréquents après imports (crm_import, import_service) - correction : dédoublonner par adresse normalisée à la validation (second contact refusé « adresse en double »), et signaler par adresse autant que par `contact_id`.
23. P3 - docs/plans/2026-09-24-p096-brouillons-en-lot.md:8-10 - « 5 P1, 23 P2 et P3 » et « reprend toutes les corrections » sont inexacts - preuve : le rapport V1 compte 27 P2 et P3 (constats 6 à 32) ; le tableau ci-dessus relève treize reprises partielles, dont le n° 29 - correction : corriger le compte et lister ce qui n'est pas repris.

VERDICT : NO-GO
