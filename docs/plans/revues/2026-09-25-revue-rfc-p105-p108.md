# Revue adverse des RFC P-105 à P-108 (25/09/2026)

Base : les lignes ont été vérifiées au commit des RFC, `33e0955d`. 42 commits ont été ajoutés depuis, et un autre agent commite pendant la relecture. Six fichiers cités ont changé : `dashboard.py`, `config.py`, `prototypeReadModels.ts`, `CapabilityCenter.tsx`, `useDemoMask.ts` et `imap_smtp_provider.py` (`git diff --stat 33e0955d HEAD` sur l'ensemble des fichiers cités). Les autres sont identiques à HEAD. Les chemins partent de la racine du dépôt.

## P-105 - alerte proactive, mail rapproché d'une tâche

1. P2 - src/backend/app/routers/email.py:1108 - La règle 1 (« même thread_id ») et le « Pas lié » définitif « pour ce fil » ne fonctionnent pas sur IMAP, où chaque message forme son propre fil. - `_imap_to_dto` ne pose aucun `thread_id` (src/backend/app/services/email/imap_smtp_provider.py:991-1011) et le routeur écrit `'threadId': msg.thread_id or msg.id`. Chaque réponse du client dans un même échange relance donc l'alerte. Or « Pas lié » définitif est la première parade contre la fatigue d'alerte (RFC l.225). - Reconstituer le fil IMAP à partir des en-têtes `Message-ID`, `In-Reply-To` et `References`, que `headers_only` suffit à lire. L'écrire dans le design et ajouter au plan de tests « une réponse IMAP dans un fil écarté n'est pas reproposée ». À défaut, dire à l'écran que « Pas lié » ne vaut que pour ce message sur un compte IMAP.

2. P2 - src/backend/app/models/entities.py:391 - « L'affichage relit le cache EmailMessage » (RFC l.114) repose sur une clé primaire `id` seule. Or l'identifiant d'un message IMAP de la boîte de réception est son UID nu, un petit entier. - Le modèle déclare `id: str = Field(primary_key=True)  # Gmail message ID`. `_identifiant` rend `str(uid)` pour INBOX (imap_smtp_provider.py:222-225). `get_message` n'écrit la ligne que si elle est absente ou appartient au même compte (routers/email.py:1233-1242). Deux comptes IMAP produisent vite le même « 42 » : la relève bute sur l'unicité, ou le brief affiche l'objet d'un autre compte. Un déplacement de dossier change aussi l'identifiant (commit 267c5e32), et une remise à zéro d'UIDVALIDITY fait pointer le rapprochement vers un autre message. - Deux voies : ranger expéditeur et objet minimaux dans `rapprochements_courrier`, ou passer au préalable à une clé composite (compte, message). Ajouter les tests « deux comptes IMAP, même UID » et « message déplacé ».

3. P2 - src/backend/app/services/calendar/local_provider.py:419 - Le calcul de créneau libre ignore les récurrences de l'agenda local : une réunion hebdomadaire locale ne bloque que sa première occurrence. - Le cache garde la règle brute (`recurrence: str | None = None  # JSON array de règles RRULE`, entities.py:529). Google est déplié par `singleEvents` (src/backend/app/services/calendar_service.py:162) et CalDAV par `expand=True` (src/backend/app/services/calendar/caldav_provider.py:230). Aucun code du dépôt ne déplie une RRULE locale : ni `dateutil` ni `rrule` n'y est importé. Ni la §4.4 ni les tests de la §6 n'en parlent. - Déplier les RRULE locales sur l'horizon avant `creneaux_libres` (`dateutil.rrule`, dépendance à déclarer), ou tenir pour occupées les journées qui portent un événement récurrent non déplié. Ajouter le test « réunion hebdomadaire locale ».

4. P2 - src/frontend/src/components/prototype/usePrototypeReadData.ts:48 - La RFC ne dit pas ce que lit la sixième source du brief, alors que le brief se relit toutes les 5 minutes, au focus et au retour de visibilité. - Le code déclare `TODAY_REFRESH_INTERVAL_MS = 5 * 60_000` (l.5) puis `setInterval(() => void refresh(), ...)` (l.48). Si la source « Courrier » interroge le fournisseur, l'option A devient une relève de fond toutes les 5 minutes, sans consentement (docs/rules/RULES-DONNEES.md:227). Si elle ne lit que la table locale, « Courrier : rien » après trois jours sans ouvrir le Courrier passe pour une boîte vide, ce que la doctrine B-051 interdit. - Écrire que la sixième source lit seulement `rapprochements_courrier` et ne déclenche jamais de relève. Afficher sa fraîcheur (« relevé à 10 h 42 », « jamais relevé »), comme pour l'agenda. Ajouter le test vitest « le rafraîchissement du brief n'appelle pas /veille-courrier/relever ».

5. P3 - docs/plans/2026-09-25-rfc-p105-alerte-mail-tache.md:45 - Le « préalable bloquant » IMAP est périmé : B-1409 a été livré deux minutes après la RFC. - Le commit 974c0cb1 (25/09 à 15:47) passe `mark_seen=False` sur les cinq lectures (imap_smtp_provider.py:359, 437, 693, 750, 862 à HEAD). Un point reste vrai : la liste télécharge toujours les corps entiers. - Réécrire le « Préalable » de la §2 et le lot 0 : il ne reste que `headers_only=True` pour la relève.

6. P3 - docs/plans/2026-09-25-rfc-p105-alerte-mail-tache.md:172 - Le lot 2 annonce une « migration Alembic » sans les trois étapes que le code exige. C'est un P3 plutôt qu'un P2 : il s'agit d'une liste de lot à compléter, pas d'un choix de conception. - Le code fixe `ALEMBIC_HEAD_REVISION = "a7b8c9d0e1f2"` (src/backend/app/models/database.py:617) et prévient : « Toute future révision doit étendre cette preuve » (database.py:689). La preuve de schéma (l.747-757) ignore les nouvelles tables. `_supprimer_toutes_les_donnees` supprime table par table (src/backend/app/routers/data.py:632-698). - Inscrire au lot 2 la révision Alembic, le passage de `ALEMBIC_HEAD_REVISION` à la nouvelle révision, l'extension de la preuve de schéma aux deux tables et une ligne `delete()` dans la purge. Ajouter le test « base ancienne partiellement patchée ».

7. P3 - src/backend/app/routers/email.py:758 - La déconnexion d'un compte manque au lot 2 : les rapprochements et les silences de ce compte resteraient orphelins. - `disconnect_account` efface les `EmailMessage` à la main (l.758-764), sans cascade ORM, et aucun `PRAGMA foreign_keys` n'est posé dans `models/database.py`. - Ajouter au lot 2 « déconnexion d'un compte », avec son test.

8. P3 - docs/plans/2026-09-25-rfc-p105-alerte-mail-tache.md:119 - Le plafond de 50 et le filigrane s'articulent mal : après une absence, les messages au-delà des 50 plus récents ne sont jamais rapprochés. - La relève lit « 50 messages au plus » depuis `last_sync`, et la liste IMAP part du plus récent (`reverse=True`, imap_smtp_provider.py:359). Le critère IMAP SINCE va au jour près (RFC 3501, à vérifier dans le futur code de la relève). Rien ne dit où le filigrane se pose après une relève tronquée. - Poser le filigrane sur le plus ancien message traité et paginer jusqu'à le rejoindre. À défaut, afficher dans le brief « N messages non examinés », selon la doctrine de B-425.

9. P3 - src/backend/app/models/entities.py:375 - Aucun champ ne porte la marque « à reconnecter », posée une seule fois (RFC l.127). - `EmailAccount` n'a que `last_sync`, jamais écrit, et aucun état de compte (entities.py:345-378). - Nommer le support, colonne (donc migration) ou `Preference`, et l'inscrire au lot 2 ou 3.

10. P3 - docs/plans/2026-09-25-rfc-p105-alerte-mail-tache.md:105 - L'interrupteur général « désactivé par défaut » contredit la relève « au geste » à l'ouverture du Courrier (l.120) : le rapprochement au geste tourne-t-il sans activation ? - l.105 et l.120. - Préciser si l'interrupteur gouverne la seule relève périodique ou tout le rapprochement (question pour Ludo).

11. P3 - docs/plans/2026-09-25-rfc-p105-alerte-mail-tache.md:115 - Les silences et les rapprochements écartés sont gardés sans limite, hors de portée de l'anonymisation. - Dans `silences_courrier`, la « valeur » est l'adresse de l'expéditeur, gardée jusqu'à réactivation. L'anonymisation ne suit que `contact_id` (§6 « Données », l.210). Un rapprochement « écarté » vit à jamais pour « ne plus jamais proposer ». - Borner la rétention des lignes écartées, purger les silences par adresse à l'anonymisation d'une fiche et mettre les silences dans l'export.

12. P3 - docs/plans/2026-09-25-rfc-p105-alerte-mail-tache.md:87 - « Oui, c'est lié » est la seule voie qui rende la règle 1 sûre, mais il manque à la liste des actions et au plan de tests. - La l.87 cite le bouton ; les l.101-104 ne listent qu'Ouvrir, Vu, Pas lié et Taire. - L'ajouter aux actions, au statut « confirmé » et aux tests vitest.

13. P3 - src/backend/app/services/planning.py:195 - Les créneaux sont calculés sur le fuseau de Paris et sans jours fériés. - `WorkCalendar` couvre du lundi au vendredi avec `timezone: str = "Europe/Paris"` et aucune liste de fériés. Le 1er et le 11 novembre tombent dans l'horizon des prochaines semaines. - Exclure les fériés français, prendre le fuseau du système ou du profil, et l'ajouter à la question 5.

14. P3 - src/frontend/src/lib/ollamaCloud.ts:6 - En phase 2, « modèle local seulement » ne tient pas : un modèle Ollama nommé `:cloud` ou `-cloud` envoie ses requêtes chez ollama.com. - `estModeleOllamaCloud` le détecte (B-1146). Et `auto_extract_entities`, pris pour modèle (RFC l.160), tourne sur le fournisseur de la conversation, en ligne compris (src/backend/app/routers/chat.py:971-973, B-1139). - Exiger en phase 2 un modèle Ollama local qui ne soit pas « cloud », avec le test « modèle :cloud refusé ».

15. P3 - src/backend/app/services/email_classifier_v2.py:194 - Le filtre des newsletters et promotions lit l'extrait, que la relève IMAP en `headers_only` ne récupère pas. - `full_text = f"{subject} {snippet} {from_name}".lower()`. - Écarter d'abord par `List-Id`, `List-Unsubscribe` et `Precedence: bulk`, puis passer le classeur.

16. P3 - docs/plans/2026-09-25-rfc-p105-alerte-mail-tache.md:96 - Aucune exigence d'accessibilité. La pastille « mail récent » sur le projet et les livrables n'est décrite que comme une marque visuelle, et les quatre actions de la ligne du brief (Ouvrir, Vu, Pas lié, Taire) n'ont ni nom accessible ni ordre de tabulation définis. - Les lignes existantes du brief passent par `Ligne`, une zone cliquable avec une étiquette texte (src/frontend/src/components/prototype/TodayDashboardCard.tsx:302-317, src/frontend/src/components/ui/Ligne.tsx:65-74, l.105). - Pastille avec texte ou `aria-label` (jamais la couleur seule). Des actions en vrais boutons nommés d'après le travail (« Pas lié à Refonte du site »), sur le modèle de `Ligne`. Un test vitest par rôle et par nom.

Questions pour Ludo (P-105) :
- Le chantier « mise au repos des écritures de fond » (docs/plans/2026-09-25-chantier-mise-au-repos-ecritures-de-fond.md) attend encore ton arbitrage. Le valides-tu ? Sans lui, le lot 6 (relève toutes les 30 minutes) ne peut pas partir.
- L'interrupteur « désactivé par défaut » coupe-t-il aussi le rapprochement à l'ouverture du Courrier ? Si oui, Dr_logic ne verra rien tant qu'il ne l'aura pas activé.
- Sur un compte IMAP, acceptes-tu en V1 que « Pas lié » ne vaille que pour un message ? Ou faut-il reconstituer les fils avant de livrer ?
- Combien de temps garder les rapprochements écartés et les silences posés « jusqu'à réactivation » ?
- Horaires : le fuseau de Paris vaut-il pour tous, y compris hors métropole ? Faut-il exclure les jours fériés ?
- L'alerte doit-elle aussi apparaître dans le bloc « Cette semaine » de l'Accueil (P-135, livré après la RFC) ?

VERDICT : NO-GO

## P-106 - lire et exporter des formats structurés

1. P2 - src/backend/app/routers/planning.py:171 - L'option A exporte des plans que personne ne peut construire : aucune route ni aucun écran ne crée de dépendance ou de durée. - Le routeur du planning n'offre que GET schedule (l.171), POST calculate (l.192), DELETE dependency (l.271) et GET snapshot (l.297). `TaskSchedule` et `TaskDependency` ne sont construits nulle part hors des modèles, de la lecture (routers/planning.py:72) et de l'export RGPD (routers/data.py:235-236). L'interface n'appelle `/schedule` nulle part. Tout projet réel sortirait `incomplete` et sans dates : l'export serait vide pour Dr_logic. L'option n'est « plus petite » (RFC l.73) que parce qu'elle ne livre rien. - Déclarer les lots B à D de P-039 (la saisie) comme prérequis de A, ou commencer par B (la lecture), qui répond au besoin exprimé (question pour Ludo).

2. P2 - src/backend/app/services/planning.py:82 - « On exporte exactement ce qui a été calculé » (RFC l.47) est faux pour les arcs et les jalons : l'instantané ne les contient pas. - `PlanningResult` porte les tâches, le chemin critique et les dates (l.82-95). `PlanningTaskResult` n'a ni dépendances ni `is_milestone` (l.54-68) : ce champ n'existe qu'en entrée (l.41). Pour dessiner « une dépendance par arc, avec type et décalage », le GraphML devra relire `TaskDependency` au moment de l'export, qui a pu changer depuis le calcul. - Recalculer `fingerprint_inputs` (l.120) sur les entrées courantes et refuser l'export si l'empreinte diffère de `snapshot.input_hash` (« plan modifié depuis le calcul, recalcule »). Autre voie : ranger les entrées dans l'instantané, avec une nouvelle `ENGINE_VERSION`.

3. P2 - src/frontend/src/components/chat/ChatInput.tsx:547 - La §8 affirme que le texte extrait part « sous la finalité documents du consentement ». C'est faux pour `read_file` et `search_files`. - `finaliteCloud` ne vaut `'documents'` que si le message porte une pièce jointe ou si la conversation en a porté une (l.547-551). Un document indexé que lit l'outil `read_file` (src/backend/app/services/contexte_execution.py:48-49, sans carte) part sous l'accord `llm`. Or le lot 2 élargit ce que `read_file` rend : cartes mentales, BPMN, plannings. - Corriger la §8. Puis, au choix, demander l'accord `documents` dès qu'un outil de lecture de fichier est offert à un modèle en ligne, ou le dire au Centre de confiance (question pour Ludo).

4. P3 - docs/plans/2026-09-25-rfc-p106-formats-structures.md:55 - La RFC se contredit sur yEd. L'option C écarte GraphML parce qu'« un nœud ajouté sans position est invisible ou empilé » (l.40). Le lot 1 exporte pourtant sans coordonnées, en comptant sur yEd pour la mise en page (à vérifier : yEd ne met pas en page à l'ouverture). - l.40 et l.55. - Poser des coordonnées simples (colonne selon l'ordre topologique, ligne selon le rang), ou écrire dans l'en-tête du fichier « Mise en page > Hiérarchique ».

5. P3 - docs/plans/2026-09-25-rfc-p106-formats-structures.md:73 - La justification « P-107 en a besoin » est fausse : P-107 demande un écrivain BPMN, que P-106 ne prévoit pas. - P-106 ne livre que Mermaid et GraphML (l.48-55) et exclut l'écriture BPMN (l.69). P-107 l.69 prévoit pourtant l'« export BPMN du registre par l'écrivain XML de P-106 ». - Retirer l'argument, ou ajouter au périmètre un écrivain BPMN avec la question des coordonnées.

6. P3 - docs/plans/2026-09-25-rfc-p106-formats-structures.md:51 - « Livré en bloc de code dans la conversation » : aucun chemin ne le permet. La route d'export est un GET sans modèle (l.57) et aucun outil du chat n'est prévu. - l.51 et l.57. - Retirer la promesse, ou nommer l'outil et sa classe d'effet.

7. P3 - docs/plans/2026-09-25-rfc-p106-formats-structures.md:68 - La copie horodatée n'a pas d'emplacement. Posée à côté de l'original, dans un dossier indexé ou synchronisé, elle serait réindexée et doublerait les résultats de recherche. Sous Windows, un horodatage avec « : » donne un nom de fichier invalide. - `dossiers_choisis` indexe le dossier de travail et les racines synchronisées (src/backend/app/services/indexation.py:186-202). - Fixer l'emplacement (sous `~/.therese/outputs` ou dans un dossier choisi) et un format sans deux-points, avec un test Windows.

8. P3 - docs/plans/2026-09-25-rfc-p106-formats-structures.md:67 - Les opérations Freeplane doivent interdire tout attribut arbitraire. Selon ses réglages, Freeplane exécute les attributs `script` (Groovy), et une consigne injectée par un document pourrait en faire poser un. - Le lot 3 prévoit « poser une note » et conserve les « attributs inconnus » (l.68). - Liste fermée d'opérations, sans écriture d'attribut, avec le test « attribut script refusé ».

9. P3 - docs/plans/2026-09-25-rfc-p106-formats-structures.md:76 - La question 4 (defusedxml ou lxml durci) est technique : elle n'a pas à remonter à Ludo, qui n'est pas technicien. Par ailleurs, `lxml` n'est qu'une dépendance transitive. - `lxml` 6.1.1 figure dans uv.lock:1406 mais pas dans pyproject.toml. - Trancher en interne : `XMLParser(resolve_entities=False, no_network=True, load_dtd=False, huge_tree=False)` et refus de tout DOCTYPE. Déclarer `lxml` en dépendance directe.

Questions pour Ludo (P-106) :
- Aujourd'hui, personne ne peut saisir de dépendances ni de durées. Faut-il d'abord faire la saisie du planning (suite de P-039), ou commencer par la lecture des fichiers de Dr_logic ?
- Quand l'assistante lit un document en cours de conversation, son contenu part sous l'accord donné pour le chat. Faut-il un accord « documents » dédié ?
- Où ranger la copie d'une carte modifiée : à côté de l'originale, ou dans un dossier de THÉRÈSE ?

VERDICT : NO-GO

## P-107 - un périmètre d'action visible et gouvernable

1. P1 - src/backend/app/services/tool_confirmations.py:181 - `empreinte_action` n'est pas une empreinte : elle contient le texte de l'e-mail. La trace « sans contenu » promise (RFC l.64) écrirait destinataires, objet et corps en clair dans un journal que la purge RGPD conserve. - La fonction rend `"|".join(("send_email", destinataires, copies, (objet or "").strip(), (corps or "").strip()))` (l.181-189), et `None` pour tout autre outil (l.155-158). La purge garde le journal : « On garde les logs d'audit (trace legale) » (src/backend/app/routers/data.py:698). Les corps de mails de tiers survivraient donc à « Effacer toutes mes données ». - N'écrire que l'outil, la conversation, la décision et un condensat SHA-256 salé de l'action. Définir une identité pour chaque outil sous carte. Le test « un marqueur glissé dans les arguments n'apparaît dans aucune ligne » (l.99) doit commencer par `send_email`.

2. P2 - src/backend/app/routers/data.py:893 - La RFC aligne la rétention « sur le nettoyage des journaux » (l.108), mais ce nettoyage ne tourne jamais. - `DELETE /api/data/logs` n'a aucun appelant dans l'interface et aucun planificateur : ses seules références sont routers/data.py:907 et src/backend/app/services/audit.py:218. Les tâches de fond se limitent à OAuth, aux notifications et à la purge RGPD des contacts (src/backend/app/main.py:341-383). - Planifier un nettoyage au démarrage puis chaque jour, avec une durée écrite, et décider si ces lignes survivent à la purge totale.

3. P2 - src/backend/app/services/contexte_execution.py:43 - La colonne « données qui sortent » est déclarée à la main, alors qu'elle dépend de la configuration. La vue peut donc encore mentir, contrairement à la promesse de la RFC (l.43, l.106). - `create_calendar_event` est classé `MUTATION_LOCALE` (l.43), mais écrit chez Google quand l'agenda est un agenda Google. `read_emails` est classé `LECTURE_SEULE` (l.36), mais le contenu lu part chez le fournisseur du chat si celui-ci est en ligne. Un modèle Ollama `:cloud` envoie tout chez ollama.com (src/frontend/src/lib/ollamaCloud.ts:6). La sentinelle ne compare que « observe » à `requires_confirmation`. - Calculer la sortie des données à partir de la configuration réelle (fournisseur, type d'agenda, modèle), comme le fait déjà P-118 (`phrasesDeLEtat`, CapabilityCenter.tsx à HEAD). Tester chaque combinaison.

4. P3 - src/backend/app/routers/agents.py:1351 - L'inventaire des chemins de fond (RFC l.56) oublie le pont MCP. L'Atelier confie le jeton de session à OpenClaw et lui ouvre huit outils de lecture : contacts, e-mails, factures, agenda. Plus largement, aucune sentinelle ne couvre les chemins de fond. - `"therese-bridge"` et `THERESE_MCP_TOKEN` (l.1351-1359) ; src/backend/app/services/mcp_therese_server.py:1-16. - Nommer ce pont dans le registre. Ajouter une sentinelle sur les points d'appel sortants (appels `stream_response`, recherche web, clients HTTP) qui exige une entrée au registre.

5. P3 - src/frontend/src/components/prototype/CapabilityCenter.tsx:588 - P-118 a été livré après la RFC. Le Centre de confiance lit déjà le service d'IA et la recherche web, et la ligne « Traitement externe » s'appelle désormais « Ce qui sort de ta machine ». - Commit aff326d0 (25/09 à 16:13) : `phrasesDeLEtat`, et la ligne l.588 à HEAD. - Greffer le lot 1 sur ce qu'a livré P-118 au lieu de refaire la ligne. Mettre à jour la §2 et l'annexe.

6. P3 - docs/plans/2026-09-25-rfc-p107-perimetre-bpmn.md:51 - Les quatre régimes mélangent deux axes : l'effet (observe, prépare, exécute) et la validation (propose). `send_email` est d'abord « propose », puis « exécute ». Et `generate_document`, est-ce « prépare » ou « propose » ? - `generate_document` est classé `MUTATION_LOCALE` (contexte_execution.py:44) et passe sous carte (tool_confirmations.py:49). - Deux colonnes : l'effet, déclaré, et la validation, calculée. Le vocabulaire affiché en découle.

7. P3 - docs/plans/2026-09-25-rfc-p107-perimetre-bpmn.md:69 - Le lot 2 compte sur « l'écrivain XML de P-106 » et son extracteur BPMN, mais P-106 ne prévoit aucun écrivain BPMN. De plus, un BPMN sans diagramme (BPMNDI, coordonnées) ne s'affiche pas dans les modeleurs courants (à vérifier). C'est le problème même que P-106 juge « large » (P-106 l.40-41). - P-106 l.48-55 et l.69. - Chiffrer dans P-107 l'écrivain BPMN et sa mise en page, ou livrer l'export en Mermaid.

8. P3 - docs/plans/2026-09-25-rfc-p107-perimetre-bpmn.md:82 - La RFC veut tracer « dès le lot 1 » les confirmations des écrans. Or le serveur ne voit pas ces confirmations, et sans fournisseur de contexte le crochet exécute l'action sans rien demander. - src/frontend/src/components/app/useExternalActionConfirmation.ts:26-34. La décision 3 n'apparaît dans aucun lot de la §6. - Créer une route de trace, présenter la trace comme déclarée par l'interface, et l'inscrire dans un lot.

9. P3 - docs/plans/2026-09-25-rfc-p107-perimetre-bpmn.md:68 - « Retirer un outil » ne touche que la liste du chat. Les agents d'action, l'Atelier et le pont MCP liraient encore les e-mails d'un utilisateur qui croit avoir retiré l'outil. - La liste coupée est celle de src/backend/app/routers/chat.py:2483-2502, alors que le pont expose `list_emails` (mcp_therese_server.py:10). - Dire à l'écran le périmètre exact du retrait, ou l'appliquer à tous les chemins.

10. P3 - docs/plans/2026-09-25-rfc-p107-perimetre-bpmn.md:69 - L'export BPMN est un artefact purement visuel : il ne rend aucun service à un lecteur d'écran. La vue qui fait foi doit rester un tableau texte, ce qui n'est écrit nulle part. - La décision 2 (l.81) propose déjà la forme « qui fait, qui valide, où partent les données ». - Écrire que la vue du Centre de confiance est un tableau texte structuré (en-têtes de colonnes, une ligne par outil), le BPMN n'étant qu'une vue dérivée. Ajouter un test vitest par rôle `table`.

Questions pour Ludo (P-107) :
- Le journal des validations : que doit-il garder (outil, date, oui ou non, sans le contenu) ? Combien de temps ? Doit-il survivre à « Effacer toutes mes données », comme le journal actuel ?
- Le Centre de confiance doit-il dire ce qui sort d'après ta configuration réelle (modèle local ou en ligne, agenda Google ou local), plutôt que par une liste figée ?
- L'Atelier ouvre tes contacts, e-mails et factures aux agents OpenClaw. Ce pont doit-il apparaître dans le périmètre, avec son interrupteur ?
- « Retirer un outil » : seulement dans la conversation, ou aussi pour les agents et l'Atelier ?

VERDICT : NO-GO

## P-108 - reprendre son travail sur une autre machine

1. P2 - src/backend/app/routers/data.py:1103 - Aucun contrôle de version entre les deux machines : une archive d'une version plus récente s'importe dans une application plus ancienne. - Le manifeste interne ne porte que `{"version": 1, "couverts": [...]}` (l.1103). `app_version` n'est que dans le `.json` voisin (l.1281), que le lot 1 prévoit justement de « régénérer ». Au démarrage, une base de révision inconnue passe la preuve de schéma, puisque toutes les colonnes anciennes y sont, et `alembic_version` est ré-estampillé à la tête locale, plus ancienne (src/backend/app/models/database.py:675 et l.759). L'application ancienne écrit alors dans un schéma qu'elle ne connaît pas, et sa purge RGPD ignore les tables nouvelles. C'est le cas le plus courant : une machine se met à jour avant l'autre. - Mettre `app_version` et la révision Alembic dans le manifeste interne, et refuser l'import d'une révision inconnue (« mets cette machine à jour d'abord »). Ajouter le test « archive d'une version plus récente refusée avant toute étape destructive ».

2. P2 - src/backend/app/services/mcp_service.py:277 - Importer une archive venue d'ailleurs revient à importer des commandes que THÉRÈSE lancera au démarrage suivant. - L'archive contient `mcp_servers.json` (routers/data.py:1069). Au démarrage, chaque serveur `enabled` est lancé (`if server.enabled:`, l.277 ; `_init_mcp_bg`, src/backend/app/main.py:301-315), et la RFC exige justement un redémarrage après import. Quiconque détient la passphrase peut fabriquer une archive qui exécute ce qu'il veut : archive déposée chez un tiers au lot 2, ou remise par un tiers. - À l'import, restaurer les serveurs MCP désactivés et les lister « à réactiver » avec leur commande, comme les dossiers. Ajouter le test « serveur importé non lancé ».

3. P2 - src/backend/app/routers/data.py:1424 - Selon la RFC, « l'archive de sécurité existante reste le filet » (l.60). En réalité une seule est gardée, et chaque import efface la précédente. - `_prune_pre_restore_backups(backup_dir, keep=name)` (l.1424, définie l.1367). En usage alterné, deux relais suffisent à perdre l'état d'avant le premier. Une facture émise sur B puis écrasée par un relais plus ancien libère son numéro, qui sera réattribué : c'est contraire à la numérotation continue (src/backend/app/routers/invoices.py:81). - Garder une archive de sécurité par relais (au moins les N dernières, ou jusqu'à confirmation). Refuser l'import si l'archive de sécurité n'a pas pu être chiffrée.

4. P2 - docs/plans/2026-09-25-rfc-p108-plusieurs-machines.md:59 - La garde contre l'écrasement n'a pas de mécanisme. De plus, si l'identifiant d'installation vit en base, le premier import l'écrase. - La RFC relève elle-même l'absence d'`updated_at` et de suppression logique (l.34-37). Les tâches de fond écrivent en base toutes les heures (notifications, main.py:345-364) : la date du fichier ne distingue donc pas le travail de l'utilisateur. Et « la restauration a remplacé la table `preferences` ENTIÈRE » (routers/data.py:1632) : un `installation_id` rangé en préférence voyagerait avec l'archive, et les deux machines porteraient le même. - Ranger `installation_id` et le compteur local hors de la base et hors des `targets` de l'archive, dans un fichier à part. Incrémenter le compteur sur les écritures métier (routes de création et de modification), et décrire la comparaison. Ajouter le test « B travaille, puis importe un relais plus ancien : refus sans confirmation ».

5. P3 - src/backend/app/routers/data.py:1067 - La liste de « ce qui ne voyage pas » est incomplète ; il faut la dériver des `targets` de l'archive. - Restent hors de l'archive : `agents/<id>/SOUL.md` (src/backend/app/services/agents/config.py:101), `tools/` (src/backend/app/services/skills/tool_installer.py:55), `voices/` et `models/whisper` (src/backend/app/services/voice_local.py:60, 72), `token_usage.json` (src/backend/app/services/token_tracker.py:508). - Générer la liste depuis `targets`, et la tester.

6. P3 - docs/plans/2026-09-25-rfc-p108-plusieurs-machines.md:109 - Côté sécurité, la RFC ne cite que la clé maîtresse. L'archive porte aussi les jetons OAuth Gmail, les mots de passe IMAP et la configuration des connecteurs MCP, soit l'accès complet aux boîtes mail. - `refresh_token` et `imap_password` de `EmailAccount` (src/backend/app/models/entities.py:355, 363), `mcp_servers.json` dans l'archive (routers/data.py:1069). - Le dire à l'écran au moment du choix de la passphrase.

7. P3 - docs/plans/2026-09-25-rfc-p108-plusieurs-machines.md:55 - La RFC ne dit pas comment le fichier arrive au serveur. Deux voies : envoi dans la requête (taille, délai de 30 s d'`apiFetch`, mémoire), ou chemin local lu par le serveur (validation du chemin). - l.55-58. - Choisir, et appliquer le plafond dès l'entrée.

8. P3 - docs/plans/2026-09-25-rfc-p108-plusieurs-machines.md:93 - L'alerte « dossier synchronisé » (lot 5) protège dès aujourd'hui ceux qui copient `~/.therese` avec la clé en clair : c'est le lot le moins cher et le plus urgent. - l.40 et l.93. - Le placer en premier.

9. P3 - docs/plans/2026-09-25-rfc-p108-plusieurs-machines.md:33 - La RFC compte « neuf magasins Zustand », mais dix fichiers de `stores/` appellent `persist(` (à vérifier : lesquels vivent réellement dans la webview). - `grep -l "persist(" src/frontend/src/stores/*.ts` rend 10 fichiers. - Corriger le compte.

Questions pour Ludo (P-108) :
- Les deux machines doivent-elles avoir la même version de THÉRÈSE pour passer le relais ? Le code plaide pour oui, avec un refus sinon.
- Les connecteurs (MCP) arrivent-ils actifs sur l'autre machine, ou désactivés jusqu'à ce que tu les relances ?
- Combien d'états « d'avant relais » garder sur chaque machine ? Aujourd'hui, un seul.
- L'alerte « tes données sont dans un dossier synchronisé » peut-elle partir avant le reste ?

VERDICT : NO-GO

## Récapitulatif

| RFC | P1 | P2 | P3 | Verdict | Motif principal |
|---|---|---|---|---|---|
| P-105 | 0 | 4 | 12 | NO-GO | Pas de fils IMAP, cache EmailMessage indexé sur l'UID seul, récurrences locales ignorées, sixième source du brief non définie |
| P-106 | 0 | 3 | 6 | NO-GO | Rien à exporter (aucune saisie PERT), instantané sans arcs ni jalons, finalité « documents » fausse pour `read_file` |
| P-107 | 1 | 2 | 7 | NO-GO | `empreinte_action` écrirait le corps des mails dans un journal gardé après la purge, rétention inopérante, sortie des données déclarée à la main |
| P-108 | 0 | 4 | 5 | NO-GO | Aucun contrôle de version, serveurs MCP importés lancés au démarrage, une seule archive de sécurité, garde sans mécanisme |
