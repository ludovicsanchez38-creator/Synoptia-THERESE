1. P1 - docs/plans/2026-09-24-p096-brouillons-en-lot.md:65 - La route de création est synchrone (« Rend {created, failed} ») alors que le travail est dit suivi et annulable dans Travaux (l.50-51) ; la confirmation attend la fin du lot et le client coupe à 30 s - preuve : src/frontend/src/components/app/ExternalActionConfirmation.tsx:75 `await action.run()` sous une couche modale plein écran, Échap et Annuler neutralisés pendant le vol (l.61, l.136) ; src/frontend/src/services/api/core.ts:120 `API_TIMEOUT_MS = 30_000` ; src/backend/app/services/email/imap_smtp_provider.py:604-608 une connexion IMAP et une liste des dossiers par brouillon, délais 15 s + 30 s (l.37-38) ; 50 dépôts ne tiennent pas en 30 s, l'écran dira « L'action n'a pas abouti : Délai de 30000 ms dépassé » pendant que le moteur continue de créer - correction : POST /api/email/drafts/batch valide tout puis répond 202 {task_id, batch_id} sans attendre ; l'action confirmée ne fait que lancer ; le lot tourne en tâche de fond liée au registre ; le compte rendu se lit par GET /api/email/drafts/batch/{batch_id}, alimenté contact par contact.
2. P1 - docs/plans/2026-09-24-p096-brouillons-en-lot.md:67-68 - L'anti-doublon est confié « au registre du travail », qui n'a aucune place pour (batch_id, contact_id) -> draft_id ni pour une cause par contact, et qui refuse une relance sous le même identifiant - preuve : src/backend/app/models/processing.py:58-80 (colonnes id, type, label, state, step, progress, project_id, conversation_id, entity_id, error : une seule chaîne) ; src/backend/app/services/traitements.py:181-182 `IdentifiantDejaPris` si l'id existe ; traitements.py:376-390 purge à 30 jours ; src/backend/app/services/task_registry.py:182-184 un redémarrage pose INTERRUPTED sans dire quels contacts sont servis - correction : nouvelle table persistée (migration Alembic) `brouillons_lot_items(batch_id, contact_id, statut créé|en_cours|échec|incertain|non_tenté|refusé, draft_id, cause_code, maj)` avec UNIQUE(batch_id, contact_id), sans objet ni corps ; chaque lancement ou relance = nouveau ProcessingTask avec `entity_id=batch_id`.
3. P1 - docs/plans/2026-09-24-p096-brouillons-en-lot.md:67-68 - Deux exécutions du même lot peuvent tourner en même temps (délai client du point 1, puis nouveau clic sur « Confirmer ») ; « saute les contacts déjà créés » se lit avant la création, les deux passes créent chacune le brouillon - preuve : ExternalActionConfirmation.tsx:77-80 un échec garde la carte ouverte et réarme « Confirmer » ; traitements.py:304-326 `actif_pour` existe mais n'est qu'un contrôle puis action, avec des await entre les deux ; imap_smtp_provider.py:305-315 un TimeoutError rend la main pendant que le thread de dépôt continue - correction : réserver chaque item avant l'appel au fournisseur par UPDATE conditionnel (non_tenté -> en_cours, rowcount 1 sinon sauter) ; refuser un second lancement si `actif_pour("brouillons_lot", batch_id)` sous un verrou asyncio par batch_id ; test avec deux POST simultanés du même lot.
4. P1 - docs/plans/2026-09-24-p096-brouillons-en-lot.md:76 - « Aperçu et compte rendu passent par maskText » ne masque rien sur cette surface, et l'étape 1 (noms et adresses) comme la liste des contacts bloqués ne sont pas citées - preuve : src/frontend/src/hooks/useDemoMask.ts:67 identité si `replacementMap.size === 0` ; `populateMap` n'est appelé que par TasksPanel.tsx:119, MemoryPanel.tsx:258, CRMPanel.tsx:91 ; ConversationCanvasPrototype.tsx:99 n'importe que useDemoStore ; src/frontend/src/lib/demoMask.ts:154 `if (!key) continue` : un contact sans prénom ni nom (société + adresse, cas courant en B2B) n'entre jamais dans la table, sa société et son adresse passent en clair ; demoMask.ts:168 un nom de moins de 3 lettres n'est pas masqué - correction : `maskContact` sur chaque ligne de la liste, des bloqués, de l'en-tête d'aperçu et du compte rendu ; `populateMap` sur les contacts du lot avant tout maskText du texte résolu ; étendre buildReplacementMap aux fiches sans nom ; test démo qui arrive directement par « Écrire » sans passer par le CRM.
5. P1 - docs/plans/2026-09-24-p096-brouillons-en-lot.md:44-46 - « Un champ vide bloque, jamais de brouillon "Bonjour ," » est intenable avec la fusion `{**variables_utilisateur, **champs_du_contact}` passée à resolve_text (l.69-70) - preuve : src/backend/app/services/variables_service.py:311-321 une clé présente à "" est substituée sans signal (donc « Bonjour , ») ; une valeur None aussi (essai .venv 3.13.5 : re.sub avec un remplaçant qui rend None donne « x  y », sans erreur) ; une clé omise laisse la Variable homonyme de l'utilisateur prendre la place du champ du contact (sa propre `{entreprise}` chez le client) ; `unknown` ne distingue pas un champ vide d'une faute de frappe ; une valeur faite d'espaces passe - correction : calculer les manques AVANT resolve_text (jetons du modèle qui sont des champs du contact dont strip() est vide = contact bloqué) ; ne passer que des valeurs non vides nettoyées ; tout jeton `unknown` restant refuse le LOT entier ; tests "", "  ", None, homonyme utilisateur.
6. P2 - docs/plans/2026-09-24-p096-brouillons-en-lot.md:50-51 - Le travail s'affiche dans Travaux sans masque : un nom ou une adresse dans label, step ou error y apparaît en clair en mode démo, et y reste 30 jours - preuve : src/frontend/src/components/traitements/TraitementsPanel.tsx:91 `{t.label}`, :114 `{t.step}`, :127 `{t.error}` sans maskText ; traitements.py:376 rétention de 30 jours ; la démo peut être activée après le lot - correction : label, step et error ne portent que des compteurs (« 12 brouillons », « 7 / 12 ») ; causes par contact uniquement dans la table des items, lues par l'écran masqué.
7. P2 - docs/plans/2026-09-24-p096-brouillons-en-lot.md:38-39 - « Les champs du contact priment sur une variable homonyme » : écrasement silencieux d'une Variable de l'utilisateur - preuve : variables_service.py:28-29 `prenom`, `nom`, `entreprise`, `email` sont des noms valides et non réservés ; un utilisateur qui signe « {nom} » (son propre nom) ou cite « {email} » (sa propre adresse) enverra le nom et l'adresse du destinataire - correction : espace de noms distinct pour le contact (`{contact_prenom}`, `{contact_nom}`, `{contact_entreprise}`, `{contact_email}`, compatibles `[a-z0-9_]`) ; à défaut, avertissement bloquant à l'étape 2 quand une Variable homonyme existe.
8. P2 - docs/plans/2026-09-24-p096-brouillons-en-lot.md:36-40 - Rien sur les jetons mal formés : `{prénom}`, `{Prenom}` ne sont ni résolus ni signalés par le moteur et partent tels quels dans N brouillons - preuve : variables_service.py:286 `_TOKEN_RE = \{([a-z0-9_]{1,32})\}` (ni accent ni majuscule) ; le garde-fou n'existe que côté écran : src/frontend/src/services/api/variables.ts:111-126 `jetonsMalFormes` (P-061) - correction : appeler jetonsMalFormes à l'étape 2 et porter la même règle au moteur (aperçu et création refusent le lot) ; test `{prénom}`.
9. P2 - docs/plans/2026-09-24-p096-brouillons-en-lot.md:62 - « Adresse présente » n'est pas « adresse valide » : une fiche qui porte deux adresses donne un brouillon à deux destinataires - preuve : src/backend/app/models/schemas.py:283 n'exige qu'un « @ », un point après le dernier « @ » et aucune espace : `a@b.fr,pirate@x.fr` passe ; src/backend/app/services/crm_import.py:459 ne teste que « @ » ; imap_smtp_provider.py:539 et src/backend/app/services/gmail_service.py:286 `', '.join(to)` ; essai avec le Python du dépôt (.venv 3.13.5) : `getaddresses(["a@b.fr,pirate@x.fr"])` rend deux adresses et l'en-tête `To: a@b.fr, pirate@x.fr` est émis sans erreur - correction : au moteur, getaddresses doit rendre exactement une adresse, sans virgule, point-virgule, chevron ni caractère de contrôle, sinon contact refusé « adresse invalide » ; même contrôle à l'étape 1 pour ne pas le proposer.
10. P2 - docs/plans/2026-09-24-p096-brouillons-en-lot.md:78 - Un champ de contact multi-ligne fusionné dans l'objet : pas d'injection d'en-tête (la bibliothèque standard la refuse) mais un échec mal attribué et le nom du contact au journal, contrairement à « jamais le corps » - preuve : essai .venv 3.13.5 : `msg["Subject"]="Bonjour Jean\nBcc: pirate@x.fr"` passe à l'affectation, puis `as_bytes()` lève HeaderParseError dont le message contient la valeur ; côté IMAP l'appel est dans `_sync_create` (imap_smtp_provider.py:604-608), la route journalise `{e}` (src/backend/app/routers/email.py:1406) et l'impute « au serveur IMAP » (email.py:1407-1410) ; côté Gmail `_encoder_message` lève (gmail_service.py:315) hors de tout try de la route (email.py:1425), donc 500 - correction : avant fusion, remplacer \r, \n et caractères de contrôle des champs du contact et des Variables par une espace dans l'objet (list_mode inline imposé), puis strip ; ne pas réutiliser le gestionnaire de la route : journaliser `type(e).__name__` et contact_id seulement ; ne pas compter sur la bibliothèque comme défense.
11. P2 - docs/plans/2026-09-24-p096-brouillons-en-lot.md:52-53 - « La cause par contact » passée par message_pour_ecran (l.66) sera la même phrase générique pour toute panne, et « Réessayer les échecs » relancera des échecs non réessayables - preuve : src/backend/app/services/error_handler.py:120-127 seules TheresError et ErreurPourEcran passent, le reste devient « Une erreur s'est produite… Réessaie » ; les pannes réelles sont des RuntimeError (humanisation IMAP), TimeoutError (imap_smtp_provider.py:313) ou HTTPException 401/429 (gmail_service.py:172-181) - correction : classer chaque échec par code (auth, quota, réseau, délai_incertain, adresse_invalide, champ_invalide, dossier_brouillons_introuvable) avec un libellé écrit pour chacun ; seuls réseau et quota sont réessayables ; auth, quota et dossier arrêtent le lot, le reste passe « non tenté ».
12. P2 - docs/plans/2026-09-24-p096-brouillons-en-lot.md:53 - Un délai dépassé est rangé en échec alors que le brouillon peut exister : « Réessayer les échecs » crée un doublon - preuve : imap_smtp_provider.py:305-315 `asyncio.wait_for(loop.run_in_executor(...))` : le thread de dépôt n'est pas interrompu, TimeoutError levée pendant qu'il continue ; `_message_du_brouillon` (l.529-545) ne pose aucun Message-ID - correction : statut « incertain » distinct, exclu de la relance, avec consigne « vérifie tes Brouillons » ; mieux : Message-ID déterministe par (batch_id, contact_id) et recherche avant toute relance d'un incertain (IMAP SEARCH HEADER Message-ID, Gmail `rfc822msgid:`).
13. P2 - docs/plans/2026-09-24-p096-brouillons-en-lot.md:51 - « Annulable entre deux brouillons » : le bouton Arrêter n'apparaîtra pas si le producteur se contente de lire l'état, et une annulation asyncio laisserait un brouillon créé et non compté - preuve : traitements.py:294-300 `can_cancel` d'une tâche running exige `task_registry.est_vivante` (adaptateur inscrit par lier_adaptateur, l.88-100) ; task_registry.py:88-99 AnnulationCooperative est l'adaptateur adapté ; imap_smtp_provider.py:305-310 le thread survit à une annulation - correction : écrire que le lot s'enrôle avec AnnulationCooperative, lit le drapeau avant chaque contact, finit et enregistre le brouillon en cours, se termine CANCELLED avec « 7 créés, 5 non tentés » ; jamais AnnulationParTacheAsyncio ; dire à l'écran que les brouillons déjà créés restent dans la boîte.
14. P2 - docs/plans/2026-09-24-p096-brouillons-en-lot.md:60 - batch_id « généré par l'écran » et compte rendu tenu par le panneau : fermer, ouvrir un message reçu ou changer de scénario vide l'état ; « Réessayer les échecs » devient introuvable et une nouvelle saisie crée un nouveau lot, avec des doublons pour les contacts déjà servis ; deux fenêtres donnent deux batch_id - preuve : src/frontend/src/components/prototype/EmailConversationCard.tsx:228-258 l'effet remet tout à zéro sur `messageId`, `resource` ou `nouvelleRedaction` ; ConversationCanvasPrototype.tsx:340-350 le canevas démonte EmailMessageCanvas dès que le scénario change - correction : batch_id et état du lot dans un store, rechargeables par GET /drafts/batch/{id} ; entrée « Voir le compte rendu » depuis Travaux ; au lancement, avertir si un lot des dernières 24 h a déjà créé un brouillon pour l'un des contacts choisis (empreinte compte + contact + objet résolu).
15. P2 - docs/plans/2026-09-24-p096-brouillons-en-lot.md:45-46 - « 3 contacts à compléter » : compléter une fiche oblige à quitter la surface e-mail, ce qui détruit la sélection et le modèle, et Échap ou le rail ferment sans question - preuve : ConversationCanvasPrototype.tsx:340-350 rendu conditionnel par scénario (email ou memory) ; src/frontend/src/lib/saisieEnCours.ts:17 `inscrireSaisieEnCours` (B-978) existe et le panneau ne s'y inscrit pas - correction : par contact bloqué, « Retirer du lot » et « Retirer les N bloqués » ; saisie en ligne du champ manquant (mise à jour de la fiche) sans quitter ; inscription dans saisieEnCours dès qu'un contact est coché ou le modèle modifié.
16. P2 - docs/plans/2026-09-24-p096-brouillons-en-lot.md:31-35 - Liste, recherche, filtre par étiquette et « 4 contacts sans adresse » ne peuvent pas venir des données actuelles de l'écran - preuve : src/frontend/src/stores/contactsStore.ts:21 et :76 au plus 200 contacts chargés, triés par mise à jour ; src/backend/app/routers/memory.py:542-575 GET /contacts sans filtre d'adresse ni d'étiquette ni compteur, `le=200` ; src/backend/app/models/entities.py:33 étiquettes en JSON dans une chaîne - correction : route dédiée (ex. GET /api/email/drafts/batch/candidats?q=&tag=&offset=) qui filtre l'adresse non vide côté serveur et rend des compteurs exacts (proposables, sans adresse, exclus) ; le moteur réapplique ce filtre aux contact_ids reçus.
17. P2 - docs/plans/2026-09-24-p096-brouillons-en-lot.md:74-75 - Aucune règle d'exclusion : contacts archivés, consentement expiré ou base légale absente, fiches de portée projet ou conversation seront proposés pour un envoi en nombre préparé - preuve : entities.py:37 `stage` (dont archive), :46-50 champs RGPD (base légale, expiration, consentement), :53-54 `scope` et `scope_id` ; memory.py:562 le filtre de portée ne s'applique que si l'écran le demande ; seule l'anonymisation vide l'adresse (src/backend/app/routers/rgpd.py:226) - correction : exclure `stage == "archive"` par défaut ; signaler une expiration RGPD passée ou une base légale vide ; trancher la portée (global seul en V1) et la faire respecter au moteur.
18. P2 - docs/plans/2026-09-24-p096-brouillons-en-lot.md:47-48 - requestExternalAction est fail-open : sans fournisseur de confirmation, les N brouillons partent sans question - preuve : src/frontend/src/components/app/useExternalActionConfirmation.ts:19-33 « Sans provider, l'action part immédiatement » ; la protection ne tient aujourd'hui qu'au montage sous le fournisseur (src/frontend/src/App.tsx:265-267) - correction : pour le lot, confirmation fail-closed (refus et message si le contexte est absent) ; test qui monte le parcours sans fournisseur et vérifie qu'aucun POST ne part.
19. P2 - docs/plans/2026-09-24-p096-brouillons-en-lot.md:24-27 - L'ambiguïté visible d'« Écrire » n'est pas levée : seul le nom accessible change, l'infobulle n'existe pas et la contrainte de largeur invoquée non plus - preuve : ConversationCanvasPrototype.tsx:2053 conteneur `flex flex-wrap` (les pastilles passent à la ligne) ; :2055-2069 la pastille n'a ni aria-label ni title ; src/frontend/src/lib/etabli.ts:20-23 ActionEtabli n'a que id et label ; l'option de la palette (ConversationCanvasPrototype.tsx:566-590) affiche aussi `action.label` - correction : trancher dans le design ; recommandé : libellé visible « Écrire un e-mail » (le retour à la ligne absorbe la largeur) ; à défaut, champ `libelleAccessible` sur ActionEtabli appliqué en aria-label ET title sur la pastille et l'option de palette, le nom commençant par le libellé visible (WCAG 2.5.3).
20. P2 - docs/plans/2026-09-24-p096-brouillons-en-lot.md:29-53 - Parcours en cinq étapes sans règle clavier ni lecteur d'écran : on ignore si c'est un assistant (une étape visible) ou une page, où va le focus, ce qui est annoncé - preuve : le panneau est une region titrée par un h2 masqué (ConversationCanvasPrototype.tsx:327, :336) ; un bouton « Suivant » qui disparaît renvoie le focus au body ; le compteur « 12 contacts choisis », l'aperçu « 1 / 12 » et la progression n / N changent sans annonce - correction : écrire : groupe radio pour la bascule un/plusieurs ; fieldset + legend pour les cases ; plafond expliqué, cases en aria-disabled plutôt que disabled pour rester découvrables ; focus sur le titre de l'étape à chaque changement ; aria-live polite pour compteur et aperçu, progression annoncée par paliers ; précédent et suivant nommés par le destinataire ; test clavier de bout en bout.
21. P2 - docs/plans/2026-09-24-p096-brouillons-en-lot.md:92-93 - Q1 : recommandation, blocage en V1 et aucune syntaxe de repli - preuve : `{prenom|Madame, Monsieur}` ne serait ni résolu (variables_service.py:286, « | » refusé) ni signalé (variables.ts:120 le contenu échoue au motif `^[\p{L}\p{N}_\- ]{1,40}$` et est ignoré) : il partirait en clair dans les brouillons ; Contact n'a pas de civilité (entities.py:26-34) ; un contact société seule sera bloqué sur `{prenom}` - correction : blocage, plus « Retirer les N bloqués » et saisie en ligne (point 15) ; si un repli vient plus tard, modifier ensemble resolve_text et jetonsMalFormes, avec un test commun.
22. P2 - docs/plans/2026-09-24-p096-brouillons-en-lot.md:95 - Q3 : recommandation, pas de délai fixe mais une seule session IMAP par lot et un disjoncteur - preuve : `_connect_mailbox` (imap_smtp_provider.py:249) et la liste des dossiers (l.548-575) sont rejoués à chaque brouillon, soit 50 connexions successives, exposées aux limites de connexion des hébergeurs (seuils : à vérifier) ; une panne d'authentification ou un 429 Gmail (gmail_service.py:172-181) ferait échouer les N contacts un par un - correction : méthode fournisseur `create_drafts(demandes)` qui ouvre une connexion, résout le dossier une fois et dépose en boucle ; Gmail en séquentiel ; arrêt au premier échec auth, quota ou dossier et après 3 échecs réseau consécutifs, reste classé « non tenté » ; un seul nouvel essai avec attente croissante sur 429.
23. P2 - docs/plans/2026-09-24-p096-brouillons-en-lot.md:96 - Q4 : oui, mais pas « grâce au registre » seul - preuve : task_registry.py:182-184 un redémarrage pose INTERRUPTED avec un texte générique ; traitements.py:61-165 aucun état par contact - correction : réponse 202 (point 1), table des items (point 2), compte rendu relisible hors du panneau (point 14) ; à la reprise, l'item en_cours devient « incertain » ; « Reprendre le lot » ne relance que non_tenté et les échecs réessayables.
24. P3 - docs/plans/2026-09-24-p096-brouillons-en-lot.md:94 - Q2 : recommandation, 50 convient pour une TPE ; le quota d'API Gmail n'est pas le goulot (coût unitaire de drafts.create et plafond par utilisateur : à vérifier dans la documentation Google), le goulot est IMAP (point 22) - preuve : imap_smtp_provider.py:604-608 une connexion par brouillon - correction : une constante serveur unique (50), lue par l'écran, testée aux deux bords (50 accepté, 51 refusé).
25. P3 - docs/plans/2026-09-24-p096-brouillons-en-lot.md:43-44 - « Même fonction pour l'aperçu et l'exécution » ne garantit pas le même texte : une fiche ou une Variable modifiée entre l'aperçu et la création change les brouillons sans nouvel aperçu - preuve : variables_service.py:346-353 `variables_revision` existe pour ce cas en V1 et n'est pas repris ; `Contact.updated_at` (entities.py:56) - correction : l'aperçu rend une empreinte (révision des Variables + max(updated_at) des contacts + hash du modèle), la création la renvoie, le moteur refuse un lot périmé (409 « l'aperçu n'est plus à jour »).
26. P3 - docs/plans/2026-09-24-p096-brouillons-en-lot.md:62-63 - « Bornes V1 : 20 jetons, 60 000 caractères » : par objet, par corps ou cumulées ? et l'objet n'a pas de borne propre - preuve : variables_service.py:284-285 et :304-329 bornes appliquées par appel de resolve_text - correction : écrire que resolve_text s'applique séparément à l'objet (list_mode inline, longueur maximale après fusion à fixer) et au corps ; une erreur de borne refuse le lot, pas un contact.
27. P3 - src/frontend/src/lib/lexiqueParcours.test.ts:38-39 - « Le test E3 est mis à jour en conséquence » (design l.27) : E3 ne lit que TITRES_ETABLI, changer le nom accessible ne le fait ni tomber ni progresser ; le titre « Écrire un message » et l'en-tête « Nouveau message » gardent l'ambiguïté message ou e-mail - preuve : lexiqueParcours.test.ts:28-40 ; etabli.ts:45 ; EmailConversationCard.tsx:364 et :421 (« Brouillon de réponse » affiché aussi en rédaction libre, et le serait en lot) - correction : TITRES_ETABLI.email en « Écrire un e-mail » (passe la regex E3) ; ajouter à E3 un cas sur le nom accessible (contient « e-mail », commence par le libellé visible) ; titre de section « Brouillon » en rédaction libre et « Modèle du lot » en mode plusieurs.
28. P3 - docs/plans/2026-09-24-p096-brouillons-en-lot.md:10-12 - Les brouillons ne porteront pas la signature du compte : THÉRÈSE ne l'ajoute qu'à l'envoi - preuve : email.py:1302-1312 `signature_html` injectée dans send_email seulement ; create_draft (email.py:1359-1432) n'y touche pas ; EmailConversationCard.tsx:338 corps en texte brut ; qu'un client ajoute la signature en expédiant un brouillon existant : à vérifier (Gmail, Apple Mail) - correction : trancher (signature texte ajoutée au brouillon, ou mention « sans signature » dans l'aperçu) et fixer `html: false` pour le lot, les champs du contact n'étant pas échappés.
29. P3 - src/backend/app/services/email/imap_smtp_provider.py:606-608 - Le brouillon IMAP est déposé sans le drapeau \Draft, et sans UIDPLUS il reçoit un identifiant de synthèse à la seconde, identique pour les brouillons du lot créés dans la même seconde - preuve : `mailbox.append(msg.as_bytes(), ..., dt=datetime.now())` sans flag_set ; l.612-614 `draft_{%Y%m%d%H%M%S}` - correction : `flag_set=[MailMessageFlags.DRAFT]` (effet sur Apple Mail et Outlook : à vérifier) ; ne jamais utiliser draft_id comme clé, la clé d'item est (batch_id, contact_id).
30. P3 - docs/plans/2026-09-24-p096-brouillons-en-lot.md:15-16 - « Ne garde aucune copie des brouillons hors de la boîte » : vrai pour le lot, faux dès qu'un de ces brouillons est ouvert dans THÉRÈSE, qui met le message et son corps en cache - preuve : email.py:1234-1242 et :1265-1277 mise en cache d'un message consulté - correction : reformuler (« le lot n'écrit aucun objet ni corps dans la base ») et vérifier que la table des items n'en stocke pas.
31. P3 - docs/plans/2026-09-24-p096-brouillons-en-lot.md:77 - « Comme la suppression dans les fiches » : précédent non retrouvé dans les fiches contact, à vérifier ; le patron réel de lecture seule en démo est src/frontend/src/components/memory/ProjectModal.tsx:392 `fieldset disabled={demoEnabled}` avec son message l.364-366 - preuve : aucune suppression conditionnée à demoEnabled trouvée dans components/crm ni components/memory - correction : citer le précédent exact et garder le bouton « Créer » visible mais inactif avec la raison, pour ne pas faire croire que la fonction n'existe pas.
32. P3 - docs/plans/2026-09-24-p096-brouillons-en-lot.md:84-85 - « Aucun envoi » est tenu par le code actuel, mais l'espion prévu (« sur send ») ne vise pas le fond - preuve : création IMAP = `mailbox.append` seul (imap_smtp_provider.py:600-620), Gmail = POST users/me/drafts (gmail_service.py:350) ; l'envoi passe par `aiosmtplib.send` (imap_smtp_provider.py:481) et POST users/me/messages/send (gmail_service.py:256) - correction : espionner `aiosmtplib.send`, `GmailService.send_message` et toute URL contenant `/send`, en plus de `send_message` des deux fournisseurs.
VERDICT : NO-GO
